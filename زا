
Change ONLY the AI Model Selector behavior and layout.

The current ChatPop model selector opens as a bottom sheet from the bottom of the screen.

I do NOT want that behavior.

Use the attached reference screenshot only to understand the interaction and positioning. Do not copy its branding or exact design.

MODEL SELECTOR BEHAVIOR

When the user taps the AI model selector in the top navigation bar:

- Open a floating model panel directly BELOW the model selector button.
- The panel should visually originate from the top selector.
- Opening animation should go TOP → DOWN.
- Do NOT open it from the bottom.
- Do NOT use a bottom sheet.
- Do NOT make it full-screen.

The Chat screen should remain visible behind the floating panel.

The panel should have a fixed maximum height so it does not extend beyond the usable iPhone screen.

SCROLLING

The content INSIDE the model panel must be vertically scrollable.

The panel itself stays positioned under the model selector while the user scrolls through the available AI providers/models.

This is important because later we will have many models.

Structure it so it can contain:

AUTO
- Auto
  ChatPop chooses the best AI

CHATGPT
- ChatGPT models

CLAUDE
- Claude models

GEMINI
- Gemini models

DEEPSEEK
- DeepSeek models

Later we will add multiple specific models under every provider, so make the list architecture expandable.

The selected model should:
- Have a clear selected state
- Show a checkmark
- Update the model name displayed in the top selector

INTERACTION

- Tap model selector → panel opens downward.
- Scroll vertically INSIDE the panel.
- Tap a model → select it.
- Tap outside the panel → close it.
- Tap the selector again → close it.

Keep the opening/closing animation smooth and iOS-like.

NATIVE iOS GLASS

Continue using the REAL native iOS glass/material implementation we requested.

Do NOT replace it with custom fake CSS glass.

Use the native iOS glass/material solution already added to the project for:
- Model selector button
- Floating model panel
- Relevant model controls

If additional native configuration or compatible libraries are required for the existing native glass implementation, configure them as needed.

Do NOT recreate the iOS project and do NOT change:
- Bundle Identifier
- Signing
- Development Team
- Existing working Capacitor/iOS structure

Keep the existing real-iPhone build working.

Do not change any unrelated part of the Chat screen.

After finishing:
- Build
- Sync the EXISTING iOS project
- Verify there are no errors
- Stop and report the changes