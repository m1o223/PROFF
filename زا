The camera functionality is good now. Do not change the camera flow.

The remaining problem is discoverability: the two bottom circular buttons currently look like empty gray circles, so a new user will not know what they do.

I know what they do because I designed them, but a first-time user will not.

Keep the current layout and native material/glass styling, but add clear native iOS symbols inside the controls.

Left bottom button:

* represents exit/cancel camera;
* use a clear native iOS close/back symbol;
* preferably xmark or an appropriate back/close SF Symbol;
* tapping it should immediately dismiss the camera and return to BlueMind without creating an attachment.

Right bottom button:

* represents switching between front and rear cameras;
* use the standard native iOS camera-switch SF Symbol;
* something equivalent to arrow.triangle.2.circlepath.camera / native camera rotation icon;
* tapping it should switch cameras with the current functionality preserved.

Center:

* keep the large white shutter button exactly as the main capture action.

Requirements:

* icons must be clearly visible;
* use white/system foreground;
* centered perfectly inside the circular glass buttons;
* do not make icons oversized;
* preserve comfortable iOS touch targets;
* keep the current native glass/material appearance;
* do not redesign the camera screen;
* do not change capture quality or camera logic.

The goal is that a completely new user understands the controls instantly without explanation.

Verify on a real iPhone:

1. Left button is obviously “close/exit”.
2. Right button is obviously “switch camera”.
3. Both actions still work.
4. No regression to camera speed or image quality.
