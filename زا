TASK: Change the main BlueMind Settings presentation on iOS to an almost full-screen native-style sheet, similar to the ChatGPT Settings presentation shown in the reference.

IMPORTANT:
Do NOT change the Settings content structure yet.
This task is ONLY about the main Settings container / presentation geometry.

====================================================
CURRENT PROBLEM
====================================================

BlueMind Settings currently opens as a smaller floating modal/card over the chat/menu.

I do NOT want that.

I want Settings to behave like a large iOS sheet that occupies almost the entire screen.

The ChatGPT reference shows the intended behavior:

- Settings covers nearly the full display.
- Only a small amount of the background remains visible around/above it.
- The Settings surface itself becomes the main screen.
- Internal content scrolls inside the sheet.
- It does not visually feel like a small popup.

====================================================
TARGET GEOMETRY
====================================================

On iPhone:

Settings should open to approximately 90–97% of the available screen height.

It should begin near the top safe-area region, leaving only a small visual gap.

The left and right edges should also sit very close to the screen edges.

Do NOT leave large margins around the Settings sheet.

The result should feel like:

almost-full-screen iOS sheet

NOT:

centered card
floating popup
small glass modal

====================================================
TOP SHAPE
====================================================

Keep smooth native-style rounded top corners.

The top-left and top-right corners should have a large soft radius.

The bottom can extend naturally to the bottom of the screen/safe area.

Do not make the whole Settings view look like a standalone rounded rectangle floating in the center.

====================================================
BACKGROUND BEHIND SETTINGS
====================================================

When Settings is open:

The underlying BlueMind menu/chat should be heavily dimmed.

The user may perceive that something exists behind the sheet, but must NOT be able to comfortably read the text behind it.

Use a strong dark scrim / system-style dimming treatment.

Do not make the background completely visually disconnected, but it should be functionally unreadable.

====================================================
SETTINGS BACKGROUND
====================================================

The Settings sheet should use a deep dark neutral gray/black surface similar to iOS Settings-style dark surfaces.

No blue tint.

No transparent glass so strong that chat text remains readable underneath.

The main Settings surface should feel solid enough to become the dominant layer.

====================================================
CLOSE BUTTON
====================================================

Keep the X control in the upper-right.

It must remain fixed relative to the Settings sheet header and must not scroll away with the content.

The X closes ONLY Settings.

====================================================
SCROLLING
====================================================

The entire Settings content must scroll inside this large sheet.

Header/close behavior should remain stable.

Do not scroll the underlying page.

Do not allow the chat/menu behind Settings to move while Settings is open.

====================================================
DISMISS BEHAVIOR
====================================================

Settings may close ONLY by:

1. Tapping the main Settings X
2. Native downward swipe / sheet dismissal gesture, if intentionally supported

Do NOT close Settings when:
- opening a child selector
- choosing Appearance
- changing color
- changing Voice
- changing notification settings
- opening another Settings subsection

Child screens/selectors must return to the Settings sheet.

====================================================
IOS IMPLEMENTATION
====================================================

On iOS, prefer a real native large-sheet / sheet-presentation architecture if the current native bridge allows it.

Do not simulate the geometry using random CSS transforms if a native iOS presentation controller can own the container.

If the Settings content itself remains React/WebView-based, that is fine, but the outer presentation must behave like a proper iOS almost-full-screen sheet.

====================================================
DO NOT CHANGE
====================================================

Do NOT redesign:

- Settings sections
- icons
- text
- profile content
- account rows
- Appearance
- Voice
- Notifications
- Help
- Logout

Only change the main Settings container size, positioning, dimming, scrolling behavior and dismissal behavior.

====================================================
REFERENCE BEHAVIOR
====================================================

Use the provided ChatGPT screenshot as the geometry/behavior reference.

I do NOT want a pixel-for-pixel visual copy.

I want the same presentation concept:

small top gap
almost entire screen covered
large continuous Settings surface
internal scrolling
fixed close button
very little readable content visible behind it

====================================================
REAL IPHONE TEST
====================================================

Test on the connected real iPhone.

Verify:

1. Settings opens almost full-screen.
2. Only a small background area remains visible.
3. Background text is not readable.
4. Settings scrolls smoothly.
5. Underlying menu/chat does not scroll.
6. X stays available.
7. Child selectors do not dismiss Settings.
8. Downward swipe behavior works only if intentionally implemented.

At the end report:
- old Settings container dimensions
- new presentation architecture
- percentage/geometry used
- background dimming approach
- whether native sheet presentation is being used
- real-iPhone result
