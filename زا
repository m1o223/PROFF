Update ONLY the Writing Mode UI based on the current implementation.

Do not redesign the feature and do not change its backend/AI behavior.

I want Writing Mode to visually belong to the exact same iOS chat design system as Normal Chat.

1. MOVE THE FOUR QUICK ACTIONS

The four current actions:

- Write an email
- Improve text
- Rewrite professionally
- Translate

are currently positioned around the middle of the screen.

This is wrong.

Move them DOWN so they sit directly ABOVE the bottom message composer.

They should visually belong to the composer area.

Keep a small consistent gap between:
Quick Actions
and
Composer.

They must NOT float in the center of the empty screen.

Make them lighter and less visually dominant.

Prefer compact horizontally arranged chips/buttons.
Allow horizontal scrolling if needed rather than making huge buttons.

Do not let them overlap the composer or keyboard.

When the conversation starts, these suggestions may disappear/collapse so the conversation gets the available space.

2. USE THE NORMAL CHAT COMPOSER

Replace the current Writing Mode input implementation with the SAME shared composer/component used by Normal Chat.

Do NOT create another composer that only looks similar.

Writing Mode should reuse the same source-of-truth component.

It must have the same:

- Apple/iOS glass material
- border
- thin highlight/stroke
- corner radius
- shadow
- internal spacing
- + button
- microphone button
- send arrow
- icon sizing
- disabled/active send state
- safe-area handling
- keyboard behavior
- multiline behavior

Writing Mode may configure a slightly larger INITIAL/minimum text area if this is useful for writing.

However, do NOT turn it into the current huge rectangular input.

Preferred behavior:

Normal Chat composer foundation
+
slightly more comfortable writing height if necessary
+
automatic multiline expansion.

It should still unmistakably be the BlueMind chat composer.

Keep the Writing-specific placeholder:

“Ask BlueMind to write…”

or the existing equivalent.

3. STANDARDIZE ALL BUTTONS TO THE BLUE MIND IOS GLASS SYSTEM

Inspect Normal Chat and identify the exact glass/native-backed button component/style it uses.

Writing Mode controls must use the same design language.

This includes:

- hamburger/menu button
- Writing Type selector
- Writing Tools button
- Quick Action chips
- + attachment button
- microphone
- send button
- popup/menu controls

They should have the same:

- glass material
- thin border/stroke
- highlight
- radius language
- icon weight
- press feedback
- opacity
- spacing

Do NOT create a separate “Writing Mode glass style”.

There should be ONE BlueMind iOS glass language throughout the app.

4. WRITING TYPE MENU

Keep all existing Writing Types and functionality.

Do not remove:

Email
Report
Essay
Story
Professional
Social Media
Templates
CV
Other

But make the menu visually consistent with the same iOS glass system.

Avoid an unnecessarily huge floating rectangle.

Keep:
- readable spacing
- scrollability if necessary
- native/iOS feeling
- clear selected state

Do not change the actual feature logic.

5. WRITING TOOLS MENU

Keep:

- Personal Writing Style
- Change Writing Type
- Start New Writing Chat

But standardize this popup/menu to the same iOS glass language.

Do not make it visually heavier than the rest of the screen.

6. MAIN EMPTY STATE

Keep the Writing Mode identity in the center before the conversation starts:

Writing Mode
Write, rewrite, improve, or translate anything.

But REMOVE the four Quick Actions from this central area.

The middle should become cleaner.

The four Quick Actions belong immediately above the composer.

Once conversation messages exist, the large empty-state content should disappear/collapse and the conversation should use that space.

7. HEADER

Keep the current Writing Mode header concept.

But make sure its buttons and selector use the exact same BlueMind iOS glass system as Normal Chat.

If the selected type is Email, avoid ugly truncation such as:

Writing Mode · E...

Use a cleaner compact title such as:

Writing · Email

or another layout that fits naturally without awkward truncation.

Do not change navigation behavior.

8. KEYBOARD

Test on the physical iPhone.

When keyboard opens:

Quick Actions
↓
Composer
↓
Keyboard

must stack correctly.

No overlap.
No duplicated composer.
No floating controls over each other.
No giant empty gap.
No content hidden behind the keyboard.

The composer must remain directly above the keyboard.

9. IMPORTANT ARCHITECTURE RULE

Do NOT solve this by copying CSS from Normal Chat into Writing Mode.

Reuse shared components/tokens/native-backed glass wherever possible.

The goal is:

ONE BlueMind iOS glass design system.
ONE shared chat composer.
Different content/features for each AI mode.

10. DO NOT TOUCH

Do not change:

- Writing AI logic
- prompts
- backend
- persistence
- authentication
- Normal Chat behavior
- AI Plan
- Private Chat
- Hidden Chat
- Projects
- unrelated screens

This task is Writing Mode UI/layout only.

11. FINAL RESULT

The final Writing Mode should visually read as:

HEADER
[Menu] [Writing · Email ▼] [Writing Tools]

CENTER / CHAT AREA
Writing Mode
Write, rewrite, improve, or translate anything.

Then near the bottom:

[Write an email] [Improve text] [Rewrite professionally] [Translate]
                  ↓ small gap
[ + | Ask BlueMind to write… | microphone | send ]
                  ↓
              iOS safe area

Once chatting starts:

HEADER
MESSAGES
QUICK ACTIONS if appropriate
SHARED COMPOSER

The Writing Mode should feel like Normal Chat specialized for writing — not a separate app inside BlueMind.

After implementation, report:
- which shared composer is being reused
- which shared glass components are being reused
- whether any Writing-specific duplicate composer/CSS remains
- physical iPhone keyboard test result
- files changed
