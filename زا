Task: Rebuild ONLY the Settings selection popups for:
1. Appearance
2. App Accent Color
3. Chat Color

Do NOT redesign the rest of Settings in this task.

====================================================
GOAL
====================================================

I want these three Settings selectors to behave and look like modern native iOS selection bubbles / popovers.

The current implementation uses large heavy panels that feel too web-like.

Replace them with compact, floating, rounded iOS-style glass selection popups.

====================================================
1. APPEARANCE
====================================================

When the user taps the Appearance row:

Open a compact iOS-style glass popup.

The popup should contain exactly:

- System
- Dark
- Light

Each option should have:
- its existing icon
- label
- generous touch target
- soft rounded shape
- native-looking glass material

The currently selected option should show a checkmark.

Do NOT use a giant full-width panel.

Do NOT cover most of the Settings page.

The popup should feel like a floating Apple-style selection bubble.

====================================================
2. APP ACCENT COLOR
====================================================

When the user taps App Accent Color:

Open the same style of iOS glass popup.

Each color option should show:
- circular color swatch
- color name
- checkmark on the currently selected color

Use the existing available accent colors and preserve current functionality.

Do NOT redesign the color palette in this task.

Only change the presentation.

====================================================
3. CHAT COLOR
====================================================

When the user taps Chat Color:

Use the exact same popup system.

Each row should contain:
- circular color swatch
- color name
- current selection checkmark

The current large color sheet/panel should be replaced by this compact Apple-style floating popup.

====================================================
VISUAL LANGUAGE
====================================================

All three selectors must use ONE shared popup style.

Use:
- dark graphite glass in Dark Mode
- correct light equivalent in Light Mode
- strong blur
- soft translucent surface
- large smooth corner radius
- subtle edge/highlight
- no harsh borders
- no flat black rectangles

The underlying Settings content may remain slightly visible through blur, but must not compete visually with the popup.

====================================================
SIZE / POSITION
====================================================

The popup should be compact.

Do not make it full-screen.

Do not make it extend unnecessarily from top to bottom.

Place it naturally near the tapped row while keeping it inside the safe screen area.

If there is not enough room below:
- position it above or adjust intelligently

Do not clip against screen edges.

====================================================
SELECTION BEHAVIOR
====================================================

When a user taps an option:

- update the selection immediately
- show the checkmark on the new choice
- apply the existing setting logic
- close the popup smoothly after selection

For Appearance:
- System / Dark / Light should apply immediately

For colors:
- accent/chat color should update using existing logic

Do NOT create duplicate state systems.

====================================================
ANIMATION
====================================================

Use a subtle iOS-style presentation animation.

Allowed:
- slight scale-in
- fade
- soft spring

Not allowed:
- large bounce
- sliding across the whole screen
- zoom from full-screen
- aggressive animation

Dismiss smoothly.

====================================================
DISMISSAL
====================================================

The popup should close when:

- user selects an option
- user taps outside the popup
- user taps close if a close control is retained
- Settings is closed

No stale overlay should remain.

====================================================
LAYERING
====================================================

Very important:

The selection popup must render ABOVE:
- Settings content
- chat composer
- + button
- chat UI
- any underlying screen controls

Nothing from the app behind Settings may appear above this popup.

Use the correct presentation/stacking hierarchy.

Do not solve this with random huge z-index values.

====================================================
DO NOT CHANGE
====================================================

Do NOT change:
- Settings main container
- profile area
- Change Email
- Change Password
- Subscription
- Delete Account
- General
- Language
- titles
- icons themselves
- existing setting logic/data

Only rebuild the presentation of:

- Appearance selector
- App Accent Color selector
- Chat Color selector

====================================================
SHARED IMPLEMENTATION
====================================================

Create/reuse ONE shared iOS selection popover component.

Do not build three unrelated implementations.

The same component should support:
- icon options
- color swatches
- labels
- checkmarks

====================================================
REAL IPHONE TEST
====================================================

Test on the connected real iPhone:

APPEARANCE:
1. Open Appearance
2. Confirm System/Dark/Light show correctly
3. Select each option
4. Confirm checkmark moves
5. Confirm theme updates
6. Confirm popup closes cleanly

APP ACCENT COLOR:
1. Open selector
2. Confirm colors render
3. Select multiple colors
4. Confirm checkmark/state updates
5. Confirm popup closes

CHAT COLOR:
1. Open selector
2. Confirm color rows render
3. Select multiple colors
4. Confirm correct chat color updates
5. Confirm popup closes

Also verify:
- tap outside dismisses
- no clipping
- no composer/+ above popup
- Dark Mode correct
- Light Mode correct

At the end report:
- shared popup component used
- old panels removed/replaced
- files changed
- how selection state is preserved
- real iPhone test result
