The previous change did not solve the actual UI behavior.

You changed the iOS maximum-height calculation from a fixed value to a dynamic availableHeight, but in practice the composer still does not expand upward as requested.

This means the limitation is probably not only the maxHeight calculation.

I want you to inspect the entire layout chain that contains the iOS composer and find what is physically preventing it from growing upward.

Do not just increase numbers again.

Check specifically for:

* parent container height constraints;
* .frame(height:), .frame(maxHeight:), fixed frames;
* GeometryReader bounds;
* VStack/HStack layout constraints;
* Spacer behavior;
* safe-area insets;
* keyboard container restrictions;
* clipping;
* .clipped();
* masks;
* parent overflow hidden / clipping equivalents;
* z-index/layering;
* whether the composer is inside a container that only allows bottom growth;
* whether the drag gesture changes internal content height but not the outer container;
* whether the expanded composer is being pushed downward instead of growing upward;
* whether another wrapper view is constraining its top edge.

Desired result:

When dragging the top handle upward, the top edge of the composer itself must move upward.

The bottom edge should remain anchored above the keyboard.

In other words:

bottom stays fixed
while
top moves upward

as the composer height increases.

The composer should be able to overlap the carousel area and reach approximately the middle of the AI Plans card, or slightly above its middle.

The carousel must NOT act as a collision boundary.

Do not push the carousel upward.

Do not resize the whole page.

The composer should behave like a foreground overlay surface.

Please inspect the actual rendered hierarchy and identify the exact view/container that is constraining upward expansion.

If necessary, move the iOS composer into a more appropriate overlay/ZStack layer so its bottom remains keyboard-anchored while its top can freely expand upward.

Keep:

* current downward resize behavior;
* current handle;
* auto-grow;
* internal text scrolling;
* microphone;
* attachments;
* send button;
* keyboard behavior.

Do not modify the website.

After fixing it, report:

1. What exact container/constraint was preventing upward expansion.
2. Why the previous availableHeight change did not visually work.
3. What layout change was made.
4. Confirmation that the bottom edge remains anchored above the keyboard.
5. Confirmation that the top edge can now move upward over the carousel area.
6. Confirmation that the composer can reach approximately the middle/slightly above the middle of the AI Plans card.
7. Confirmation that no web behavior was changed.
