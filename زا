PROJECT FILES — PRODUCTION-QUALITY iOS IMPLEMENTATION. READ EVERYTHING BEFORE CHANGING CODE.

We are now implementing Project → Files ONLY.

This is not a placeholder task and not a visual mockup.

I want a complete, serious, production-quality iOS Files workspace for each Project.

DO NOT touch Chats, Tasks, Notes, Project Overview, Web, or unrelated backend code.

Before writing code, inspect the existing Project model, file APIs, attachment system, storage behavior, iOS file picker, file preview infrastructure, and current project routing.

Do not assume anything exists. Verify it first.

⸻

CORE RULE

Files opened/added from:

Project X → Files

must belong to Project X.

Never mix files between Projects.

Every operation must preserve the current project identity / projectId / equivalent existing identifier.

If the backend does not currently support project-scoped files, STOP before inventing fake persistence and report exactly what is missing.

⸻

1 — PAGE DESIGN

Replace the current placeholder Files screen completely.

Build a polished native iOS Files workspace using BlueMind’s established visual language:

* black/dark background
* native iOS typography
* SF Symbols
* compact Liquid Glass/material where appropriate
* proper safe areas
* BlueMind primary #193B68 only where an accent is actually needed
* no Web-looking cards/buttons
* no unnecessary giant empty areas

The page should clearly communicate:

current Project → Files

Include:

* native/glass back button
* project context/name
* Files title
* compact + / Add File action
* native search

You have design freedom within these requirements. Make it look like a serious first-party iOS workspace.

⸻

2 — FILE LIST

Display the Project’s real files.

Each item should expose useful information when available:

* thumbnail or file-type icon
* filename
* extension/type
* size
* added/modified date

Handle long filenames correctly without breaking layout.

Support at least the file types already supported by the application.

Examples may include:

* PDF
* images
* TXT
* documents
* other existing supported attachments

Do not claim support for formats the application cannot actually process.

⸻

3 — ADD FILE

Add File must use the appropriate native iOS file selection experience.

Flow:

Project Files
→ Add File
→ native picker
→ select file
→ return immediately to Project Files
→ show upload/progress state if needed
→ real success/failure result

Do NOT send the user into an old Web file screen.

Do NOT create duplicate attachments if callbacks fire twice.

Do NOT show File added before persistence/upload actually succeeds.

Use the global BlueMind toast system for final feedback.

⸻

4 — PROJECT OWNERSHIP IS CRITICAL

Audit this carefully.

A file added inside Project A must NEVER appear in Project B unless explicitly shared by an existing backend feature.

Test with at least two projects:

Project A

add test-A.pdf

Project B

add test-B.pdf

Then verify:

Project A → only its appropriate files

Project B → only its appropriate files.

If project scoping is currently unsupported by the backend, report that instead of faking frontend filtering.

⸻

5 — FILE PREVIEW

Tapping a file should open a proper iOS-native/in-app preview whenever technically supported.

Examples:

Image
→ high-resolution fullscreen image viewer.

PDF
→ proper PDF preview/viewer with scrolling/zoom.

Text
→ readable native presentation where supported.

Other supported documents
→ appropriate iOS preview mechanism.

Do not render a blurry thumbnail as the fullscreen file.

Do not reuse the previous low-resolution image bug.

Fullscreen previews must use the original/full-resolution payload whenever available.

Do not route the user into the legacy Web UI.

⸻

6 — LOADING / QUALITY

File previews must not get stuck forever.

Implement proper:

* loading state
* success state
* error state
* retry where useful

A thumbnail may appear quickly in the list, but a thumbnail must never silently become the final fullscreen source when the original is available.

Preserve original file/image quality.

⸻

7 — FILE ACTIONS

Where supported by existing architecture, provide native iOS actions through context menu/swipe/menu:

* Rename
* Share
* Delete

Only expose an action if it actually works.

Do not create dead buttons.

Delete should require appropriate confirmation and only remove the intended Project file.

Rename must persist if backend rename support exists.

Share should use the native iOS share experience where appropriate.

⸻

8 — SEARCH

Add native-feeling search for the currently loaded Project files.

At minimum search filename locally.

Do not create a new backend search service merely for this task.

Search must remain scoped to the current Project.

⸻

9 — EMPTY STATE

Delete the current placeholder:

This project section is ready for its dedicated content in the next stage.

Replace it with a compact useful state such as:

No files yet

Add files to keep your project resources together.

[Add File]

Do not place a giant icon/message in the exact center of an otherwise empty screen.

⸻

10 — FUTURE AI CONTEXT

Architect the frontend/data flow so Project Files retain their Project association.

Later, BlueMind AI may use these files as Project context.

DO NOT implement AI file reading/RAG/vector search/context injection now.

This task is Files UI + existing real file functionality only.

⸻

11 — ARABIC / RTL

Test:

* Arabic filename
* English filename
* Swedish filename
* mixed Arabic + Latin filename

RTL must not break:

* icons
* file metadata
* menus
* navigation
* truncation

⸻

12 — ERROR HANDLING

Handle real failures.

Examples:

* upload fails
* unsupported file
* file unavailable
* preview fails
* network failure
* delete fails

Never silently fail.

Never claim success before the operation succeeds.

Use the global iOS toast system for short transient feedback.

⸻

13 — ABSOLUTELY DO NOT

Do NOT:

* build a static mockup
* leave dead controls
* invent fake files
* fake persistence with frontend-only state and call it complete
* modify the website
* rewrite working backend APIs unnecessarily
* modify Chats
* modify Tasks
* modify Notes
* modify Project Overview
* add AI/RAG
* add folders unless existing backend architecture already supports them
* add unrelated features

⸻

14 — REAL IPHONE ACCEPTANCE TEST

This task is NOT finished until the following has been tested on the real iPhone:

1. Project → Files opens.
2. Empty state is correct.
3. Add File opens the native picker.
4. Select a real file.
5. File appears only after the appropriate real operation succeeds.
6. Leave Files and return.
7. File is still there if persistence is supported.
8. Open the file.
9. Preview works at proper quality.
10. Close preview.
11. Search finds it.
12. Rename works if supported.
13. Share works if supported.
14. Delete works if supported.
15. Test a second Project and confirm files do not leak between Projects.
16. Test at least an image and PDF if supported.
17. Test Arabic/RTL.
18. Confirm Web is unchanged.

⸻

BEFORE YOU SAY “DONE”

Audit your own implementation.

Do not report completion merely because compilation passes.

Report separately:

UI implemented: yes/no

Real file picker tested: yes/no

Upload/persistence tested: yes/no

Project isolation tested: yes/no

Preview tested: yes/no

Rename tested: yes/no/not supported

Share tested: yes/no/not supported

Delete tested: yes/no/not supported

Real iPhone tested: yes/no

Web unchanged: yes/no

Then list any backend capability that is genuinely missing.

If something is not tested or unsupported, say so explicitly. Do not hide it behind “build passed”.

Stop after Project Files is complete. Do not continue to Tasks or Notes.
