postgresql://postgres.qfjnwxivmarecukjjqtg:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
We are now moving the EXISTING ChatPop backend from the local PostgreSQL database to the new REAL Supabase PostgreSQL cloud database.

IMPORTANT:
Do NOT rebuild the backend.
Do NOT change the database technology.
Do NOT create a second backend.
Do NOT touch the frontend or iOS project.

I am providing the Supabase PostgreSQL Session Pooler connection string below.

SUPABASE DATABASE CONNECTION STRING:

PASTE_MY_SUPABASE_CONNECTION_STRING_HERE

IMPORTANT SECURITY RULES:
- Treat this connection string as a SECRET.
- Do NOT print it in terminal output unnecessarily.
- Do NOT include it in your final response.
- Do NOT commit it to Git.
- Do NOT put it in .env.example.
- Do NOT expose it to the frontend/iOS app.
- Do NOT log the database password.

==================================================
1. CONFIGURE DATABASE_URL
==================================================

Use the supplied Supabase connection string as the backend DATABASE_URL.

Store it ONLY in the existing backend local environment file:

chatpop-backend/.env

For example:

DATABASE_URL=<the Supabase connection string I provided>

If the connection string still contains:

[YOUR-PASSWORD]

replace that placeholder with the real Supabase database password already configured locally / supplied by me.

If the password contains special URL characters, percent-encode them correctly.

Do NOT modify the actual username/host/port/database unless technically necessary.

==================================================
2. PRESERVE CURRENT BACKEND
==================================================

Use the EXISTING ChatPop backend architecture.

The backend already contains:
- Node.js
- TypeScript
- Fastify
- PostgreSQL
- migrations
- authentication
- users
- user settings
- sessions
- refresh tokens
- chats
- messages
- media
- security/ownership checks

Do NOT rewrite these.

We are only switching the database target from local PostgreSQL to Supabase PostgreSQL.

==================================================
3. FIRST TEST THE CONNECTION
==================================================

Before running migrations:

1. Load DATABASE_URL from .env.
2. Attempt a secure PostgreSQL connection to Supabase.
3. Verify:
   - connection succeeds
   - SSL/TLS requirements are handled correctly
   - database is reachable
   - correct database is selected

Do not run destructive operations before confirming connectivity.

==================================================
4. RUN THE EXISTING MIGRATIONS
==================================================

Once connection succeeds:

Run the EXISTING ChatPop database migrations against Supabase.

Do NOT manually recreate tables if migrations already define them.

Create all currently required tables, including whatever already exists in the project, such as:

- users
- user_settings
- sessions
- refresh_tokens
- chats
- messages
- media
- storage_deletions
- rate_limits

and any other tables defined by the existing migrations.

==================================================
5. VERIFY DATABASE STRUCTURE
==================================================

After migrations:

Verify that Supabase contains the expected:

- tables
- primary keys
- foreign keys
- unique constraints
- indexes
- timestamps
- ownership relationships

Pay special attention to:

users.email uniqueness

chats.user_id -> users.id

messages.chat_id -> chats.id

messages.user_id -> users.id

media.user_id -> users.id

session/refresh token ownership

==================================================
6. DO NOT USE SUPABASE CLIENT AUTH
==================================================

Important architectural rule:

Do NOT replace our existing ChatPop backend authentication with Supabase Auth in this task.

Our architecture remains:

iPhone / ChatPop App
        ↓
ChatPop Backend API
        ↓
Supabase PostgreSQL

NOT:

iPhone
↓
Supabase database directly

The frontend/iPhone must never receive the PostgreSQL DATABASE_URL.

==================================================
7. TEST REAL USER REGISTRATION
==================================================

After migration:

Run a real backend registration test against Supabase.

Create a test user through the EXISTING register API/service.

Verify:

- user row appears in Supabase PostgreSQL
- email is normalized
- password is NOT plaintext
- password_hash uses the current secure hashing architecture
- user settings are created correctly
- IDs and timestamps are valid

==================================================
8. TEST LOGIN
==================================================

Login using the same test account.

Verify:

- password verification succeeds
- session is created
- access/refresh flow works
- last_login_at updates correctly
- /me returns the correct safe user data

==================================================
9. TEST PERSISTENCE
==================================================

Very important:

Restart the backend.

Then verify that the same user still exists and can log in again.

We need proof that the data is stored in the Supabase cloud database and not only locally/in-memory.

==================================================
10. TEST USER SETTINGS
==================================================

Test:

GET user settings
UPDATE user settings

Verify changes persist in Supabase after backend restart.

==================================================
11. TEST CHATS
==================================================

Using the authenticated test user:

- create a chat
- list chats
- rename a chat
- search chats
- fetch a chat

Verify all chat data persists in Supabase.

==================================================
12. TEST MESSAGES
==================================================

Save at least:

- one user message
- one assistant/mock message if supported by current persistence API

Verify:
- messages are linked to the correct chat
- user ownership is preserved
- data persists after restart

Do NOT connect AI providers in this task.

==================================================
13. TEST OWNERSHIP SECURITY
==================================================

Create two test users:

User A
User B

Verify:

- User A cannot read User B's chats
- User A cannot read User B's messages
- User A cannot access User B's media metadata
- User B cannot access User A's data

These ownership protections must still work after moving to Supabase.

==================================================
14. MEDIA TABLE
==================================================

Verify the existing media metadata table works against Supabase.

Do NOT upload large video/image binary files into PostgreSQL.

Continue using the existing architecture where PostgreSQL stores metadata/storage references only.

Actual object storage will be connected separately later.

==================================================
15. KEEP LOCAL DATABASE AS FALLBACK FOR NOW
==================================================

Do NOT delete the existing local PostgreSQL setup yet.

Keep it untouched until the Supabase migration and tests are fully confirmed.

Once Supabase is verified, Supabase should become the main DATABASE_URL for this environment.

==================================================
16. .ENV SAFETY
==================================================

Ensure:

.env is ignored by Git.

.env.example contains only something like:

DATABASE_URL=
PORT=
...

No real connection string.
No real password.

Run a Git check to make sure the Supabase connection string has not been accidentally staged or committed.

==================================================
17. HEALTH / READINESS
==================================================

Verify the backend health/readiness endpoint using the new Supabase connection.

The database readiness check should succeed.

Do not expose database credentials or host details publicly through /health.

==================================================
18. DO NOT TOUCH
==================================================

Do NOT modify:

- chatpop-frontend
- React UI
- Capacitor
- iOS native UI
- Xcode
- Bundle Identifier
- Development Team
- Signing
- Creative Studio
- voice UI
- subscription UI

This task is ONLY:

Existing ChatPop Backend
→ Supabase PostgreSQL

==================================================
19. DEFINITION OF DONE
==================================================

This task is complete only when:

- backend successfully connects to Supabase
- existing migrations successfully run
- all expected tables exist
- registration works
- login works
- /me works
- sessions work
- user settings persist
- chats persist
- messages persist
- ownership tests pass
- backend restart does not lose data
- no secrets are committed

==================================================
20. FINAL REPORT
==================================================

When finished, STOP.

Report only safe information:

1. Whether Supabase connection succeeded
2. Which migrations ran
3. Which tables were created
4. Whether registration passed
5. Whether login passed
6. Whether /me passed
7. Whether settings persistence passed
8. Whether chats/messages persistence passed
9. Whether ownership/security tests passed
10. Whether restart persistence passed
11. Whether .env is safely ignored by Git
12. Any remaining issue

DO NOT include:
- DATABASE_URL
- password
- host credentials
- raw tokens
- secrets