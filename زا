We need to fix TWO issues in the voice recording / transcription flow.

IMPORTANT:
Do not make a superficial UI patch and do not guess the cause of the transcription error.
First inspect the existing implementation and all files related to:
- microphone / audio recording
- recorder state
- waveform visualization / animation
- stop / cancel recording
- audio file/blob creation
- upload/request to backend
- transcription API/backend route
- response parsing
- error handling

Then fix the actual causes.

==================================================
1. VOICE RECORDING WAVEFORM — MATCH CHATGPT BEHAVIOR
==================================================

This is the main visual/interaction issue.

Use the attached ChatGPT screen recording as the behavioral reference.

When recording starts, the waveform should behave like ChatGPT:

- The waveform must appear progressively from the recording-controls side and continuously travel/fill across the available waveform area.
- New audio bars should be generated continuously while the user is speaking.
- It must NOT appear as one static pre-rendered line.
- It must NOT suddenly stretch the entire waveform.
- It must NOT scale the existing bars horizontally.
- The bars should feel like a live stream of microphone amplitude data.

As recording continues:
- new bars enter continuously,
- older bars move across the waveform area,
- the animation stays smooth,
- the waveform should react naturally to microphone volume,
- silence = smaller bars,
- louder speech = taller bars.

The waveform must remain clipped inside its container and must never overlap:
- cancel/delete button
- stop button
- send button
- input/container edges

The controls themselves should stay fixed.
Only the waveform content should move/update.

Please study the reference recording carefully and reproduce the interaction behavior, timing and direction as closely as possible.

Do NOT rebuild unrelated parts of the composer/input UI.

Also verify:
- no layout jumping when recording begins
- no horizontal overflow
- no waveform escaping its mask/container
- stable 60fps-style animation where possible
- proper cleanup of animation/recorder state after cancel, stop, send, failure, or unmount

==================================================
2. "TRANSCRIPTION FAILED" — FIND THE REAL ROOT CAUSE
==================================================

Currently after recording, the UI shows:

"Transcription failed"

Do NOT simply hide this message or add retries without knowing the cause.

Trace the COMPLETE recording → transcription pipeline.

Check every stage:

1. microphone permission
2. MediaRecorder / native recorder initialization
3. supported audio MIME type / codec
4. audio chunks actually being produced
5. final Blob/File size
6. file extension
7. MIME type sent to backend
8. FormData field name
9. frontend request URL
10. backend route
11. authentication headers if required
12. Content-Type handling
13. backend multipart parsing
14. temporary file/buffer handling
15. transcription provider request
16. provider credentials/environment variables
17. provider response
18. backend response format/status
19. frontend response parsing
20. cleanup timing / accidentally deleting or resetting the audio before upload finishes

Add temporary diagnostic logs where necessary so we can identify exactly which stage fails.

Log useful information such as:
- recorder MIME type
- number of chunks
- resulting audio blob size
- request endpoint
- HTTP status
- backend error body
- transcription-provider status/error
- response payload

Never log secrets/API keys.

If the browser/device produces an audio format that the transcription service does not accept, fix the format/codec handling properly instead of applying a fake workaround.

Also check iOS/Safari behavior specifically because this app is being tested on iPhone.

If necessary, inspect frontend AND backend code. Do not assume the problem is frontend-only.

==================================================
EXPECTED RESULT
==================================================

After the fix:

1. Tap microphone.
2. Recording UI switches cleanly into recording mode.
3. The live waveform behaves like the ChatGPT reference: progressively generated, moving naturally, reacting to speech, with fixed controls.
4. Tap stop/send.
5. A valid audio recording is created.
6. The audio reaches the correct backend transcription endpoint.
7. Transcription succeeds.
8. The resulting text is inserted/displayed normally.
9. No "Transcription failed" appears unless there is a genuine external failure.
10. On genuine failure, show the real useful error in development logs while keeping the user-facing error clean.

Before modifying code, inspect the current architecture and identify the relevant files.
After implementing, report:
- which files were changed
- the exact root cause of the transcription failure
- how it was fixed
- how the waveform behavior was changed
- any iOS/Safari-specific issue you found

Do not change unrelated functionality or styling.
