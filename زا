STOP.

Do NOT modify anything except the conversation long-press behavior inside the iOS drawer.

This task is ONLY about making the conversation context menu behave exactly like a native iOS app (same interaction quality as the ChatGPT iOS app).

====================================================
GOAL
====================================================

Right now the long press behaves as if the entire drawer is reacting.

That is wrong.

The drawer must NEVER react.

ONLY the selected conversation row may react.

====================================================
EXPECTED BEHAVIOR
====================================================

Each conversation must behave like an independent native iOS list cell.

Every conversation row owns its own interaction.

The drawer is only the container.

The drawer must never animate when a conversation is long pressed.

====================================================
LONG PRESS
====================================================

When I long press a conversation:

ONLY that conversation becomes active.

The drawer must:

• NOT move
• NOT scale
• NOT bounce
• NOT blur more
• NOT dim differently
• NOT scroll
• NOT enter another interaction state

Everything stays completely still.

Only the selected conversation owns the interaction.

====================================================
CONTEXT MENU
====================================================

Use the native iOS context menu.

The menu should appear from the selected conversation naturally.

The appearance should feel native.

Small fade.

Small scale.

Nothing exaggerated.

No custom popup.

No custom animation.

No React imitation.

Use the real native iOS behavior.

====================================================
AFTER OPENING
====================================================

The conversation remains in place.

The drawer remains frozen.

Other conversations do not animate.

Nothing shifts.

Nothing repositions.

Nothing resizes.

====================================================
AFTER CLOSING
====================================================

When the context menu closes:

Everything returns instantly.

No flicker.

No redraw.

No layout jump.

No delayed animation.

No visible refresh.

====================================================
GESTURE OWNERSHIP
====================================================

The long press gesture belongs ONLY to the conversation row.

It must NOT belong to:

• Drawer
• ScrollView
• Container
• Overlay
• Parent View

Only the conversation cell.

====================================================
MENU OPTIONS
====================================================

Keep these actions:

• Pin
• Rename
• Add to Project
• Delete

Use the native iOS menu presentation.

====================================================
DO NOT MODIFY
====================================================

Do NOT change:

• Drawer colors
• Drawer layout
• Blur
• Search
• Header
• Buttons
• Animations outside this task
• Android
• Web

This task is ONLY about the conversation long press behavior.

====================================================
IF THERE IS OLD CODE
====================================================

If old gesture recognizers, old long-press handlers, text selection handlers, React handlers, or custom recognizers interfere with the native iOS interaction:

Find them.

Remove or replace them.

Do NOT layer new gesture code over old gesture code.

There must be ONE clean interaction path.

====================================================
REPORT
====================================================

When finished provide:

1. What component originally owned the long press.
2. Why the whole drawer reacted instead of only the conversation.
3. What old gesture code you removed or replaced.
4. What now owns the interaction.
5. Confirmation that the drawer itself no longer reacts.
6. Confirmation that Android and Web were NOT modified.
