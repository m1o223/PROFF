GLOBAL IOS TOAST SYSTEM — rebuild the old status message UI properly

I want to replace the current old in-page status/notification message system with a proper global native iOS-style toast/banner system.

This must be an iOS frontend system available across the entire app.

⸻

Current problem

Some status messages such as:

Project created

are currently rendered as large old custom boxes inside the page layout.

This looks outdated and is sometimes partially hidden by page content or controls.

I do NOT want these messages tied to individual page layouts anymore.

⸻

Goal

Build a reusable global toast/banner component for iOS.

It should appear for events such as:

* success
* error
* warning
* info
* saved
* created
* deleted
* uploaded
* copied
* login success/failure
* network/API failure
* any short user-facing status message

⸻

Visual design

The toast should feel like modern native iOS:

* small
* compact
* floating
* translucent glass/material
* rounded corners
* subtle blur
* subtle border/highlight
* soft shadow only if needed
* clean SF Symbol
* short readable text

Think of a compact iOS floating notification, not a large alert box.

Approximate concept:

[ icon   Message text ]

Example:

[✓  Project created]

or

[!  Something went wrong]

Do not make it full-width unless text absolutely requires it.

⸻

Placement

This is critical.

The toast must render in a global overlay layer above the entire app UI.

It must appear above:

* chat composer/input
* Studio composer
* Projects
* Smart Hub
* login screens
* side menu
* sheets
* modal content
* normal pages

It must NOT be clipped by a page container.

It must NOT sit underneath fixed inputs.

It must NOT depend on the current screen’s z-index.

Use a top-level native overlay/window/root host or equivalent clean architecture so the toast is truly global.

⸻

Position

Prefer a compact floating position near the top of the usable screen, respecting:

* Dynamic Island
* status bar
* safe area

It should not cover important navigation controls.

If top placement conflicts with a native element on a specific screen, choose a consistent safe offset.

Do not anchor it to individual page content.

⸻

Animation

Use native-feeling motion:

* quick fade/slide in
* brief visible duration
* smooth fade/slide out

Keep animation subtle.

Do not bounce excessively.

Example timing:

* enter: ~200–300ms
* visible: ~2–3 seconds
* exit: ~200ms

Error messages may stay slightly longer if needed.

⸻

Message types

Create a reusable API/state model such as:

showToast({ type, message })

Suggested types:

* success
* error
* warning
* info

Use SF Symbols or equivalent native icons.

Keep colors subtle and consistent with dark mode.

BlueMind primary can be used only where appropriate:

BLUE_MIND_COLORS.primary
#193B68

Do not color the whole toast bright blue.

⸻

Queue behavior

If multiple messages fire quickly:

* do not stack 5 giant banners on screen
* queue them cleanly
* show one at a time or use a controlled compact stack

Avoid duplicate identical messages firing repeatedly.

⸻

Global architecture

Before implementation, inspect how current status messages are shown across the app.

Find:

* page-specific success banners
* old alert boxes
* inline status messages
* duplicated notification code

Replace appropriate transient status messages with the new global toast system.

Do NOT replace actual forms, validation messages, or permanent page content that should remain inline.

⸻

Important

This is iOS frontend only.

Do NOT modify backend behavior.

Do NOT redesign unrelated pages.

Do NOT break Web.

If Web has its own toast system, leave it unchanged.

⸻

Example desired flows

Create Project
→ project creation succeeds
→ global glass toast appears:
✓ Project created

Login
→ success
→ ✓ Logged in

API error
→ ! Something went wrong

File upload
→ ✓ File uploaded

Copy
→ ✓ Copied

⸻

Real iPhone verification

Test the global toast on:

1. Normal Chat
2. Login
3. Projects
4. Smart Hub
5. BlueMind Studio
6. Side menu
7. A page with the keyboard/composer open

Verify:

* toast appears above everything
* not clipped
* readable
* safe-area correct
* no layout shift
* page does not resize
* input/composer remains usable
* message disappears automatically

⸻

Final report

Tell me:

1. which old status-message implementations were found
2. which ones were replaced
3. where the new global toast host lives
4. how z-order/overlay is guaranteed
5. how queueing works
6. confirmation Web was not changed

Stop after building the global toast system. Do not redesign unrelated UI.
