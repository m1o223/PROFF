Fix the current crash that happens when I tap Sign In / Log In / Sign Up in the ChatPop iOS app.

IMPORTANT:
Do NOT redesign anything.
Do NOT modify the backend.
Do NOT change the authentication flow visually.
Do NOT change Bundle ID, Signing, Development Team, Capacitor, or project structure.

This task is ONLY to diagnose and fix the native iOS crash.

The crash occurs inside:

NativeAuthViewController.swift

Xcode currently stops around this code:

let leftLine = UIView()
let rightLine = UIView()

for line in [leftLine, rightLine] {
    line.backgroundColor = .separator
    line.heightAnchor.constraint(equalToConstant: 1).isActive = true
}

leftLine.widthAnchor.constraint(equalTo: rightLine.widthAnchor).isActive = true

let separator = UIStackView(
    arrangedSubviews: [leftLine, divider, rightLine]
)

The exception shown by Xcode is an Auto Layout / NSLayoutConstraint exception similar to:

"Unable to activate constraint with anchors ..."

Inspect the full exception, but the likely cause is that the equal-width constraint between leftLine and rightLine is being activated BEFORE both views have been inserted into a common view hierarchy.

Fix this correctly.

For example, create/add the arranged subviews to the UIStackView first, then activate any constraint between leftLine and rightLine only after they share a common ancestor.

Do NOT merely hide or suppress the exception.

Also inspect the surrounding NativeAuthViewController code for any other constraints being activated between views before they share a common ancestor.

==================================================
REQUIRED VERIFICATION
==================================================

After fixing it, test the complete native auth navigation flow:

1. Open Settings
2. Tap Sign In
3. Authentication entry screen opens without crash
4. Tap Log In
5. Login screen opens
6. Go back
7. Tap Sign Up
8. Create Account screen opens
9. Back and Close buttons work
10. Repeat opening/closing the flow several times

There must be:
- No crash
- No freeze
- No Auto Layout exception
- No duplicated native auth views
- No broken constraints

Run the app on the existing iOS project / real-iPhone target if available.

Preserve all existing UI and functionality.

When finished, report:
- Exact root cause
- Exact file and lines changed
- How you fixed the constraint hierarchy issue
- Whether the auth flow now opens correctly
- Whether any other Auto Layout warnings remain

Do not implement new features in this task.
