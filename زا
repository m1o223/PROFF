Task: Menu Layering ONLY (Dark Mode ONLY)

Do NOT touch Light Mode.

Do NOT modify colors outside Dark Mode.

Do NOT modify animations.

Do NOT modify buttons.

This task is ONLY about the Menu layer appearance.

========================================================

Current issue:

The Menu does not feel like a separate iOS panel.

Compared to the ChatGPT iOS menu:

• The Menu should feel like a floating panel above the Chat.
• There should be a clear separation between the Menu layer and the Chat layer.
• The Chat must remain behind the Menu.
• The Menu must visually own its own surface.

========================================================

Dark Mode ONLY.

The Menu background should NOT be pure black.

Use a premium dark material (dark gray / graphite style).

It should feel like an iOS floating panel.

Do NOT change Light Mode.

========================================================

The right edge of the Menu must become the clipping boundary.

Nothing from the Chat should visually leak through that edge.

No controls.

No buttons.

No input.

No icons.

Everything behind the Menu must remain behind it.

========================================================

The Menu should feel like an independent panel floating above the application.

The Chat should remain visible on the right (about 15–20%), but clearly behind the Menu layer.

There should be a clean visual separation between both layers.

========================================================

Audit the current masking / clipping implementation.

If previous clipping or overlay code is causing visual leakage:

rewrite it cleanly.

Do NOT patch around the issue.

========================================================

Verify:

✓ Dark Mode only changed.

✓ Light Mode untouched.

✓ Menu feels like an independent iOS panel.

✓ Chat remains behind the Menu.

✓ Nothing leaks outside the Menu edge.

========================================================

Final report:

• Explain what caused the visual leakage.

• Explain how the Menu layer is now isolated.

• Confirm Light Mode was not modified.

