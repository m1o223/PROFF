TASK: Make ONLY TWO focused UI changes to the existing AI Plan detail screen.

IMPORTANT:
Do NOT redesign this page.
Do NOT change the current layout, phases, tasks, progress, plan logic, backend logic, or AI Plan generation.
Do NOT remove any existing functionality.
Do NOT change the content/text of the plan.
Keep everything exactly where it is unless required for the two changes below.

CHANGE 1 — ADD A DEDICATED AI BUTTON

At the top-right of the AI Plan screen, we currently have:
- Edit button
- Delete button

Add a THIRD circular button for BlueMind AI.

The top-right controls should become:
AI / Edit / Delete

Requirements:
- AI button must use the exact same circular Apple/iOS glass design system as the other native buttons.
- Use a recognizable BlueMind AI/sparkle icon.
- Do not use a large rectangular “AI” button.
- Keep all three buttons visually consistent:
  - same diameter
  - same glass/material
  - same border
  - same blur
  - same shadow
  - same press animation
  - same spacing
  - same icon weight

The AI button belongs specifically to the currently opened Plan.

When tapped, it must open the AI assistant for THIS plan.

The AI assistant must receive the current plan context so it knows:
- plan ID
- plan name
- plan type
- phases
- tasks
- completed tasks
- progress
- existing plan information

Do NOT implement new AI planning intelligence in this task.
Reuse the existing AI Plan assistant/chat functionality where possible.

AI ASSISTANT PRESENTATION:

Do NOT navigate to a completely separate full-screen page.

Open the AI assistant as an iOS-style sheet/modal covering approximately 90% of the screen height.

Leave approximately 10% of the underlying Plan visible at the top so the user clearly understands that the AI assistant is opened on top of the current Plan.

The sheet must:
- have Apple/iOS-style rounded top corners
- use the existing BlueMind dark appearance
- use the existing iOS glass system
- have a clear glass X/close button
- close correctly when X is tapped
- return the user to the SAME Plan and same state
- not create duplicate AI sheets
- not reopen itself after closing

Use the existing BlueMind chat input design inside this AI sheet.

The AI chat input should include the same existing controls where applicable:
- attachment/plus
- microphone
- send

Do not invent a new chat input design.

CHANGE 2 — CONVERT THE PAGE CONTROLS TO APPLE GLASS

Keep the existing page structure and text, but update the interactive controls to the existing BlueMind Apple/iOS glass design system.

This includes especially:
- Back button
- new AI button
- Edit button
- Delete button
- “Improve with AI”
- phase selector cards/buttons
- task interaction controls/check buttons
- any other tappable buttons on this screen

IMPORTANT:
Do not blindly put heavy blur on every large content card.

The large Phase/Task content containers can remain readable dark surfaces.

The GLASS treatment should primarily communicate INTERACTIVE CONTROLS.

Use the same existing native glass components already implemented elsewhere in BlueMind.

Do not create another fake CSS glass system if a reusable native/iOS component already exists.

VISUAL STYLE:

Keep:
- black background
- white primary text
- gray secondary text
- BlueMind accent colors
- current content hierarchy

Improve the buttons using:
- translucent dark material
- subtle border/highlight
- Apple-style blur
- circular glass controls where appropriate
- consistent corner radius
- subtle depth/shadow
- native-feeling press feedback

Completed tasks must remain clearly distinguishable.

Do NOT make text harder to read just to achieve a glass effect.

VERY IMPORTANT — DO NOT TOUCH:

Do not change:
- Plan progress calculations
- Phase calculations
- task completion logic
- existing plan data
- backend persistence
- plan creation flow
- AI Plan list page
- Projects
- Schedule
- Face ID
- authentication
- other BlueMind chat modes

This task is ONLY:
1. Add the contextual AI button + 90% AI sheet.
2. Convert this AI Plan detail screen's interactive controls to the existing Apple/iOS glass design.

TEST ON REAL IPHONE:

After implementation:
1. Open an existing AI Plan.
2. Confirm Back/Edit/Delete still work.
3. Confirm the new AI button appears beside Edit/Delete.
4. Tap AI.
5. Confirm the AI sheet opens at approximately 90% height.
6. Confirm the current Plan remains slightly visible behind it.
7. Confirm X closes the AI sheet correctly.
8. Confirm reopening AI does not create duplicate sheets.
9. Confirm the AI assistant receives the currently opened Plan context.
10. Confirm task completion still works.
11. Confirm progress still updates correctly.
12. Confirm there is no horizontal overflow or clipped buttons.
13. Confirm the actual updated production bundle is synced and visible on the connected iPhone.

When finished, report exactly:
- files changed
- existing glass components reused
- how the AI button was connected
- how current Plan context is passed to AI
- confirmation that no plan/task logic was changed
- confirmation of real-iPhone testing
