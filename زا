STOP.

Do NOT continue styling.

Do NOT modify any other screen.

This task is ONLY about making the iOS Drawer feel exactly like a native iPhone drawer.

The current implementation is closer, but it still feels like a rectangular panel instead of a native layered iOS interface.

====================================================
STEP 1 — REMOVE THE RECTANGULAR LOOK
====================================================

Right now the drawer looks like one large rectangle.

That is NOT how the ChatGPT iOS drawer behaves.

The drawer must feel like a native iOS layer.

Remove every hard rectangular edge.

Do not leave a straight vertical separator between the drawer and the chat.

====================================================
STEP 2 — CHAT MUST BECOME A CARD
====================================================

The chat is NOT the phone background.

The chat itself is another floating card.

When the drawer opens:

• the chat keeps its own rounded geometry
• large iOS corner radius
• rounded from top to bottom
• clipped correctly
• no straight edge

Think of it as two cards:

Card 1 → Drawer

Card 2 → Chat

NOT:

Rectangle | Rectangle

====================================================
STEP 3 — TRUE LAYERING
====================================================

The drawer is the front layer.

The chat is the second layer.

Between them there should be visual depth.

No hard separator.

No fake border.

No fake shadow.

The geometry itself must create the separation.

====================================================
STEP 4 — DARK MODE SURFACES
====================================================

Only DARK MODE.

Do NOT touch Light Mode.

Drawer:

native iOS dark surface.

Chat:

another slightly different dark surface.

Very subtle difference.

The user should immediately understand there are two independent layers.

====================================================
STEP 5 — NATIVE IOS MASK
====================================================

Do NOT fake this using CSS borders.

Do NOT fake this using shadows.

Use proper clipping/masking.

The chat card itself owns the rounded corners.

The drawer owns its own surface.

====================================================
STEP 6 — KEEP THE CURRENT ANIMATION
====================================================

The animation timing is already acceptable.

Do NOT redesign it.

Just make the geometry correct.

The movement must remain smooth.

====================================================
STEP 7 — DO NOT TOUCH ANYTHING ELSE
====================================================

Do NOT modify:

Search

Buttons

Header

Icons

Spacing

Typography

Input

Context Menu

Chat List

Android

Web

Only Drawer geometry and native layering.

====================================================
IF OLD CODE PREVENTS THIS
====================================================

If any previous clipping,
mask,
overlay,
transform,
or layout code prevents a native implementation,

remove it.

Do NOT stack new code over old code.

Replace the old implementation with one clean native implementation.

====================================================
FINAL REPORT
====================================================

When finished provide:

1. Which old layout caused the rectangular appearance.
2. What clipping/masking was removed.
3. How the new native card geometry works.
4. Confirmation that only iOS Dark Mode Drawer was modified.
5. Confirmation that Android and Web were NOT modified.

Before considering the task finished, compare the result visually with the ChatGPT iOS drawer and verify that the overall feeling is the same (layered native cards), while keeping BlueMind's own branding and design.
