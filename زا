MENU + BOTTOM COMPOSER IOS FIX — only these two issues

We are fixing only two visual/layout problems on the iOS side menu/chat screen.

Do not redesign unrelated parts of the menu or chat.

⸻

1. SEARCH BUTTON MUST USE REAL IOS GLASS

The circular Search button next to BlueMind AI currently looks like a custom dark circle.

I want it to use the same real/native iOS glass/material treatment we use elsewhere in BlueMind.

Requirements:

* keep the same general position
* keep the search icon
* circular native iOS glass/material
* real translucency/blur
* subtle border/highlight
* correct SF Symbol sizing
* proper native hit target
* no fake CSS-only glass if native iOS material/component is already available

Reuse the same native glass button component/helper already used for other BlueMind iOS controls if possible.

Do not invent another style just for Search.

⸻

2. REMOVE THE BLACK OVERLAY ABOVE THE BOTTOM INPUT / PLUS BUTTON

There is currently an unwanted black/dark layer covering part of the bottom composer area.

In the screenshot, the area around:

* the + button
* the lower chat/composer area
* the New Chat region

looks like a black cover/overlay is sitting above the real UI.

This creates a clipped/covered appearance.

Find the exact cause.

Inspect:

* bottom composer wrapper
* fixed/sticky footer container
* overlay layer
* gradient
* background view
* safe-area background
* z-index
* mask/clipping
* side-menu overlay interaction
* any Web-style bottom cover still affecting iOS

Do NOT fix this by just moving the + button upward.

Remove/correct the actual covering layer.

⸻

EXPECTED RESULT

The bottom input/composer should look clean and unobstructed.

The + button must be fully visible.

The input should not have any extra black slab/cover above it.

The side-menu and composer should coexist without one masking the other.

If the side menu intentionally dims the main screen behind it, that dimming must not create an extra opaque block specifically over the composer.

⸻

IOS ONLY

These changes are for the iOS presentation only.

Do not modify the Web menu/search styling.

Do not change backend.

Do not change chat behavior.

Do not change menu items.

⸻

TEST ON REAL IPHONE

1. Open side menu.
2. Verify Search button is real iOS glass.
3. Close/open menu multiple times.
4. Verify composer/input area remains clean.
5. Verify + is fully visible.
6. Verify no black overlay remains.
7. Verify keyboard/composer still work normally.
8. Verify Web is unchanged.

After fixing, report:

* which component caused the black overlay
* which native glass component is used for Search
* confirmation that only iOS was changed
