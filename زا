Update ONLY the + attachment menu inside Writing Mode.

The Writing Mode composer is now correct.
Do not redesign the composer again.

I only want to change what happens when the user taps the + button.

IMPORTANT:
Writing Mode is a writing-focused chat, so it must NOT show the full general chat attachment/action menu.

When the + button is tapped, show ONLY these 3 options:

1. Camera
2. Photos
3. Files

Remove/hide from Writing Mode:
- Create Images
- Search
- Write / Edit
- any other general chat tools

The menu must be SMALL and anchored directly above the + button.

Do NOT open a large centered modal.
Do NOT open a huge bottom sheet.
Do NOT cover the main Writing Mode content.

Desired behavior:

           [ Camera ]
           [ Photos  ]
           [ Files   ]
               ↑
             [+]

The popup should appear just above the + button and visually feel attached to it.

Use the same BlueMind iOS glass/native-backed menu style already used elsewhere in the app.

Requirements:
- compact floating popup
- iOS glass material
- thin border/stroke
- rounded corners
- native-looking spacing
- icon + label for every row
- correct press states
- correct safe-area placement
- dismiss when tapping outside
- dismiss after selecting an option
- no overlap with keyboard
- if keyboard is open, position the popup above the composer safely

Use these actions:

Camera:
Open camera capture flow.

Photos:
Open iOS photo library picker.

Files:
Open iOS document/file picker.

Do not change the existing attachment implementations if they already work.
Reuse the current handlers/components.

This customization must apply ONLY to Writing Mode.

Normal Chat and other chat modes should keep their own existing + menu.

Do not touch:
- composer layout
- Writing Type
- Writing Tools
- Quick Actions
- AI logic
- backend
- other screens

Final expected Writing Mode composer area:

[quick actions]

[ + | Ask BlueMind to write… | mic | send ]

When + is tapped:

small glass popup directly above +:
Camera
Photos
Files

After implementation, report:
1. which component/menu was changed
2. whether existing Camera/Photos/Files handlers were reused
3. how popup anchoring above + is implemented
4. physical iPhone test result
