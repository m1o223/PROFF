Update ONLY the Menu behavior in the existing ChatPop iOS/mobile UI.

IMPORTANT:
Keep the existing iOS project exactly as it is.
Do not recreate iOS, change Capacitor configuration, signing, bundle ID, or any native settings.

MENU BEHAVIOR

When the user taps the Menu button on the Chat screen, open a Chat History panel/drawer OVER the current Chat screen.

The reference screenshots are only for understanding the interaction.
Do NOT copy ChatNow's exact design.

The ChatPop menu should:

- Slide in from the RIGHT side of the screen.
- Cover most of the screen.
- Keep the current Chat screen behind it.
- Add a subtle dark overlay/dimming behind the panel.
- Use a smooth iOS-style slide animation.
- Respect the iPhone top and bottom safe areas.

At the top of the panel include:

- A Close / Back button
- Title: "Chats"
- Settings button/icon

Then include:

- Search field for searching previous chats
- New Chat button

Below that:

RECENT CHATS

Show the user's previous conversations as a simple vertical list.

For now, use mock/local chat history such as:

- New chat
- Plan my trip
- Explain this code
- Image ideas

Tapping a previous chat can simply select/highlight it for now.
No backend or database is needed yet.

IMPORTANT:
Keep this menu very minimal.

DO NOT add:

- Creative Studio
- Playground
- Plus / Pro banners
- Subscription
- Profile
- Image generation
- Video generation
- Promotional cards
- Any other sections

The menu currently needs ONLY:

Close/Back
Settings
Search Chats
New Chat
Previous Chats

VISUAL STYLE

Use ChatPop's existing identity:

- Black background
- Very dark gray surfaces
- Light blue accent
- White text
- Minimal modern iOS appearance

Do not copy ChatNow's colors, cards, graphics, branding, or exact layout.

The panel should feel native to ChatPop.

IMPORTANT IOS RULE:

Work on the existing frontend only.

After finishing:
- Build the frontend
- Run npx cap sync ios
- Update the EXISTING iOS project

Do NOT:
- delete /ios
- run npx cap add ios
- create another Xcode project
- change signing
- change Development Team
- change Bundle Identifier
- change existing native iOS configuration

When finished, STOP and report what you changed.

Do not implement any additional features until I review the menu on my real iPhone.
