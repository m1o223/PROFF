TASK: Refactor and redesign the AI Plan screen on iOS so it feels like a proper BlueMind chat experience, while preserving the AI Plan purpose and logic.

IMPORTANT:
Do not redesign unrelated screens.
Do not change backend logic yet.
Do not finalize AI categories yet.
Do not remove existing AI Plan functionality.
Do not replace working iOS/native-backed components with fake CSS glass.

The goal of this task is to fix the AI Plan screen structure, layout, composer, and iOS visual consistency first.

==================================================
1. CURRENT PROBLEM
==================================================

The current AI Plan screen looks too much like a landing page.

It currently has:
- back button
- large AI logo
- large “Create AI Plan” title
- description text
- large visible chips such as Study / Website / Programming
- very large empty area
- oversized bottom input box

This does not feel like a chat screen.

AI Plan should feel like:
“a specialized AI chat for planning”

It should still be visually distinct from Normal Chat, but it must follow the same BlueMind chat interaction language.

==================================================
2. MAIN DESIGN DIRECTION
==================================================

Keep the AI Plan identity, but restructure the page as a real iOS chat-style interface.

The page should contain:

A. Top header
B. AI Plan identity / lightweight empty state
C. AI options entry point
D. Shared BlueMind chat composer at the bottom

Do NOT turn the screen into a dashboard.

Do NOT add large cards everywhere.

==================================================
3. HEADER
==================================================

Use a proper BlueMind iOS-style chat header.

The back button should remain.

Make sure:
- same circular/glass style as the rest of BlueMind
- same size as other iOS header controls
- correct spacing
- correct safe-area positioning

Add or keep a compact AI Plan title/identity in the header area if appropriate.

Do NOT use a huge header title that dominates the page.

Do not make the header look different from the rest of the app.

==================================================
4. AI LOGO / IDENTITY
==================================================

Keep the current AI logo/identity.

The AI logo is important and should remain visible.

However:
- refine its placement
- reduce unnecessary empty space
- make it feel integrated into the chat screen
- use the existing iOS/native-backed visual system where appropriate

Do NOT replace the logo.

Do NOT remove the BlueMind identity.

The logo should not be huge.

==================================================
5. EMPTY STATE
==================================================

In the middle of the screen, use a very lightweight empty-state message.

Example concept:

How can I help you build your plan?

Optional small supporting text:
Choose a mode or describe what you want to organize.

Do NOT use large explanatory paragraphs.

Do NOT make this section look like onboarding.

The empty state should disappear or become irrelevant once the chat starts.

==================================================
6. AI PLAN OPTIONS
==================================================

IMPORTANT:
Do NOT permanently show Study / Website / Programming as large visible chips in the middle of the page.

Those options should NOT remain permanently expanded.

Instead, add a compact AI Options / Plan Type / AI Mode selector.

This selector should be easy to access.

Possible placement:
- above the composer
- integrated near the composer
- or in the chat header area

Preferred behavior:
Tap one compact control
-> open a native iOS-style menu or sheet
-> later we will define the final AI Plan categories

For now, prepare the UI architecture correctly.

Do NOT finalize the final category structure yet.

==================================================
7. TEMPORARY OPTIONS
==================================================

The currently existing options:
- Study
- Website
- Programming

may be reused temporarily inside the menu/sheet for testing.

But do NOT hardcode the final design around these three options.

The architecture must allow more AI Plan modes later.

==================================================
8. AI OPTIONS CONTROL
==================================================

The AI Options control should:

- look like a compact BlueMind iOS glass control
- clearly indicate that the user can choose a planning mode
- have a chevron or appropriate indicator
- show the currently selected mode if one is selected

Example concept:

[ AI Plan Type ▾ ]

or:

[ General Plan ▾ ]

Do not use huge buttons.

Do not use a custom fake CSS dropdown if the project already has a native/native-backed selection component.

==================================================
9. IOS OPTIONS SHEET / MENU
==================================================

When the user taps the AI Options control:

Open a proper iOS-style sheet/menu.

Use the existing BlueMind iOS/native-backed popup/sheet architecture.

The sheet should:
- appear cleanly
- dim/background correctly
- support tap outside / close behavior
- show a selected checkmark where appropriate
- have correct spacing
- feel native to the rest of the app

Do NOT build a giant custom WebView panel.

==================================================
10. BOTTOM COMPOSER
==================================================

This is one of the most important changes.

The current oversized AI Plan input should be removed/replaced.

Reuse the SAME shared BlueMind composer architecture used by Normal Chat.

The structure should be:

[ + ] [ text input ] [ microphone ] [ send ]

Same:
- glass
- height
- radius
- spacing
- icon size
- padding
- keyboard behavior
- safe-area handling
- multiline behavior

AI Plan must NOT have its own separate giant composer.

==================================================
11. COMPOSER PLACEHOLDER
==================================================

Do not use generic:

Ask anything...

Use an AI Plan-specific placeholder.

Preferred examples:

“What do you want to plan?”

or:

“Tell BlueMind what you want to organize…”

or:

“Describe the plan you want to build…”

Use one short clear sentence.

Do not use long instructional text in the composer.

==================================================
12. COMPOSER BEHAVIOR
==================================================

The AI Plan composer should behave exactly like Normal Chat where applicable.

Requirements:

- native iOS keyboard
- multiline
- grows naturally
- maximum height
- internal scroll after maximum height
- Arabic support
- Swedish support
- English support
- emoji support
- copy/paste
- correct cursor behavior

When keyboard opens:
- composer stays above keyboard
- no overlap
- no duplicate composer
- no layout jump
- no huge gap
- content remains reachable

==================================================
13. ATTACHMENT BUTTON
==================================================

Keep the + button.

Use the same shared + button style as Normal Chat.

Do not change its visual design.

AI Plan can later have AI Plan-specific attachment/actions if needed.

For now:
do not invent unrelated actions.

==================================================
14. MICROPHONE
==================================================

Use the same microphone button as Normal Chat.

Same:
- design
- icon
- size
- glass
- placement

If voice input already works:
preserve it.

Do not create a new voice implementation.

==================================================
15. SEND BUTTON
==================================================

Use the same send button style as Normal Chat.

Empty input:
same disabled state.

Text entered:
same active state.

Do not make AI Plan use a different arrow or different button style.

==================================================
16. CHAT-STYLE LAYOUT
==================================================

The overall AI Plan screen should feel like this:

TOP:
Back button + compact AI Plan identity

MIDDLE:
AI logo
small empty-state text

ABOVE COMPOSER:
compact AI Options selector

BOTTOM:
shared BlueMind composer

Once messages exist:
the middle empty state should no longer dominate the page.

The conversation should naturally occupy the available space.

==================================================
17. CONVERSATION AREA
==================================================

Prepare the page structure so AI Plan can behave like a conversational planning assistant.

Do not change backend behavior yet.

But make sure the layout supports:
- user messages
- AI responses
- scrolling
- future streaming
- future plan-building UI inside the conversation

Do not hardcode the screen as a static landing page.

==================================================
18. AI PLAN IDENTITY
==================================================

AI Plan should still feel different from Normal Chat.

It may keep:
- its own logo
- its own placeholder
- its own options selector
- its own planning-specific future tools

But it should use the SAME BlueMind chat interaction system.

Think:

Same BlueMind chat foundation
+
AI Plan-specific identity

==================================================
19. DO NOT CHANGE BACKEND YET
==================================================

Do NOT:
- change AI prompts
- change AI provider
- change API routes
- change plan-generation backend
- change stored plan structure
- change chat persistence logic
- change Projects
- change authentication

This task is primarily frontend/UI architecture.

==================================================
20. IOS GLASS
==================================================

Use the existing iOS/native-backed BlueMind components.

If the project already has:
- native glass bridge
- Swift-backed controls
- Capacitor native UI
- existing shared glass buttons
- native sheets

reuse them.

Do NOT recreate iOS glass with only:
backdrop-filter
blur()
box-shadow

unless that exact component is already the app's approved implementation.

==================================================
21. SPACING
==================================================

Fix spacing carefully.

The current screen has too much empty vertical space.

The new layout should feel balanced.

Do not:
- push the composer too far down
- place suggestions too high
- leave massive empty gaps

Use consistent spacing with the rest of BlueMind.

==================================================
22. SAFE AREA
==================================================

Respect:
- Dynamic Island
- top safe area
- bottom home indicator
- keyboard
- different iPhone sizes

Do not hardcode only for one device.

==================================================
23. SCREENSHOTS / RESTART ISSUE
==================================================

Do not introduce any logic that causes:
- app restart
- route reset
- Face ID re-trigger
- navigation reset
when taking screenshots or when the app goes briefly inactive.

Do not modify lifecycle logic in this task unless absolutely required.

==================================================
24. DO NOT TOUCH
==================================================

Do NOT modify:

- Normal Chat content
- Writing Mode content
- Projects
- Search
- Private Chat
- Hidden Chat
- Face ID
- Settings
- Reminders
- Schedule
- backend
- Android

Only modify shared components if required for this AI Plan UI refactor.

==================================================
25. IMPLEMENTATION ORDER
==================================================

Please work in this order:

1. Inspect current AI Plan screen files.
2. Identify current Normal Chat composer.
3. Reuse the shared composer in AI Plan.
4. Rebuild AI Plan layout around chat architecture.
5. Add compact AI Options selector.
6. Move Study / Website / Programming into a menu/sheet temporarily.
7. Fix spacing.
8. Test keyboard behavior.
9. Test on real iPhone.
10. Verify existing AI Plan actions still work.

==================================================
26. REAL IPHONE TEST
==================================================

Test on the connected physical iPhone.

Verify:

- AI Plan opens correctly
- back button works
- AI logo displays
- AI Options control opens
- options sheet closes correctly
- composer opens keyboard
- multiline works
- Arabic typing works
- Swedish å ä ö work
- emoji works
- microphone remains available
- send button state works
- keyboard does not overlap composer
- home indicator spacing is correct
- screen does not restart
- navigation does not reset

==================================================
27. FINAL REPORT
==================================================

When finished, report:

1. AI Plan files changed
2. shared composer file used
3. where the AI Options control is located
4. how the options menu/sheet is implemented
5. whether it is native/native-backed or WebView/CSS
6. how keyboard handling works
7. how safe area is handled
8. what temporary AI options remain
9. what was intentionally NOT changed
10. physical iPhone test result

FINAL DESIGN RULE:

AI PLAN SHOULD FEEL LIKE A SPECIALIZED BLUE MIND CHAT.

NOT A LANDING PAGE.
NOT A DASHBOARD.

KEEP THE AI PLAN IDENTITY.
KEEP THE AI LOGO.
KEEP ITS UNIQUE PURPOSE.

BUT USE THE SAME BLUE MIND IOS CHAT FOUNDATION.
