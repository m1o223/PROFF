Refine ONLY the "New Reminder" screen and the reminder notification accent colors.

Do NOT redesign the entire Reminders page.
Do NOT change reminder logic, data, validation, save behavior, or navigation.
Only make the following visual fixes.

1. CLOSE X BUTTON

The X button inside the New Reminder screen is still using the older custom version.

Replace it with the SAME real native iOS glass close control already used elsewhere in the app.

Requirements:
- Same native iOS glass/material implementation
- Same size
- Same circular geometry
- Same icon weight
- Same visual behavior
- Do NOT recreate it with custom CSS
- Reuse the existing native iOS implementation already available in BlueMind

The X must visually match the current Settings/iOS close control.


2. NEW REMINDER INPUTS

The current fields feel too thin, inconsistent and visually unfinished.

Rebuild/refine the field layout so every field has proper spacing and clear boundaries.

Fields:

Title
Description
Date
Time
Reminder Before
Repeat
Every

IMPORTANT:
Each control must have its own clear container and its own space.

Nothing should visually merge with the next field.

Use a neutral dark iOS-style input surface:
- dark graphite / charcoal
- NOT pure black
- NOT blue
- subtle border
- soft rounded corners
- consistent radius
- consistent height
- consistent internal padding

The Title input should be a standard single-line field.

The Description field must be taller than it is now.
It should clearly look like a multiline text area.
Give it enough vertical height for approximately 3–4 lines of text.

Do NOT make it compressed.


3. DATE + TIME

Date and Time currently visually blend together too much.

They must be two clearly separated controls.

Requirements:
- visible space between them
- independent rounded containers
- each field has its own border
- no shared border
- no visual overlap
- equal visual height
- aligned correctly on the same row

Date should occupy more width than Time if necessary.

The screen must remain fixed to the iPhone viewport.
No horizontal overflow.


4. REMINDER BEFORE

Give "Reminder Before" its own complete bordered input container.

It must not look like plain text floating on the page.

Same neutral iOS field treatment as the other controls.


5. REPEAT + EVERY

Repeat and Every must also be separate controls.

Repeat should have enough width for values such as:

Does not repeat
Daily
Weekly
Monthly
Yearly

Every should remain smaller.

Do NOT allow either field to stretch or push the page horizontally.


6. BUTTONS

Cancel:
- neutral dark glass/material
- no blue tint

Save:
The current Save button is too light blue.

Change it to the SAME BlueMind dark blue already used as the primary BlueMind accent elsewhere in the app.

Do NOT invent a new blue.

Reuse the existing BlueMind primary blue token/value from the project.

The Save button should only become visually active when the form is valid.


7. REMINDER NOTIFICATIONS ACCENT COLOR

On the main Reminders screen, the bell icon background inside "Reminder Notifications" is currently too light blue.

Replace that light blue with the SAME BlueMind dark blue primary accent used by the app.

Again:
Do NOT hardcode another blue.
Reuse the existing BlueMind primary blue token/value.


8. CONSISTENCY

Use one shared visual system for all New Reminder fields:

- same corner radius
- same border style
- same dark neutral material
- same text alignment
- same internal padding
- same label spacing

The screen should feel like one coherent native iOS form rather than separate custom inputs.


9. IMPORTANT — DO NOT BREAK LAYOUT

The New Reminder screen must never move horizontally.

Keyboard opening must NOT:
- shift the page left/right
- resize fields incorrectly
- crop controls
- create horizontal scrolling

All controls must remain inside the iPhone viewport.


FINAL RESULT

The New Reminder page should look cleaner, more structured and more native iOS.

Main visual corrections:

- replace old X with real native iOS glass X
- make Description taller
- separate Date and Time clearly
- give every field its own complete container/border
- use neutral dark graphite inputs
- use BlueMind dark blue for Save
- use BlueMind dark blue for the reminder notification accent
- keep everything aligned and locked to the viewport

Do not make unrelated changes.
