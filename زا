Make ONLY the following final layout/style corrections to the ChatPop Menu screen.

Do NOT redesign the Creative Studio card, chats list, Settings screen, backend, or any unrelated feature.

==================================================
1. PURE BLACK BACKGROUND
==================================================

The entire Menu screen background must be pure black / near pure black.

Remove the current dark-gray page background.

The main base background should visually match the main ChatPop chat screen:

#000000 / true black

Cards can remain dark gray where appropriate, but the PAGE itself must be black.

==================================================
2. TOP HEADER — CHANGE THE POSITIONS
==================================================

The current top header has:

Back button on the LEFT
ChatPop in the CENTER
Settings on the RIGHT

Change it.

New layout:

LEFT:
ChatPop

RIGHT:
Back / Close arrow

REMOVE the Settings icon from this Menu header completely.

So visually:

ChatPop                                      [ Back ]

The ChatPop text should be aligned naturally on the left with good iPhone safe-area spacing.

Move the EXISTING back control to the top-right position where the Settings button currently is.

Do not add another Settings button somewhere else in this screen.

==================================================
3. TOP HEADER MUST NOT HAVE AN OPAQUE COVER
==================================================

Currently the top header feels like there is an opaque dark cover/background sitting above the scrolling content.

I do NOT want that.

The header must behave more like modern iOS navigation material:

- keep it visually lightweight
- no solid rectangular black/gray cover
- content should be allowed to scroll underneath it
- when content passes underneath, it should remain faintly visible through a subtle native iOS blur/material effect

Use the Settings screen behavior/style as the reference:
when content moves underneath the header, it looks softly blurred instead of being abruptly hidden by a solid block.

The base app background remains black, but the sticky header itself should be transparent/translucent.

IMPORTANT:
Use the REAL existing iOS material/glass implementation where available.
Do not fake it with a heavy CSS rectangle.

==================================================
4. HEADER SCROLL BEHAVIOR
==================================================

The top header should remain fixed/sticky while the Menu content scrolls vertically underneath it.

The scrolling content should continue behind the header.

Use:
- subtle blur
- subtle translucency
- clean native iOS effect

Avoid:
- hard opaque background
- visible rectangular header container
- abrupt clipping

==================================================
5. BOTTOM SEARCH AREA
==================================================

Apply the SAME principle to the Search area at the bottom.

The Search control should remain a floating/sticky control near the bottom safe area.

BUT:

Do NOT put it inside a large opaque footer/background.

Currently it feels like there is a dark cover underneath/around the Search input.

Remove that opaque footer.

The area surrounding the Search control should be transparent/translucent.

When chat rows/content scroll underneath the bottom Search area:
- they should remain faintly visible
- apply subtle iOS blur/material
- no solid black/gray block hiding everything

The Search button itself can remain a dark glass/material pill.

Only remove the big opaque background behind it.

==================================================
6. SEARCH POSITION
==================================================

Keep Search at the bottom.

It should:
- float above the bottom safe area
- stay centered
- remain reachable
- not cover too much of the chat list
- use the existing Search icon/text

Do not move Search back to the top.

==================================================
7. SCROLLING
==================================================

The whole middle Menu content should scroll naturally:

- subscription card
- Creative Studio card
- Chats
- old conversations

The content should be able to scroll underneath BOTH:

TOP:
transparent/blurred header

BOTTOM:
transparent/blurred Search overlay

There should be enough content inset/padding so the first and last items can still be fully accessed.

==================================================
8. FINAL MENU STRUCTURE
==================================================

The final screen should feel like:

PURE BLACK BACKGROUND

TOP FLOATING / TRANSLUCENT AREA:
ChatPop                              Back Arrow

↓

Subscription
Creative Studio
Chats
Chat history
(all vertically scrollable)

↓

BOTTOM FLOATING / TRANSLUCENT AREA:
[ Search ]

No Settings button in the top Menu header.

==================================================
9. VISUAL QUALITY
==================================================

I want this to feel very close to native modern iOS:

- pure black screen
- translucent floating header
- translucent floating bottom search area
- native blur/material when content passes behind
- no visible opaque header/footer boxes
- smooth scrolling
- no clipping glitches
- no white flashes

==================================================
10. DO NOT CHANGE
==================================================

Do not change:

- Creative Studio card design
- Creative Studio media animation
- chat names
- chat selection
- New Chat behavior
- Settings screen
- authentication
- backend
- voice features
- iOS signing
- Bundle Identifier
- Development Team

Only make the Menu/header/Search/background corrections described above.

Test on the real iPhone and verify:
1. Menu background is true black.
2. ChatPop is top-left.
3. Back arrow is top-right.
4. Settings icon is gone from this header.
5. Content visibly scrolls underneath the translucent header.
6. Search has no opaque footer behind it.
7. Content can scroll underneath the Search area with blur.
8. Everything remains smooth and readable.
