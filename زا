There is a new iOS lifecycle bug.

Taking a screenshot while BlueMind is open causes the entire app to restart/reinitialize.

This is NOT expected behavior.

Please reproduce and trace exactly what happens when iOS takes a screenshot.

Check:

* appStateChange listeners
* scenePhase / UIApplication lifecycle handling
* Capacitor App listeners
* document visibilitychange
* WebView reload logic
* Face ID lock/unlock lifecycle
* route initialization
* session restoration

Important:
A screenshot must NOT:

* reload the WebView
* restart React
* clear navigation state
* trigger Face ID again
* reset the current route
* recreate the app session

Expected behavior:
If I am inside Settings, AI Plans, a Plan, or any other screen and take a screenshot, I must remain on the exact same screen with the exact same state.

First diagnose the exact event that fires during screenshot capture and identify which listener is causing the restart.
Then fix ONLY that lifecycle bug.

Do not redesign UI and do not change unrelated app logic.

After fixing it, test on the real iPhone:

1. Open BlueMind.
2. Navigate to Settings.
3. Take a screenshot.
4. Confirm the app stays on Settings.
5. Repeat inside AI Plans.
6. Confirm there is no reload, Face ID prompt, or route reset.

Report the exact root cause and the file/function that was causing the restart.
