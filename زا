Supabase connectivity is now working on my home Wi-Fi.

Authentication and SELECT 1 succeeded on the Session Pooler port 5432.

The only remaining issue is verified TLS certificate trust:
SELF_SIGNED_CERT_IN_CHAIN

I have now downloaded the official Supabase database CA certificate from:
Supabase → Database Settings → SSL Configuration → Download certificate

Please:

1. Locate the downloaded Supabase CA certificate on this Mac.
2. Copy it into a safe backend-only location inside chatpop-backend, for example:
   chatpop-backend/certs/supabase-ca.crt

3. Configure the existing backend environment using the existing:
   DATABASE_CA_FILE
   setting (or the equivalent already implemented).

4. Keep TLS certificate verification ENABLED.

Do NOT use:
rejectUnauthorized: false

Do NOT disable TLS verification.

5. Verify the database again using:
- DNS
- TCP
- TLS certificate verification
- authentication
- SELECT 1

6. Once verified TLS succeeds, run the EXISTING database migrations against Supabase.

7. Verify all existing tables are created.

8. Run the existing backend tests for:
- registration
- login
- /me
- sessions
- settings
- chats
- messages
- ownership/security
- persistence

9. Keep using Session Pooler port 5432 unless there is a real technical reason to change it.

10. Do not touch frontend or iOS yet.

11. Do not expose:
- DATABASE_URL
- database password
- certificate secrets
- auth tokens

When finished, report:
- whether verified TLS succeeded
- whether SELECT 1 succeeded with certificate verification ON
- whether migrations succeeded
- tables created
- tests passed/failed
- any remaining blocker
