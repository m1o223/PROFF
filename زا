We are now starting the first real UI implementation for ChatPop.

IMPORTANT:
The current iOS project has already been successfully created with Capacitor and tested on a real iPhone. Do NOT rebuild, replace, recreate, or restructure the existing iOS project.

For this task, I want you to work primarily on the frontend and create ONLY the main Chat screen.

REFERENCE:
I have reference screenshots from another chat application. Use them only to understand the general UX structure and interaction ideas.

Do NOT make an exact copy.
Do NOT copy its branding, assets, colors, graphics, or exact visual design.
ChatPop must have its own identity.

==================================================
1. TARGET
==================================================

Design the interface specifically for iPhone/mobile.

This should NOT be designed as a desktop website that is later resized for mobile.

The UI must feel like a real modern iOS application when running through our existing Capacitor iOS project.

For now, create ONLY ONE SCREEN:

CHAT SCREEN

Do NOT create:
- Login
- Sign Up
- Get Started
- Onboarding
- Home page
- Profile
- Settings
- Subscription page
- Image Generator page
- Video Generator page

We will build those later.

==================================================
2. CHAT SCREEN — TOP BAR
==================================================

Create a top navigation area containing:

LEFT:
- Menu button

CENTER:
- AI model selector

RIGHT:
- New Chat button/icon

The layout must respect the iPhone top Safe Area and devices with Dynamic Island/notch.

==================================================
3. AI MODEL SELECTOR
==================================================

The model selector should be interactive.

When tapped, open a clean iOS-style model selection panel/dropdown.

For now, use MOCK DATA ONLY.

Include:

- Auto
- ChatGPT
- Claude
- Gemini
- DeepSeek

Do NOT connect these models to any API yet.

The currently selected model should be clearly visible in the top bar.

==================================================
4. CHAT AREA
==================================================

Create the main conversation area.

For the empty/new-chat state, keep the screen clean and minimal.

Do not fill the screen with unnecessary cards, banners, ads, or promotional content.

The conversation area must support vertical scrolling when messages are eventually added.

==================================================
5. MESSAGE INPUT
==================================================

Create a polished message composer fixed near the bottom of the screen.

Placeholder:

"Message ChatPop..."

Include:

- + button
- Text input
- Microphone button
- Send button

The Send button should become active when the user types text.

The composer must correctly handle:
- iPhone bottom Safe Area
- iOS keyboard
- Different iPhone screen sizes
- Dynamic Island/notch
- Keyboard opening and closing

The keyboard must NOT cover the message input.

==================================================
6. + BUTTON
==================================================

When the user taps the + button, open an iOS-style attachment menu.

For now include:

- Camera
- Photos
- Audio
- File

These can initially be UI interactions/mock actions.

Do not build complex upload/backend functionality yet unless something minimal is required for the interface to work.

==================================================
7. CHATPOP VISUAL IDENTITY
==================================================

ChatPop should have its own design language.

Main colors:

Background:
Black

Primary accent:
Light Blue

Primary text:
White

Secondary surfaces/cards:
Very dark gray / near-black

Keep the interface:
- Clean
- Minimal
- Modern
- Premium
- Native-feeling on iPhone

Avoid unnecessary gradients, pink, purple, or unrelated colors.

Do not copy ChatNow's visual identity.

==================================================
8. BACKEND / AI
==================================================

DO NOT connect the backend yet.

DO NOT add:
- OpenAI API
- Claude API
- Gemini API
- DeepSeek API
- AI routing
- Authentication
- Database
- Subscription logic

The model selector and chat interactions can use mock/local state for now.

We are focusing ONLY on the frontend UI.

==================================================
9. CRITICAL — EXISTING iOS PROJECT
==================================================

The current iOS project already exists inside:

ios/

It has already been successfully installed and launched on a REAL iPhone.

YOU MUST PRESERVE THIS EXISTING iOS PROJECT.

Do NOT:

- Delete the ios directory
- Recreate the ios directory
- Run "npx cap add ios" again
- Create another Xcode project
- Replace the existing Xcode project
- Change the Bundle Identifier
- Change Signing settings
- Change the Development Team
- Change certificates/provisioning
- Change the Deployment Target
- Reset Capacitor configuration
- Modify native iOS settings unnecessarily

Do NOT touch working iOS configuration unless it is absolutely required.

If you encounter something that requires changing the existing native iOS configuration, STOP and tell me what needs to change BEFORE making that change.

Do not make that decision automatically.

==================================================
10. HOW TO UPDATE THE EXISTING iOS APP
==================================================

Work on the frontend source files.

After the UI implementation is complete:

1. Build the existing frontend.
2. Make sure the build succeeds.
3. Run the appropriate Capacitor sync command for the EXISTING iOS project, such as:

npx cap sync ios

This should update the web assets inside the existing iOS project.

DO NOT create a new iOS platform.

The goal is:

Existing ChatPop Frontend
→ UI changes
→ Build
→ Capacitor Sync
→ Existing iOS project
→ Existing Xcode project
→ Existing real-iPhone installation

==================================================
11. DO NOT OVERBUILD
==================================================

Do not add features I did not request.

Do not redesign the whole application.

Do not create additional screens.

Do not implement backend architecture yet.

Do not introduce unnecessary dependencies.

Do not restructure working project configuration without a clear technical reason.

Keep the implementation clean and easy to extend later.

==================================================
12. WHEN FINISHED
==================================================

After implementing the Chat screen:

- Run the frontend build.
- Sync it to the EXISTING iOS project.
- Check for build/type errors.
- Do not modify signing or other existing native settings.

Then STOP.

Give me a short report containing:

1. What you implemented.
2. Which files you changed.
3. Whether the frontend build succeeded.
4. Whether Capacitor sync succeeded.
5. Whether the existing iOS project was preserved.
6. Any issues I should know about before I open Xcode and run ChatPop on my real iPhone.

Do not continue to another feature until I review this version.

The goal of this task is ONLY:

ChatPop Chat UI
+
Menu
+
New Chat
+
AI Model Selector
+
Message Input
+
Attachment Menu
+
Microphone
+
Send Button

on the EXISTING iOS version, without disturbing the working iOS project.
