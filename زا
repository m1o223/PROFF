TASK: STANDARDIZE THE BOTTOM CHAT COMPOSER ACROSS ALL 5 BLUE MIND CHAT-BASED EXPERIENCES ON iOS.

IMPORTANT:
Read this entire request before changing code.

This task is NOT a redesign of the five pages.

DO NOT redesign their page content.
DO NOT make the five pages look identical.
DO NOT remove their unique features.
DO NOT change their AI behavior just to make them visually similar.

The ONLY component I want standardized is the bottom message composer / text input area.

==================================================
CORE IDEA
==================================================

BlueMind currently has approximately five different chat-based experiences.

Examples include:
- Normal Chat
- Writing Mode / Writing assistant
- AI Plan / planning assistant
- the other specialized AI assistant/chat experiences already present in the app
- any fifth existing chat-based mode currently using a message input

FIRST:
Inspect the codebase and identify the EXACT five chat-based screens/components that currently contain a conversational text composer.

Do NOT guess their file names.

Report which five you found before/while implementing.

Each experience can have completely different:
- header
- page content
- cards
- tools
- AI behavior
- system prompt
- suggestions
- assistant personality
- workflow

KEEP THOSE DIFFERENCES.

But all five must use ONE consistent BlueMind iOS composer system.

==================================================
1. NORMAL CHAT IS THE DESIGN REFERENCE
==================================================

Find the CURRENT working composer used by Normal Chat.

That is the reference implementation.

I want the other four chat-based experiences to use the same underlying composer architecture.

Do NOT look at another screen and manually recreate something that looks similar.

Do NOT create:

NormalChatComposer
WritingComposer
PlanComposer
AssistantComposer
ProfileComposer

with duplicated CSS.

Instead, create/reuse ONE shared component/architecture.

Conceptually:

<BlueMindComposer />

Then each screen provides configuration/context.

Example concept only:

<BlueMindComposer
  mode="writing"
  placeholder="What would you like to write?"
  onSend={...}
/>

Do NOT blindly use this API.
Adapt it to the existing architecture.

==================================================
2. VISUAL DESIGN MUST BE IDENTICAL
==================================================

Across all five experiences, the composer must share exactly the same:

- overall height
- minimum height
- maximum expansion behavior
- corner radius
- glass material
- blur/material behavior
- border treatment
- shadow
- horizontal margins
- bottom spacing
- internal padding
- icon sizing
- icon alignment
- text baseline
- placeholder typography
- button sizes
- button spacing
- send button shape
- microphone button shape
- attachment button shape

If I place screenshots of the five composers next to each other, they should clearly belong to the SAME BlueMind component.

The page ABOVE the composer may look completely different.

==================================================
3. APPLE / IOS GLASS
==================================================

This is the iOS version.

Inspect the existing BlueMind native/native-backed iOS glass implementation.

Reuse it wherever the Normal Chat composer already uses it.

Do NOT replace working native/native-backed glass with a generic CSS:

backdrop-filter
blur()
box-shadow

and call it “iOS glass”.

If the project already has:
- Swift bridge
- UIKit component
- native glass bridge
- Capacitor native plugin
- native-backed buttons/material

reuse the existing architecture.

The composer should feel like part of the same iOS design language as the rest of BlueMind.

==================================================
4. BASIC COMPOSER STRUCTURE
==================================================

The shared composer should preserve the existing Normal Chat structure.

Conceptually:

[ + ]   [ text input / placeholder ]   [ microphone ]   [ send ]

The exact existing Normal Chat implementation is the source of truth.

Do not arbitrarily redesign it.

==================================================
5. ATTACHMENT + BUTTON
==================================================

The + button must be the same across all five experiences.

Same:
- size
- glass
- position
- hit target
- press animation
- icon
- spacing

However:

The ACTIONS opened by + may differ depending on mode.

Example:

Normal Chat may support:
- Photos
- Camera
- Files
etc.

Writing Mode may support relevant writing attachments.

AI Plan may support files/content relevant to planning.

Therefore:

SHARED VISUAL COMPONENT
+
MODE-SPECIFIC ACTIONS

Do not force every mode to have actions it does not support.

==================================================
6. TEXT FIELD
==================================================

The actual text input must behave identically across all five screens.

Requirements:

- tap to focus
- native iOS keyboard
- multiline text
- natural height expansion
- no sudden layout jump
- correct cursor
- correct selection
- copy/paste
- emoji
- Arabic
- Swedish
- English
- mixed languages
- RTL support where appropriate

Long text must NOT overflow outside the composer.

The composer should expand until a sensible maximum height.

After reaching maximum height:
the text itself becomes internally scrollable.

==================================================
7. PLACEHOLDER MAY CHANGE
==================================================

The component is shared.

The placeholder is NOT required to be identical.

Each experience can provide its own context.

Examples:

Normal Chat:
“Ask anything…”

Writing:
“What would you like to write?”

AI Plan:
“Tell BlueMind what you want to plan…”

Other assistant:
mode-appropriate placeholder.

IMPORTANT:
Different placeholder does NOT mean different composer design.

==================================================
8. MICROPHONE
==================================================

Use the same microphone button everywhere.

Same:
- icon
- dimensions
- glass
- alignment
- press state
- disabled state

If voice functionality is not enabled in a particular mode:
do not create fake functionality.

Either use the supported shared behavior or provide the correct disabled/hidden configuration based on existing product logic.

Do not implement unrelated voice features in this task.

==================================================
9. SEND BUTTON
==================================================

Send must be visually and behaviorally consistent.

Empty input:
use the same disabled state as Normal Chat.

Text entered:
use the same active state as Normal Chat.

Sending:
prevent accidental duplicate sends where the existing chat architecture does so.

Do not create five different send button behaviors.

==================================================
10. KEYBOARD BEHAVIOR
==================================================

This is extremely important.

All five screens must behave correctly with the real iOS keyboard.

When keyboard opens:

- composer moves/stays directly above keyboard
- correct safe area
- no gap caused by incorrect viewport calculation
- no composer hidden behind keyboard
- no duplicate composer
- no toolbar overlapping composer
- content remains scrollable
- current conversation remains usable

When keyboard closes:

- composer returns to bottom safe area
- no jump
- no stuck transform
- no leftover keyboard padding

Test repeated:

open keyboard
close keyboard
open keyboard
close keyboard

The layout must remain stable.

==================================================
11. SAFE AREA
==================================================

Support real iPhones correctly.

Respect:

env(safe-area-inset-bottom)

and the project's existing native safe-area implementation.

Do not hardcode spacing for one iPhone model.

The composer must work with:
- Dynamic Island devices
- Home indicator
- different iPhone viewport heights

==================================================
12. SCROLLING
==================================================

The conversation/content area must account for the composer height.

The composer must NOT cover the final message.

When a new message appears:
the user should be able to see/reach it.

When the composer expands because of multiline text:
content should adjust appropriately.

Do not use fragile fixed pixel offsets copied separately into five screens.

==================================================
13. MODE-SPECIFIC CONTENT MUST REMAIN
==================================================

CRITICAL:

DO NOT remove mode-specific UI.

For example:

AI Plan can still have:
- Study
- Website
- Programming
- plan-building UI
- plan-specific questions
- roadmap functionality

Writing Mode can still have:
- writing type
- Email
- Report
- Essay
- Personal Writing Style
- writing tools

Normal Chat remains normal Chat.

Other specialized assistants keep their unique content.

ONLY THE COMPOSER IS BEING STANDARDIZED.

==================================================
14. MODE-SPECIFIC AI LOGIC MUST REMAIN
==================================================

Using one composer does NOT mean all messages go to the same AI endpoint/prompt.

The shared component should emit/send the user's message to the parent mode.

The parent mode remains responsible for:
- correct AI route
- correct assistant
- correct system prompt
- correct conversation
- correct mode context

Example architecture:

BlueMindComposer
        ↓
onSend(message)
        ↓
current mode decides what to do

Do NOT hardwire the shared composer directly to one assistant.

==================================================
15. MESSAGE STATE
==================================================

Input state must be isolated per active screen/session.

If I type something in Writing Mode and navigate to another assistant, it must not randomly appear there unless the existing product intentionally preserves drafts.

Do not create cross-mode draft leakage.

==================================================
16. LOADING / STREAMING
==================================================

If Normal Chat supports streaming/loading states, inspect how those interact with the composer.

Standardize the relevant composer state where appropriate.

But DO NOT break mode-specific streaming implementations.

The composer can receive state such as:

disabled
sending
streaming

from its parent.

Again:
shared UI, mode-specific logic.

==================================================
17. ERROR STATE
==================================================

Do not display backend technical errors inside the composer.

Errors such as:

Route not found
500
API path
stack trace

must never replace the input UI.

Use the app's existing user-facing error handling.

Do not expose technical backend information to the user.

==================================================
18. NATIVE PRESS INTERACTIONS
==================================================

Buttons should retain BlueMind's existing iOS interaction feel.

Use:
- proper press feedback
- subtle scale/highlight if already used
- existing animation setting
- appropriate haptic behavior only if already supported

Do not introduce random animations.

==================================================
19. ANIMATIONS SETTING
==================================================

BlueMind already has an Animations setting.

If shared composer animations are controlled by this setting, preserve that behavior.

Animations ON:
normal polished transitions.

Animations OFF:
remove non-essential motion while preserving functionality.

==================================================
20. ACCESSIBILITY
==================================================

Do not make buttons visually nice but difficult to tap.

Keep appropriate iOS hit targets.

Add/use meaningful accessibility labels for:

- Add attachment
- Microphone
- Send message

Do not rely only on icons without accessible meaning.

==================================================
21. DO NOT CREATE FIVE COPIES
==================================================

This is one of the most important requirements.

After implementation, I do NOT want five independent copies of essentially the same composer.

Avoid duplicated:
- JSX
- CSS
- keyboard handling
- safe-area calculations
- button styles
- microphone layout
- send layout

There should be one clear shared source of truth.

If we later change the shared composer corner radius or spacing, all five experiences should receive the change automatically.

==================================================
22. MIGRATION STRATEGY
==================================================

Do this carefully.

First:
identify the working Normal Chat composer and all five consumers.

Second:
extract/reuse shared behavior without breaking Normal Chat.

Third:
migrate ONE specialized mode.

Verify it.

Then migrate the remaining modes.

Do not rewrite all five blindly in one giant replacement.

==================================================
23. DO NOT BREAK NORMAL CHAT
==================================================

Normal Chat currently acts as the reference.

Do not make Normal Chat worse while extracting the shared component.

Verify:

- sending
- attachments
- microphone
- multiline
- keyboard
- streaming
- scrolling

still work after refactor.

==================================================
24. IOS REAL DEVICE TEST
==================================================

Test on the connected physical iPhone.

Do not consider browser-only testing sufficient.

For EACH of the five chat-based experiences:

1. Open screen.
2. Tap composer.
3. Keyboard opens.
4. Type short English message.
5. Type multiline message.
6. Type Arabic.
7. Type Swedish characters: å ä ö.
8. Add emoji.
9. Test send.
10. Test +.
11. Test microphone if supported.
12. Close keyboard.
13. Reopen keyboard.
14. Scroll content.
15. Verify composer does not cover content.
16. Navigate away and return.
17. Take screenshot.

Confirm the app does NOT:
- restart
- reset navigation
- duplicate composer
- trigger unexpected Face ID
- lose the current page
- overlap keyboard

==================================================
25. IMPORTANT VISUAL TEST
==================================================

Take/inspect screenshots of all five experiences.

Compare ONLY the composer.

They should have:

same glass
same geometry
same controls
same spacing
same keyboard behavior

while the content above remains unique to each mode.

==================================================
26. DO NOT TOUCH
==================================================

Do NOT redesign:
- AI Plan content
- Writing Mode content
- Normal Chat content
- assistant personalities
- Projects
- Search
- Schedule
- Reminders
- Settings
- Face ID
- Hidden Chat
- Private Chat

unless a tiny shared dependency change is strictly required for the composer.

Do not perform unrelated cleanup.

==================================================
27. BEFORE FINISHING
==================================================

Search the codebase again for conversational composer/input implementations.

Make sure there isn't a sixth accidental duplicate still being used by one of these five experiences.

Remove obsolete composer duplication ONLY when it is confirmed unused and safe to remove.

Do not delete unrelated components.

==================================================
28. FINAL REPORT
==================================================

When finished, report clearly:

1. The exact five chat-based screens found.
2. Their file paths.
3. Which composer Normal Chat previously used.
4. The new/reused shared composer file.
5. Which duplicated composer implementations were removed.
6. Which mode-specific props/configuration each screen passes.
7. How keyboard handling is centralized.
8. How iOS safe area is handled.
9. Whether the glass is native, native-backed, or WebView/CSS.
10. Whether + actions differ by mode.
11. Whether microphone behavior differs by mode.
12. Physical iPhone test result for EACH of the five screens.
13. Any remaining duplicated composer code.
14. Any functionality that could not safely be unified.

FINAL RULE:

FIVE DIFFERENT EXPERIENCES.
FIVE DIFFERENT PURPOSES.
FIVE DIFFERENT PAGE CONTENTS.

BUT:

ONE BLUE MIND MESSAGE COMPOSER.
ONE IOS DESIGN.
ONE KEYBOARD BEHAVIOR.
ONE GLASS LANGUAGE.
ONE SHARED SOURCE OF TRUTH.

Do not redesign the pages themselves.
