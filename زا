Replace ONLY the iOS app icon for BlueMind AI.

Do not change any UI inside the app.

Remove the current temporary/default app icon completely.

Use the real BlueMind primary symbol/logo that is already used inside the app — the circular flower/sunflower-like BlueMind mark.

App icon design:

* Pure black background.
* BlueMind symbol centered.
* Symbol should be solid white.
* No text inside the icon.
* No “B”, no additional badge, no gradients, no blue temporary logo.
* Keep generous padding around the symbol so it looks clean and premium on the iOS Home Screen.
* Make sure it works correctly with the iOS rounded app-icon mask.

Use this as the official icon in the iOS AppIcon asset catalog for all required sizes/scales.

IMPORTANT: reuse the actual existing BlueMind logo asset if possible. Do not redraw or invent a new version of the logo.

After replacing it, rebuild/install the iOS app and verify the new icon appears correctly on the physical iPhone Home Screen.
