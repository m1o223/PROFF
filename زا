
TASK: Refine the BlueMind model / AI mode selector behavior and size.

IMPORTANT:
Do NOT redesign the whole selector.
Keep the current BlueMind iOS / Liquid Glass style.
This is a size + interaction + selection-state fix.

==================================================
1. MAKE THE SELECTOR LARGER
==================================================

Current problem:
The BlueMind selector feels too narrow/small compared with the reference.

Change:
- increase the overall popover width
- make it feel closer to a large iOS model-picker panel
- keep comfortable side margins from screen edges
- keep rounded corners
- keep current dark translucent glass material
- keep vertical scrolling

Target:
The menu should feel substantial and easy to scan, not like a narrow floating column.

==================================================
2. DO NOT AUTO-CLOSE ON SELECTION
==================================================

Current problem:
The menu may close or feel unstable when selecting options.

Desired behavior:
The selector stays OPEN after the user selects:
- a BlueMind model
- an AI mode
- Thinking

The menu should ONLY close when:
- user taps outside the panel
- user taps the selector button again
- user uses an explicit close gesture if one exists

Do NOT close after selecting an item.

==================================================
3. MODEL SELECTION STATE
==================================================

When the user taps a model:

Examples:
- BlueMind 3.0
- BlueMind 3.5
- BlueMind 4.0
- BlueMind 4.5
- BlueMind 5.0

Immediately show a visible selected state.

Required:
- checkmark on the selected model
- subtle highlighted row/background
- smooth selection animation
- previous selected model loses its checkmark/highlight
- only ONE model can be active at a time

Do not wait for menu close to update the UI.

==================================================
4. AI MODE SELECTION STATE
==================================================

For:
- General AI
- Study AI
- Research AI
- Work AI
- Writing AI
- Coding AI

Use the same behavior:

- selected AI mode gets ✓
- selected row gets subtle highlight
- previous AI mode deselects
- only ONE AI mode active at a time
- state updates instantly

==================================================
5. THINKING MUST ACTUALLY WORK
==================================================

Current problem:
Tapping Thinking gives little or no visual feedback and may not update state.

Fix the functionality.

"Enable Thinking" should be a real toggle/state.

When OFF:
- normal unselected appearance

When ON:
- clear checkmark or iOS-style toggle/check state
- selected highlight
- state stored immediately
- AI request configuration should receive thinking=true if that is the current project behavior

When tapped again:
- disable it
- remove selected state

Do not make Thinking a one-time navigation action.

==================================================
6. SELECTION ANIMATION
==================================================

Every selection should have subtle iOS feedback:

On tap:
- slight press scale / opacity change
- row highlight fades in
- checkmark appears smoothly

Do NOT use large flashy animations.

Use fast, clean iOS-style feedback.

==================================================
7. CURRENT SELECTION MUST BE OBVIOUS
==================================================

When the menu opens:

The user must instantly see:
- which BlueMind model is active
- which AI mode is active
- whether Thinking is enabled

All three states must be visually represented.

==================================================
8. SCROLLING
==================================================

Keep the selector vertically scrollable.

Requirements:
- smooth physical iPhone scrolling
- no fixed/frozen menu
- do not let the input/composer capture the scroll
- scrollbar may appear naturally
- all options must remain reachable

==================================================
9. OUTSIDE TAP
==================================================

Tap outside the selector:
- close selector smoothly

Tap inside:
- do NOT close unless explicitly intended

Selecting any option:
- keep selector open

==================================================
10. DO NOT TOUCH
==================================================

Do not modify:
- composer
- chat messages
- attachment sheet
- home carousel
- header buttons
- backend endpoints unrelated to model/mode/thinking state

Only modify:
- selector size
- selector open/close behavior
- selected visual states
- Thinking toggle behavior
- selection animation

==================================================
PHYSICAL IPHONE TEST
==================================================

Test on real iPhone:

1. open selector
2. verify it is larger/wider
3. tap BlueMind 3.5
   -> checkmark appears immediately
4. tap BlueMind 5.0
   -> 3.5 deselects, 5.0 gets checkmark
5. menu stays open
6. tap Research AI
   -> checkmark/highlight appears
7. tap Work AI
   -> Research deselects, Work selects
8. menu stays open
9. tap Enable Thinking
   -> visible ON state appears
10. tap it again
   -> visible OFF state
11. tap outside
   -> menu closes
12. reopen menu
   -> previous selections are still visible

At the end report:
- files changed
- where model state is stored
- where AI mode state is stored
- where Thinking state is stored
- how outside-tap dismissal works
- physical iPhone test result