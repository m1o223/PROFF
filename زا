Task: Rebuild ONLY the Settings sheet/container foundation.

Do NOT redesign any Settings rows, buttons, profile controls, typography,
or individual cards yet.

We are rebuilding Settings step-by-step.
This task is ONLY the outer Settings sheet, background material, layering,
geometry and safe-area behavior.

====================================================
CURRENT PROBLEMS
====================================================

The current Settings surface does not feel like native iOS.

Problems visible on the real iPhone:

1. The Settings background is too opaque/flat gray.
2. Content behind Settings is still visually distracting and readable.
3. It looks like a gray HTML panel placed over the app instead of an iOS sheet.
4. Background UI elements can visually interfere with Settings.
5. The chat composer / + button underneath must NEVER render above or through
   the Settings sheet.
6. The overall layering/z-index/stacking behavior needs to be corrected.

====================================================
TARGET: NATIVE iOS SHEET
====================================================

Rebuild the Settings outer container as a proper iOS-style modal sheet.

The sheet should use a dark graphite/near-black glass material.

I want STRONG background blur.

Important distinction:

I do NOT want transparent glass where text behind it can still be read.

I want:
- strong blur
- dark graphite tint
- subtle translucency
- background shapes/light may be barely perceptible
- background text must NOT remain readable
- no muddy gray overlay

Think of a premium native iOS dark sheet:
depth is visible, but the underlying interface is visually suppressed.

====================================================
GEOMETRY
====================================================

The Settings sheet should:

- occupy the intended large screen area
- extend correctly toward the bottom safe area
- use large, smooth iOS-style top corner radii
- have continuous soft curves
- avoid harsh rectangular edges

Do not add multiple nested background panels.

There should be ONE clear primary Settings sheet surface.

====================================================
LAYERING — VERY IMPORTANT
====================================================

Settings must be above ALL normal application UI.

When Settings is open, these must stay underneath it:

- chat composer
- + button
- send button
- microphone
- drawer controls
- chat cards
- carousel/content
- any floating app controls

Nothing from the underlying page may visually appear above the Settings sheet.

Audit:
- z-index
- stacking contexts
- position: fixed/absolute elements
- native overlays
- WebView layers

Do not simply assign random huge z-index values.
Find the actual stacking conflict and fix the hierarchy correctly.

====================================================
BACKGROUND BEHIND THE SHEET
====================================================

The underlying app may remain visible only through the intended native blur.

Do NOT manually reduce opacity of every background component.

Do NOT individually hide chat elements.

The Settings presentation layer itself should provide the correct separation.

====================================================
SCROLLING
====================================================

Settings content must scroll INSIDE the Settings sheet.

The underlying chat/drawer must NOT scroll while Settings is open.

The sheet itself must remain geometrically stable while its internal content scrolls.

====================================================
SAFE AREA
====================================================

Respect:
- top iPhone safe area / Dynamic Island
- bottom home indicator safe area

No Settings content should collide with system UI.

====================================================
DO NOT CHANGE YET
====================================================

Do NOT modify:

- Settings title
- X button design
- profile image
- edit-profile button
- Change Email
- Change Password
- Subscription
- Delete Account
- Appearance
- Accent Color
- any Settings row/card
- fonts
- icons

Those will be redesigned in later tasks.

For now, preserve all existing Settings content and functionality.

ONLY rebuild the container underneath/around it.

====================================================
OLD CODE
====================================================

If the current Settings wrapper/background implementation conflicts with this
architecture, remove or replace that presentation code cleanly.

Do not stack another translucent rectangle over the existing Settings background.

I want ONE clean Settings presentation layer.

====================================================
REAL IPHONE TEST
====================================================

Test on the connected real iPhone.

Verify:

1. Open Settings.
2. Settings appears above every app control.
3. Chat composer/+ never appears above Settings.
4. Background is strongly blurred.
5. Text behind Settings cannot be clearly read.
6. Surface remains dark graphite/near-black, not muddy gray.
7. Settings scrolls independently.
8. Underlying page does not scroll.
9. Top and bottom safe areas are correct.
10. Closing Settings restores the underlying page normally.

Do not change any Settings controls yet.

At the end report:
- old container architecture
- root cause of the layering problem
- new sheet architecture
- blur/material implementation
- files changed
- real-iPhone test result
