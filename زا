Investigate ONLY the current iOS voice transcription failure.

The current problem is:

Recording starts, but transcription fails and the user sees Transcription failed.

Do NOT make unrelated UI changes.
Do NOT guess the cause.
Do NOT stop at the first possible explanation.

I want a full root-cause investigation.

Trace the entire pipeline from beginning to end:

Mic tap
→ microphone permission
→ AVAudioSession
→ AVAudioRecorder
→ recorded file creation
→ file finalization
→ file path / file access
→ audio format / MIME type
→ file conversion if any
→ upload/request creation
→ frontend API call
→ backend endpoint
→ backend request parsing
→ provider/transcription API call
→ provider response
→ backend response
→ frontend response parsing
→ insertion of transcribed text into the composer.

Inspect all files related to voice recording and transcription, including:

* iOS native recording code
* React/Capacitor voice hooks
* API client
* backend transcription route
* shared Web/iOS audio code
* file handling
* auth
* upload logic
* response parsing

Check every realistic failure possibility, including:

* empty or zero-byte audio file
* file not finalized before reading
* invalid temporary file path
* file:// handling problems
* unsupported audio format
* wrong extension
* wrong MIME type
* wrong sample rate/channels
* broken Blob/File/base64 conversion
* browser-only code still being used by iOS
* wrong endpoint
* wrong backend route
* missing or invalid authentication
* wrong multipart field name
* wrong headers
* malformed request body
* backend not receiving the audio
* backend receiving empty audio
* API/provider rejection
* HTTP 4xx/5xx
* provider auth/configuration issue
* rate limit
* network failure
* malformed provider response
* response field mismatch
* frontend parsing error
* async/race-condition issue

Add temporary diagnostic logging if needed.

I want concrete evidence at each step:

* did recording start?
* was a real file created?
* what is the file size?
* what is the duration?
* what is the exact format?
* was the file successfully read?
* what request was sent?
* did the backend receive it?
* what HTTP status came back?
* what exact error body came back?
* did the provider receive the audio?
* did the provider return transcription text?

Do not hide all errors behind Transcription failed during debugging.

Preserve the exact internal error so the real failure point can be identified.

If one hypothesis is disproven, continue investigating the next one.

Do not stop until you have identified the actual root cause, or until you can show clear evidence that the remaining failure is outside the codebase and explain exactly where.

If the current implementation is structurally broken because old Web recording/transcription logic is still being forced through iOS, it is acceptable to rewrite the iOS-specific transcription path cleanly.

However:

* do not delete or break the website implementation;
* do not modify unrelated web behavior;
* isolate iOS-specific logic safely.

Do not make a speculative fix and call it done.

After finding and fixing the problem, report:

1. the exact root cause;
2. the exact file/function where it happened;
3. the evidence that proved it;
4. what was changed;
5. why the change fixes the issue;
6. confirmation that repeated record → stop → transcribe cycles now work;
7. confirmation that the website was not modified or broken.

Important: keep investigating until the real cause is found. Do not stop after a superficial patch.
