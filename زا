TASK: REMOVE ALL ANIMATIONS FROM THE ENTIRE SETTINGS EXPERIENCE ON IOS

I want a complete temporary animation reset for BlueMind Settings.

The current Settings experience has visual glitches when:

- opening Settings
- closing Settings
- tapping buttons
- opening child sections
- closing child sections
- tapping rows
- tapping X
- pressing controls
- moving between Settings screens

I want ALL Settings-related animations removed for now.

====================================================
MAIN GOAL
====================================================

Settings must become completely static and predictable.

NO animations.

NO transitions.

NO scale effects.

NO fade effects.

NO slide effects.

NO spring effects.

NO bounce effects.

NO pressed shrinking.

NO hover-like effects.

NO opacity animations.

NO transform animations.

NO delayed appearing/disappearing elements.

NO transition between parent Settings and child Settings.

Everything should appear immediately and disappear immediately.

====================================================
REMOVE ANIMATIONS FROM MAIN SETTINGS
====================================================

When opening Settings:

DO NOT:
- fade in
- slide up
- scale up
- spring
- blur animate
- grow from another element

The Settings sheet should appear immediately in its final position.

When closing Settings:

DO NOT:
- fade out
- slide down
- shrink
- animate blur
- animate opacity

It should disappear immediately.

====================================================
REMOVE BUTTON PRESS ANIMATIONS
====================================================

Inside Settings, buttons and rows must NOT move when touched.

Remove any:

- scale(0.95)
- scale changes
- translate
- bounce
- spring
- press-down animation
- lift animation
- shadow animation
- opacity animation

When I tap a row:

the row must remain geometrically stable.

No jumping.
No shrinking.
No expanding.

====================================================
REMOVE CHILD SELECTOR ANIMATIONS
====================================================

For Settings child selectors such as:

- Appearance
- App Accent Color
- Chat Color
- Voice
- Voice Language
- Speech Speed
- Chat History
- Notifications
- Auto-play
- any other Settings selector

Remove all custom opening/closing animation behavior.

The child UI should:

appear directly
and
close directly

Do not animate the parent Settings underneath.

====================================================
REMOVE CSS TRANSITIONS
====================================================

Audit all Settings-related CSS.

Remove/disable:

transition:
animation:
@keyframes
transform animations
opacity transitions
filter transitions
backdrop-filter transitions
box-shadow transitions

Also inspect pseudo states such as:

:hover
:active
:focus
:focus-visible

Do not allow any of them to create movement.

====================================================
REMOVE REACT ANIMATION LOGIC
====================================================

Search for any Settings-specific code using:

setTimeout for visual transitions
requestAnimationFrame for animation
animated state flags
isOpening
isClosing
entering
exiting
transition state
motion wrappers
framer-motion
CSS class toggling for animation

Remove or bypass those paths.

====================================================
REMOVE NATIVE SETTINGS ANIMATION OVERRIDES
====================================================

If native iOS code is manually adding:

UIView.animate
UIViewPropertyAnimator
spring animations
custom presentation animations
custom dismissal animations

disable those custom Settings animations too.

IMPORTANT:

Do NOT break required native system behavior elsewhere in the app.

This task is only for Settings-related custom animation behavior.

====================================================
DO NOT BREAK FUNCTIONALITY
====================================================

Removing animations must NOT break:

- opening Settings
- closing Settings
- scrolling
- X button
- child selectors
- changing values
- logout
- delete account
- account controls
- notifications
- appearance
- colors
- voice
- language

All functionality must remain intact.

Only motion is removed.

====================================================
LAYOUT MUST NOT MOVE ON TOUCH
====================================================

This is very important.

When pressing any Settings row/button:

its:
- width
- height
- position
- padding
- border radius
- surrounding layout

must remain unchanged.

There must be ZERO visual jump.

====================================================
BACKGROUND
====================================================

When Settings opens:

background dimming should immediately appear at its final value.

Do not animate the dimming.

When Settings closes:

background dimming should immediately disappear.

====================================================
TEMPORARY DESIGN RULE
====================================================

For now I prefer:

stable UI
over
beautiful animation

Do not try to replace the removed animations with different animations.

This is a temporary "zero animation" phase.

After the Settings system becomes fully stable, we will add animations back one by one later.

====================================================
AUDIT EVERYTHING
====================================================

Do a full audit of every Settings-related animation.

Do not only remove the obvious opening animation.

Check:

- parent Settings
- cards
- rows
- child panels
- buttons
- X button
- scroll states
- profile edit button
- selectors
- alerts
- overlays
- background dimming
- pressed states

There should be NO custom animation remaining in Settings.

====================================================
REAL IPHONE TEST
====================================================

Test on the connected real iPhone.

Test repeatedly:

1. Open Settings 10 times.
2. Close Settings 10 times.
3. Tap many different rows.
4. Open/close child selectors.
5. Scroll up/down.
6. Tap X.
7. Change Appearance.
8. Change colors.
9. Open Voice.
10. Open Notifications.

Verify:

- no flicker
- no jump
- no disappearing/reappearing layers
- no delayed rendering
- no animation
- no layout shifting

====================================================
FINAL REPORT
====================================================

When finished report:

1. Every Settings animation source you found.
2. Every CSS transition/animation removed.
3. Every React animation path removed.
4. Every native custom animation disabled.
5. Whether any system-native unavoidable animation remains.
6. Real iPhone stability test result.

IMPORTANT:

Do NOT add any new animation.

This task is ONLY:

REMOVE ALL SETTINGS ANIMATIONS
STABILIZE THE UI
KEEP FUNCTIONALITY WORKING

We will design animations again later from zero.
