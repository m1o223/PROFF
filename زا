Task: Fully diagnose and fix the iOS voice-recording/transcription flow, then redesign the recording composer with a polished BlueMind-native audio meter.

We are working ONLY on the BlueMind AI iOS experience.

This task has TWO separate parts:

Part A — Diagnose and fix the current “Transcription failed” problem.
Part B — Redesign the active recording UI/audio visualization.

Do NOT treat this as only a visual task.

I want you to first understand exactly what happens from the moment the user taps the microphone until transcribed text appears back inside the composer.

⸻

IMPORTANT PROJECT RULE

BlueMind originally existed as a web app before the iOS implementation, so some browser/web microphone or transcription code may still exist in shared hooks/components.

Do NOT modify or break the website.

Do NOT delete shared web code globally simply because iOS does not need it.

If legacy web logic is still affecting the iOS path:

* isolate the iOS path;
* disconnect the problematic web logic from iOS only;
* leave web behavior unchanged;
* use native iOS recording/audio APIs where appropriate.

If there is ambiguity about whether code belongs to Web, iOS, or both, do not guess. Explain it before making a destructive change.

⸻

PART A — DIAGNOSE “TRANSCRIPTION FAILED”

Right now recording appears to start and the UI reacts, but after stopping/sending the recording, the user gets:

“Transcription failed”

I do NOT want this error hidden or replaced with a generic success state.

I want you to determine the actual failure point.

Trace the entire pipeline step by step.

⸻

STEP A1 — TRACE THE COMPLETE AUDIO FLOW

Inspect exactly what happens during:

Tap microphone
→ microphone permission
→ audio session configuration
→ recorder creation
→ recording start
→ audio metering
→ recording stop
→ finalize audio file
→ read audio file
→ convert/prepare payload
→ send to transcription service/API
→ receive response
→ parse response
→ insert text into composer.

Identify where this current failure occurs.

Do not assume the backend is the issue.

Do not assume the recorder is the issue.

Prove which stage fails.

⸻

STEP A2 — CHECK MICROPHONE PERMISSIONS

Verify:

* microphone permission is correctly declared in iOS;
* permission is actually granted before recording starts;
* denied/restricted permission is handled properly;
* app does not attempt to start recording before permission callback finishes.

If permission is already granted, continue diagnosis.

Do not keep requesting permission unnecessarily.

⸻

STEP A3 — CHECK AVAudioSession

Inspect the native iOS audio-session configuration.

Verify:

* correct AVAudioSession category;
* correct mode;
* recording is activated before recorder starts;
* audio session is deactivated/cleaned correctly afterward;
* switching between microphone/keyboard/chat does not corrupt the session;
* interruptions are handled;
* route changes are handled.

Important:

In one test screenshot iOS displayed a system message similar to “Moved to Mac” while using AirPods/audio routing.

Do NOT assume this is the cause, but check whether Bluetooth/AirPods/Continuity route changes can invalidate or interrupt the recording.

Verify behavior with:

* built-in iPhone microphone;
* AirPods/Bluetooth if available;
* route changes during recording.

⸻

STEP A4 — VERIFY AVAudioRecorder OUTPUT

Verify that the actual recorded file exists after stop().

Check:

* file URL/path;
* file existence;
* file size;
* file duration;
* actual recorded bytes;
* format/container;
* sample rate;
* channel count.

Make sure we are not sending:

* an empty file;
* a zero-byte file;
* a file that is still being written;
* an invalid/corrupted recording;
* the wrong local URL;
* an unsupported encoding.

Add temporary diagnostic logging if needed.

Do NOT leave noisy production logs afterward.

⸻

STEP A5 — CHECK RECORDING FINALIZATION / RACE CONDITIONS

Pay special attention to timing.

The failure may be caused by reading or uploading the file too early.

Verify that:

* recorder has fully stopped;
* file has been finalized;
* file can be safely read;
* conversion/upload does not begin before file completion.

Check for async race conditions between:

stopRecording
→ file reading
→ transcription request.

If the UI immediately transitions to transcription while the file is not ready, fix the underlying sequencing.

Do not add arbitrary delays like setTimeout(1000) unless absolutely necessary and justified.

Prefer proper async completion/state handling.

⸻

STEP A6 — CHECK FILE READING ON IOS

Since this project came from Web/React/Capacitor, verify that the iOS implementation is not relying on browser-specific file handling in a way that fails with native iOS file URLs.

Check:

* file:// path handling;
* Blob/File conversion;
* base64 conversion if used;
* native filesystem APIs;
* multipart upload creation;
* temporary-file access;
* MIME/content type.

If browser-style fetch(file://...), DOM APIs, Web File APIs, or another web-only method is unreliable on iOS, replace only the iOS path with a reliable native/Capacitor-compatible approach.

Leave web logic untouched.

⸻

STEP A7 — VERIFY THE TRANSCRIPTION REQUEST

Inspect the actual request sent to the transcription service.

Verify:

* endpoint;
* request method;
* authentication;
* headers;
* multipart field name;
* filename;
* MIME type;
* audio payload;
* model/provider parameters;
* language parameters if applicable.

Confirm that the request actually reaches the API.

Log/debug:

* HTTP status;
* response body;
* provider error;
* network error;
* parsing error.

Do not reduce all failures to "Transcription failed".

Internally distinguish:

* recording error;
* local file error;
* upload error;
* authentication error;
* network error;
* API rejection;
* response parsing error.

The production UI can still show a clean user-friendly message, but development/debugging must reveal the real cause.

⸻

STEP A8 — CHECK RESPONSE PARSING

If the API successfully returns transcription text, make sure the iOS code reads the correct response field.

Verify that an API schema change or iOS-specific path is not causing valid responses to be interpreted as failures.

The transcribed result must be inserted into the existing composer text.

Do not automatically send the message unless the current BlueMind behavior explicitly requires that.

User should normally be able to review/edit the transcription.

⸻

STEP A9 — ERROR RECOVERY

If transcription fails:

* recording UI must exit safely;
* microphone resources must be released;
* app must not get stuck in recording state;
* user must be able to try again immediately;
* previous composer text must not disappear;
* attachments must not disappear;
* keyboard state must remain sane.

Do not allow one failed transcription to break all later recordings.

⸻

PART B — REDESIGN THE RECORDING COMPOSER

Once the technical recording/transcription flow is correct, update the visual recording state.

The current BlueMind recording visualization is not correct.

Current problems:

1. The audio visualization is concentrated mostly in the center.
2. It looks like a traditional waveform/ocean wave.
3. X and Stop do not have the polished button backgrounds I want.
4. The full horizontal recording area is not being used well.

I want the interaction inspired by the attached ChatGPT iOS reference, but it must remain BlueMind’s own design.

⸻

RECORDING BAR LAYOUT

Keep the outer composer shape consistent with the existing BlueMind input.

During recording, arrange it approximately as:

[ Cancel X ]   [ live audio meter across available center space ]   [ Stop ]   [ Send ]

Do not redesign unrelated areas of the chat.

⸻

CANCEL BUTTON

The X button must be inside its own circular background.

Requirements:

* round;
* background slightly lighter than the main composer;
* subtle contrast;
* white/light X icon;
* centered precisely;
* comfortable hit target;
* same visual family as the Stop button.

It should feel like an actual polished iOS control, not an icon floating directly on the composer.

Pressing X:

* stops recording;
* discards the current audio;
* does NOT transcribe;
* releases recorder/audio-session resources;
* returns to normal composer;
* preserves any existing text/attachments that existed before recording.

⸻

STOP BUTTON

Stop must also use a circular background slightly lighter than the composer.

Inside:

* small white rounded-square Stop icon.

The Stop button must be visually distinct but not oversized.

Pressing Stop means:

recording ends
→ audio file finalizes
→ transcription begins.

During transcription, prevent accidental repeated Stop actions.

⸻

SEND BUTTON

Keep Send visually the strongest action.

Preferred:

* white circular button;
* dark/black upward arrow.

Keep it consistent with the current BlueMind visual language if current implementation already matches this.

Do not unnecessarily redesign it.

⸻

AUDIO VISUALIZATION — VERY IMPORTANT

Do NOT use a continuous waveform.

Do NOT use a waveform that visually resembles ocean waves.

Do NOT draw one connected line.

Use many separate vertical rounded capsules/bars.

At silence/very low input:

each bar should appear almost like a small dot/short dash.

As the user speaks:

each bar grows vertically according to the real microphone level.

Think:

• • • ▪ ▮ ▪ ▪ • ▮ ▪ • •

but animated smoothly.

Each bar should have:

* fixed narrow width;
* fully rounded ends;
* small gap between neighboring bars;
* dynamic height.

⸻

AUDIO BAR HEIGHT

The bars must NEVER become huge.

Define:

* a small minimum height;
* a moderate maximum height.

Even loud speech should remain elegant.

I do not want giant peaks.

The visualization should feel controlled and premium.

Example behavior:

silence → tiny bars

quiet speech → slightly taller

normal speech → medium

louder speech → taller

very loud speech → clamp at maximum height

Do not let clipping create extreme bars.

⸻

USE REAL MICROPHONE METERING

The animation must respond to actual microphone amplitude.

Since iOS uses AVAudioRecorder, inspect the native metering capabilities.

If necessary:

* enable audio metering;
* read average/peak power;
* normalize decibel values into a usable 0–1 range;
* smooth the value before rendering.

Do NOT use random fake heights that animate regardless of voice.

If user stops speaking, the bars should settle toward their minimum height.

If user speaks louder, bars should visibly respond.

⸻

SMOOTHING

Raw microphone values may be too noisy.

Add appropriate smoothing so bars do not jitter violently.

Use a lightweight interpolation/EMA/smoothing strategy.

Desired visual behavior:

responsive but soft.

It should not lag behind speech significantly.

It should also not flicker every frame.

⸻

DISTRIBUTION ACROSS THE INPUT

This is extremely important.

Do NOT render only 15–20 bars clustered in the middle.

Use the available horizontal space between:

Cancel X

and

Stop

Fill that center region naturally with the audio bars.

Number of bars should respond to available width.

For example:

available width
÷
(bar width + gap)

approximate number of bars.

Do not hardcode something that looks good on only one iPhone.

⸻

BAR MOVEMENT MODEL

I do not want all bars to have exactly the same height.

But I also do not want a moving historical waveform like an audio editor.

Create a natural live meter:

* current voice level drives the overall amplitude;
* neighboring bars can have slight variation;
* the center can be subtly stronger if visually helpful;
* variations must stay tied to real input level.

The result should feel alive without looking random.

⸻

BAR COLOR

This remains a BlueMind design.

Acceptable choices:

BlueMind blue

or

white/light gray

Choose whichever integrates best with the current composer.

My preference is that the implementation supports the BlueMind accent color cleanly.

Do NOT use multiple rainbow colors.

Do NOT introduce gradients unless they genuinely improve the existing design.

Keep it minimal.

⸻

TRANSCRIBING STATE

When Stop is pressed:

1. stop the native recorder safely;
2. finalize the audio file;
3. transition out of live audio metering;
4. show a polished Transcribing… state;
5. perform the transcription asynchronously.

While transcribing:

* no live bars;
* no fake microphone activity;
* show a subtle spinner/progress indicator;
* show Transcribing…;
* do not freeze the UI.

On success:

* restore normal composer;
* insert transcription text;
* focus text editor if appropriate.

On failure:

* restore normal composer;
* preserve previous content;
* show a clean error;
* allow retry.

⸻

THREADING / PERFORMANCE

Audio recording/metering must not make the chat UI lag.

Avoid unnecessary React re-renders for every raw audio sample.

Use a reasonable display refresh/update frequency.

Do not update the entire conversation tree just because audio meter values changed.

If native code emits frequent meter values, keep rendering isolated to the recording visualization component.

File reading/transcoding/upload/transcription must not block the main UI thread.

⸻

CLEANUP

Verify cleanup on:

* Cancel;
* Stop;
* successful transcription;
* failed transcription;
* app backgrounding;
* interruption;
* audio-route change;
* component unmount;
* switching chats if allowed.

No recorder, timer, native listener, meter interval, animation loop, or audio session should remain active after recording ends.

Repeated:

record → cancel → record → stop → transcribe → record

must work reliably.

⸻

DO NOT BREAK EXISTING FEATURES

Verify:

* normal text input;
* keyboard;
* Arabic RTL;
* English/Swedish LTR;
* attachments;
* images;
* Files/PDF;
* send;
* plus button;
* chat modes;
* composer resize feature;
* user text selection;
* existing backend messaging.

Voice work must not regress these.

⸻

IOS VS WEB

Again:

If you find:

MediaRecorder
getUserMedia
AudioContext
Web Speech
browser Blob/File logic
CSS waveform code
browser audio event handlers

affecting the iOS path, inspect whether that dependency is actually needed.

For iOS, prefer the native iOS recording path.

But do not remove web functionality globally.

Keep platform-specific behavior separated.

⸻

DIAGNOSTIC REPORT BEFORE/AFTER FIX

I specifically want to know WHY transcription was failing.

After finishing, do not simply say:

“Fixed transcription.”

Report the actual root cause.

Example:

Recording worked, but the iOS temporary file URL was being passed through a browser File API and produced an empty upload.

or:

The audio file was valid, but transcription was triggered before AVAudioRecorder finalized it.

or:

The server returned HTTP 401 because the native transcription request did not include the correct authorization.

These are only examples.

Report the real cause you discover.

⸻

TEST MATRIX

Test all of these:

1. Start recording and remain silent.
2. Speak quietly.
3. Speak normally.
4. Speak loudly.
5. Stop and successfully transcribe.
6. Cancel without transcription.
7. Record twice consecutively.
8. Record after a failed transcription.
9. Record with existing text in composer.
10. Record with an image attachment already selected.
11. Arabic speech.
12. English speech.
13. Built-in iPhone microphone.
14. AirPods/Bluetooth route when available.
15. Keyboard open.
16. Keyboard hidden.
17. Different iPhone sizes.
18. Weak/no network during transcription.

⸻

FINAL VISUAL ACCEPTANCE

During active recording I expect:

Left: circular X button with lighter background.

Center: many small rounded vertical audio bars filling most of the available center width.

Silence = almost dots.

Voice = bars smoothly increase/decrease according to actual microphone volume.

No giant peaks.

No connected waveform.

No ocean-wave appearance.

Right: circular Stop button containing a small white square.

Far right: current Send button, visually strongest.

The outer composer should remain visually consistent with BlueMind.

⸻

FINAL REPORT

When done, tell me:

1. Exact root cause of Transcription failed.
2. How it was fixed.
3. Which files changed.
4. What native iOS audio APIs are now used.
5. How audio-level metering works.
6. How decibel values are normalized/smoothed.
7. How the responsive number of bars is calculated.
8. Min/max visual bar height.
9. How Cancel and Stop differ technically.
10. How resource cleanup is handled.
11. Whether legacy Web audio code was affecting iOS.
12. If yes, exactly how it was isolated.
13. Confirmation that website functionality was not changed.
14. Confirmation that microphone/transcription now works through repeated test cycles.

If fixing the transcription requires changing an API/backend contract, or if you discover a risky dependency with the website, do not guess or perform a broad rewrite. Explain the problem to me first.
