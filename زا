TASK: Replace ALL Settings back buttons on iOS with REAL native iOS back controls.

IMPORTANT:
This applies to EVERY child/subpage inside Settings.

Do NOT only fix Change Email.

====================================================
CURRENT PROBLEM
====================================================

Settings contains many child pages, for example:

- Change Email
- Change Password
- General
- Notifications
- Voice
- About
- Security
- Data Controls
- Privacy
- Subscription
- any other Settings subsection

Many of these pages currently have a custom back arrow/button.

I do NOT want custom React/CSS back buttons on iOS.

====================================================
TARGET
====================================================

Every back button inside Settings must use the real native iOS back-button/navigation style.

Use Apple/iOS native behavior and rendering.

I want:

- native iOS back chevron / back control
- native sizing
- native spacing
- native touch target
- native accessibility
- native press/highlight behavior
- native positioning

Do NOT imitate it with CSS.

====================================================
IMPORTANT DISTINCTION
====================================================

The X button closes the entire Settings sheet.

The Back button does NOT close Settings.

Back should ONLY return one level inside Settings.

Example:

Settings
→ Change Email
→ Back
→ Main Settings

NOT:
→ Menu
NOT:
→ Chat
NOT:
→ close Settings

====================================================
REMOVE OLD CUSTOM BACK BUTTONS
====================================================

On iOS, remove/bypass the existing custom back implementations.

Do not leave the old circular arrow behind the native one.

There must be only ONE back control.

====================================================
APPLY GLOBALLY
====================================================

Audit every Settings child route/subview.

Any screen that currently has:
- ←
- chevron-left
- circular back button
- custom arrow icon

must be converted to the shared native iOS back implementation.

Do not fix these pages individually with separate one-off code.

Create/reuse ONE shared native Settings navigation/back system.

====================================================
POSITION
====================================================

Back control should sit in the native top-left navigation area.

Respect:
- safe area
- top inset
- left inset

It should align consistently across all Settings child screens.

====================================================
DO NOT CHANGE
====================================================

Do NOT redesign:
- page content
- inputs
- titles
- cards
- colors
- business logic

This task is ONLY replacing all Settings Back controls on iOS.

====================================================
REAL IPHONE TEST
====================================================

Test on the connected real iPhone.

Open several Settings child pages:

- Change Email
- Change Password
- General
- Notifications
- Voice
- About
- Security

Verify:

1. Same native iOS back control appears on all.
2. No custom CSS back button remains.
3. Back returns exactly one Settings level.
4. Main Settings remains open.
5. Back never sends the user directly to Chat/Menu.
6. Position is consistent on every page.

When finished report:
- every Settings screen audited
- old back implementation removed
- shared native back/navigation implementation used
- real-iPhone test result