IMPORTANT — NATIVE iOS GLASS IS REQUIRED.

The current ChatPop UI still does NOT use the native iOS glass appearance I requested.

I do NOT want a custom CSS imitation of Apple's glass.

I want you to implement the REAL Apple/iOS-native glass/material effect for the ChatPop iOS application.

This requirement applies especially to:

- Chat message input/composer
- Menu button
- New Chat button
- AI model selector
- Floating controls
- Model selection panel where appropriate

Use Apple's actual native iOS APIs/components/materials available for the iOS version we are targeting.

DO NOT manually imitate the effect using:
- CSS backdrop-filter as the main solution
- Custom gradients
- Fake reflections
- Artificial glow
- Multiple box-shadows
- Custom blur combinations pretending to be Apple Liquid Glass

I want the native Apple implementation.

If React + Capacitor cannot provide the real native effect directly, then use the correct native iOS solution.

You ARE allowed to:

- Add the necessary compatible libraries/packages
- Install required dependencies
- Add Swift code if necessary
- Add SwiftUI/UIKit components if necessary
- Create a small Capacitor native plugin/bridge if necessary
- Modify the minimum required native iOS files
- Use Apple's official native APIs
- Integrate native controls with the existing React/Capacitor application

Do whatever is technically required to get the REAL native iOS glass effect.

However:

- Keep the existing ChatPop project.
- Do NOT recreate the entire iOS project.
- Do NOT delete /ios.
- Do NOT run `npx cap add ios` again.
- Do NOT create another Xcode project.
- Do NOT change the Bundle Identifier.
- Do NOT change my Development Team or Signing configuration.
- Do NOT unnecessarily restructure the project.

If a native Swift/SwiftUI/UIKit layer is needed specifically for these controls, implement it cleanly on top of the EXISTING Capacitor iOS project.

The goal is NOT to make the whole application native Swift.

The goal is to keep our existing React + Capacitor application while using real native iOS UI/material where required.

Also fix the Menu:

- It must open FROM THE LEFT.
- Animation direction: LEFT → RIGHT.
- It must cover the ENTIRE iPhone screen when fully opened.
- No part of the Chat screen should remain visible.
- Closing it should animate back toward the LEFT.

IMPORTANT:
Do not replace the native glass requirement with a visually similar web/CSS solution because it is easier.

If additional packages or native dependencies are required, install and configure them.

After implementation:

1. Build the frontend.
2. Sync/update the existing Capacitor iOS project.
3. Build/check the native iOS integration.
4. Make sure the existing Xcode project remains intact.
5. Make sure it can still run on my real iPhone.
6. Report exactly which packages, Swift/SwiftUI/UIKit files, Capacitor plugins, or native APIs you added or changed.

Do not add unrelated features.

The priority of this task is:

REAL Apple-native iOS glass/material UI
+
full-screen Menu opening LEFT → RIGHT
+
preserve the existing ChatPop iOS project.
