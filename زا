The current Writing Mode composer is still wrong.

It visually looks like a large textarea/card, not like the iOS chat composer used in Normal Chat.

Please fix this by reusing the exact SAME shared Normal Chat composer component and native/native-backed iOS glass styling.

IMPORTANT:
Do not make a separate Writing Mode composer that only imitates the chat composer.

Required changes:

1. Composer must start compact, same height as Normal Chat.
2. It can grow vertically only when the user types multiple lines.
3. Remove the oversized empty vertical area inside the current input.
4. Use the exact same:
   - glass material
   - thin border/stroke
   - corner radius
   - background opacity
   - icon placement
   - + button
   - microphone button
   - send button
   - keyboard behavior
   - safe-area behavior
as Normal Chat.
5. Keep the Writing placeholder:
   “Ask BlueMind to write…”
6. The composer must feel like a CHAT BAR, not a CARD.
7. Move the quick actions directly above the composer with a small consistent gap.
8. Make the quick action buttons lighter and more compact, using the same iOS glass button system.
9. Do not leave a huge space between quick actions and composer.
10. Test on the physical iPhone with keyboard open and closed.

Visual target:
[Professional] [Simpler] [Rewrite] [Translate]
              small gap
[ + | Ask BlueMind to write… | mic | send ]

The composer should visually match Normal Chat almost exactly.
Only allow a slightly larger expanded state when multiline text requires it.

Please inspect the existing Normal Chat composer source and reuse it instead of copying its CSS.
