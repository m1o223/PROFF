Task: Reset the current iOS Create/Studio screen to a clean shell and decouple it from the Web implementation.

We are NOT continuing with the current Create Image design.

The existing screen has become too tied to the old Web implementation and too cluttered to evolve safely.

I want a controlled reset of the iOS Create/Studio presentation only.

⸻

1. REMOVE THE CURRENT IOS CREATE CONTENT

On the iOS Create/Studio screen, remove the current visible content:

* current image gallery/cards;
* Anime card;
* Character Design card;
* all current sample images;
* current “Create an image” title/content;
* current prompt composer specific to Create Image;
* current Create Image chip;
* current image-generation input;
* current style/category sections;
* current Create Image mode-specific UI;
* any visible old AI/create controls inside this screen that belong to the current implementation.

Do NOT leave the old UI hidden behind new UI.

Remove the iOS presentation code cleanly if it is no longer used.

⸻

2. KEEP ONLY A CLEAN TOP-LEVEL SHELL

After cleanup, the iOS page should contain only three top-level options:

Create Video

Create Image

History

Nothing else for now.

No prompt input yet.

No styles yet.

No quality controls yet.

No aspect ratio yet.

No gallery yet.

No generated media cards yet.

No backend behavior.

We will design and implement each section step by step afterward.

⸻

3. DESIGN OF THESE THREE OPTIONS

Keep them simple and polished.

They can be:

* three native iOS tabs;
* segmented/top navigation;
* or three compact buttons/cards.

But do not over-design them yet.

Use the existing BlueMind iOS design language:

* dark background;
* native iOS material/glass where appropriate;
* existing typography;
* existing spacing;
* existing BlueMind primary accent only when needed.

If BlueMind blue is used, use:

BLUE_MIND_COLORS.primary

/ --bm-primary

current value:

#193B68

Do not invent another blue.

⸻

4. IOS AND WEB MUST BE DECOUPLED

This part is critical.

The current iOS Create experience appears to be too dependent on or coupled to the Web implementation.

I want the iOS Create/Studio UI to become independently controllable.

Inspect the architecture and identify:

* which components are shared between Web and iOS;
* which CSS/layout/state belongs to Web;
* which Web-specific logic is affecting iOS;
* which Create Image state/components are reused across platforms;
* whether Capacitor/WebView rendering is forcing iOS to inherit Web behavior.

⸻

5. DO NOT BREAK THE WEBSITE

Do NOT delete or redesign the Web Create Image experience.

The website should continue to behave exactly as it currently does.

The goal is:

Web keeps its existing implementation.

iOS gets its own presentation layer.

If business/data logic is safely shareable, keep it shared.

But layout, presentation, interaction, and native iOS behavior should be isolated.

⸻

6. PREFERRED ARCHITECTURE

Separate:

shared logic/state

from

platform-specific presentation

Conceptually:

shared create/media state

then:

Web Create UI

and separately:

iOS Create UI

Do not keep one giant component filled with platform conditionals if a clean separation is possible.

⸻

7. REMOVE DEAD IOS-SPECIFIC OLD CODE

If the old iOS Create Image UI has custom code that will no longer be used after the reset, remove it cleanly.

But do NOT blindly delete shared code.

Before deleting anything, verify whether it is used by:

* Web;
* other iOS modes;
* generation logic;
* shared composer;
* attachments.

Delete only code that is genuinely obsolete for the iOS Create screen.

⸻

8. DO NOT IMPLEMENT THE NEW STUDIO YET

Do NOT start designing:

* Image generation controls;
* Video generation controls;
* History content;
* prompt systems;
* media library;
* generation settings.

We will design those separately after this cleanup.

This task is only:

clean reset + platform separation + three top-level options.

⸻

9. FINAL IOS RESULT

When I open this area on iOS, I should see a clean screen containing only:

Create Video

Create Image

History

No legacy Create Image UI underneath.

No old cards.

No old composer.

No old generated examples.

⸻

10. FINAL REPORT

After completing the cleanup, report:

1. Which old iOS Create components were removed.
2. Which code had been shared with Web.
3. How Web and iOS Create presentation are now separated.
4. Which shared logic remains shared.
5. Which files/components were changed.
6. Confirmation that the website was not visually or behaviorally modified.
7. Confirmation that iOS now shows only Create Video / Create Image / History.

Do not start building the new Create Image or Create Video experience until I give the next instruction.
