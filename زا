Adjust ONLY the maximum upward expansion height of the draggable iOS chat composer.

The drag-to-resize behavior is already working correctly, especially when dragging downward. Do not redesign or rewrite the resize system.

The current problem is that when I drag the composer upward, its maximum height stops too early.

Desired behavior

When the user grabs the top-center resize handle and drags upward, allow the composer to continue expanding much higher than it currently does.

In the attached screenshot, there is an AI Plans carousel card above the composer.

I want the expanded composer to be able to rise approximately:

* to around the middle of the visible AI Plans card, or
* slightly beyond its middle when there is enough available screen space.

In other words, the AI Plans/carousel area is allowed to become partially covered by the expanded composer.

Do NOT treat the carousel/card as a collision boundary.

The composer is an interactive foreground surface while manually expanded, so it may overlay the content behind/above it.

Maximum-height calculation

Do not solve this by adding an arbitrary fixed height that only works on my current iPhone.

Inspect the current maxHeight calculation and determine exactly what is stopping the composer from expanding further.

The maximum expanded height should be calculated primarily from:

* current viewport height;
* keyboard height;
* bottom safe area;
* top safe area;
* a small intentional minimum clearance from the actual top navigation/status area.

It should NOT use the AI Plans carousel/card as the upper limit.

The composer should be able to overlay the carousel while remaining below the critical top navigation area.

Important

Keep:

* the current minimum height;
* current auto-grow behavior;
* current downward dragging;
* current handle;
* keyboard behavior;
* internal text scrolling;
* Plus button;
* microphone;
* Send button;
* attachments;
* all current composer styling.

Only increase/fix the maximum upward expansion range.

The movement must remain continuous with the user’s finger. Do not make it suddenly jump to the larger height.

When the user releases the handle at a large height, keep the composer at that selected height. When they drag downward again, it should collapse exactly as it does now.

Layering

While expanded, make sure the composer correctly appears above the AI Plans carousel rather than pushing the carousel upward or causing the entire page layout to reflow.

The conversation/background content should remain behind it.

Check z-index, positioning, container clipping, and parent overflow rules if necessary.

Do not change those globally if they are shared with the website.

Web protection

This change is for iOS only.

If the current maximum-height calculation comes from shared Web/iOS code, do not modify the website behavior. Add an iOS-specific calculation/override.

Do not modify or break the web composer.

After implementation

Tell me:

1. What was previously limiting the maximum height.
2. What maximum-height calculation you changed for iOS.
3. How the new maximum adapts to different iPhone/keyboard sizes.
4. Confirmation that the composer can now overlay approximately half or slightly more of the AI Plans card.
5. Confirmation that downward resizing still behaves exactly as before.
6. Confirmation that no web behavior was changed.
