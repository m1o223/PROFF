I need you to correct the current ChatPop Menu implementation.

There are TWO important changes.

==================================================
1. MENU DIRECTION AND FULL-SCREEN BEHAVIOR
==================================================

The current Menu opens from the wrong side and behaves like a partial drawer.

Change it.

When I tap the Menu button:

- The Menu must ENTER FROM THE LEFT edge of the iPhone.
- It must animate from LEFT → RIGHT.
- It must expand/slide until it covers the ENTIRE screen.
- When fully open, NO part of the Chat screen should remain visible on the side.
- Treat it as a full-screen iOS navigation layer, not a narrow sidebar/drawer.

When closing:

- Animate it naturally back toward the LEFT.
- Return to the existing Chat screen underneath.

Respect the iPhone Safe Areas and Dynamic Island.

Keep the content currently inside the Menu:

- Chats title
- Close / Back control
- Settings
- Search chats
- New Chat
- Recent chats

Do not add additional sections.

==================================================
2. REMOVE THE CURRENT CUSTOM / FAKE GLASS
==================================================

I do NOT want custom CSS that merely imitates Apple's glass appearance.

Remove the custom-made glass styling currently used for the relevant controls where it is pretending to be native iOS glass.

I want us to use the REAL native iOS glass/material behavior available to the iOS version of this project wherever technically possible.

IMPORTANT:

Do not invent your own fake Liquid Glass implementation.

Do not create arbitrary:
- CSS gradients pretending to be glass
- excessive box shadows
- manually simulated reflections
- fake glow effects
- custom blur combinations intended to imitate native iOS

Prefer Apple's actual native iOS materials/components/effects where they can be used from the existing Capacitor application.

If implementing REAL native iOS Liquid Glass requires a native Swift/SwiftUI component, Capacitor plugin/bridge, or another change to the native iOS layer, DO NOT implement that automatically.

STOP first and tell me:

1. What native iOS API/component you recommend.
2. Which files would need to change.
3. Whether Swift or SwiftUI code is required.
4. Whether it can safely coexist with the existing React + Capacitor UI.
5. Whether it requires a specific iOS/Xcode version.

I will approve the native change before you make it.

Do NOT silently fall back to fake CSS glass.

==================================================
3. IMPORTANT — DO NOT DAMAGE THE EXISTING iOS APP
==================================================

The existing ChatPop iOS project already works on my real iPhone.

DO NOT:

- Delete /ios
- Recreate the iOS platform
- Run npx cap add ios
- Create a new Xcode project
- Change Bundle Identifier
- Change Signing
- Change Development Team
- Change provisioning
- Reset Capacitor
- Replace the existing native project

Preserve the existing iOS application.

==================================================
4. FIRST ACTION
==================================================

First fix the Menu direction and make it full-screen.

For the native iOS glass request, inspect the current architecture and tell me the safest way to use REAL Apple-native glass/material UI instead of the current custom imitation.

Do NOT make native architecture changes until I approve them.

Then STOP and report back.
