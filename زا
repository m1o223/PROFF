FIRST UI CHANGE ONLY — BlueMind Studio top tabs

Do not change anything else on the Studio screen yet.

I want to replace the current custom:

Create Image | Create Video | History

selector with a polished native iOS-style segmented control.

Current problem

The current selector looks like three custom web buttons inside a large outlined container.

It does not feel native to iOS.

Desired behavior

Keep exactly these three sections:

* Create Image
* Create Video
* History

But make switching between them feel like a real modern iOS segmented selector.

The selected segment should have one rounded selection pill/background.

When switching:

Create Image → Create Video → History

the selected pill should slide smoothly horizontally to the newly selected segment rather than disappearing and reappearing.

I want a subtle native spring/fluid animation — quick and controlled, not bouncy or exaggerated.

The content below should switch together with the selected state.

Interaction

Support normal tap selection.

Also investigate whether a native-feeling horizontal swipe/drag between the three Studio sections can be implemented cleanly:

swipe left/right → move to adjacent Studio section.

If implemented, the segmented indicator must remain synchronized with the page.

Do not add a fragile custom gesture that conflicts with vertical scrolling.

Visual design

Use actual native iOS components/material behavior wherever appropriate rather than reproducing a Web/CSS tab bar.

Requirements:

* compact height;
* rounded iOS geometry;
* subtle native material/glass background;
* no heavy rectangular outline;
* equal-width segments;
* perfectly centered labels;
* selected label clearly visible;
* unselected labels secondary but readable;
* appropriate iOS touch targets.

If an accent is needed, use ONLY the existing BlueMind primary token:

BLUE_MIND_COLORS.primary

current value #193B68

Do not introduce another blue.

Important architecture requirement

This is the iOS Studio presentation only.

Do not modify the Web tabs.

If the current selector comes from a shared Web component, separate the iOS presentation instead of changing the shared Web styling.

Do NOT change yet

Do not touch:

* Studio header
* back button
* Create Image content
* Create Video content
* History content
* prompt input
* Customize
* Generate
* setting chips
* backend

This task is ONLY the three-section selector.

Test on the real iPhone and verify:

1. Image / Video / History switch correctly.
2. Selection indicator slides smoothly.
3. No visual jump when switching.
4. Swipe works correctly if implemented.
5. Vertical scrolling is unaffected.
6. Web is unchanged.

Do not continue to another Studio redesign after finishing this. Report the result and stop.
