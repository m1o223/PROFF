TASK: Restyle the Reminders screen using the SAME native iOS glass language already used elsewhere in BlueMind.

IMPORTANT:
Do NOT redesign the page structure.
Do NOT change reminder logic.
Do NOT change reminder data.
Do NOT change navigation behavior.

This task is visual + native iOS presentation only.

====================================================
1. BACK BUTTON + ADD BUTTON
====================================================

The current Back button and + Add button are functionally correct.

Keep:
- same position
- same size
- same icons
- same actions

But apply the same real iOS glass treatment used in the rest of the app.

They should look like native iOS glass controls:
- translucent
- soft blur
- subtle border/highlight
- rounded
- dark glass in dark mode
- no flat custom gray circle

Do not redesign the icons.

====================================================
2. REMINDER NOTIFICATIONS CARD
====================================================

The large "Reminder Notifications" card at the top must become glass.

Keep the existing content:
- icon
- title
- subtitle
- action buttons

But remove the heavy flat blue card appearance.

Replace it with a dark iOS glass card:
- strong blur
- subtle transparency
- soft outline
- rounded corners
- iOS-style depth

The user may still recognize the accent color,
but the entire card must not look like a solid blue rectangle.

====================================================
3. BUTTONS INSIDE REMINDER NOTIFICATIONS
====================================================

The current buttons:
- Enable
- refresh
- send icon

must also use glass styling.

No flat solid blocks.

Use the same shared iOS glass button component already used elsewhere.

Keep functionality unchanged.

====================================================
4. SEARCH BAR
====================================================

"Search reminders" must use the same iOS glass input style already used for other text inputs.

Requirements:
- rounded pill shape
- dark glass
- subtle blur
- soft border
- stable geometry
- no layout jump when focused
- no scaling when tapped
- no button-like press animation

The input itself must remain fully editable.

====================================================
5. REMINDER CARDS
====================================================

Every reminder card in the list must use the same glass card treatment.

Current reminder data must remain unchanged.

Each card should keep:
- completion icon
- title
- description
- date
- time
- status
- three-dot menu

But the card surface should become:
- translucent dark glass
- soft blur
- subtle border
- rounded iOS-style corners
- consistent spacing

Do not make them solid black rectangles.

====================================================
6. THREE-DOT BUTTON
====================================================

The circular three-dot button on each reminder card must also use the native-style glass button treatment.

Keep:
- same icon
- same location
- same action

Only replace the current custom circle surface with the shared iOS glass control.

====================================================
7. THREE-DOT MENU — USE REAL iOS MENU
====================================================

This is important.

When the user taps the three-dot button,
do NOT show our custom HTML / React popup menu.

Use the real native iOS menu / context menu system.

The actions should be:

- Edit
- Delete

Delete must use the native destructive red style.

The menu must:
- be rendered by iOS
- use native spacing
- use native blur/material
- use native press behavior
- use native typography
- dismiss like a real iOS menu

Do NOT imitate Apple with CSS.

====================================================
8. EDIT / DELETE FLOW
====================================================

When user selects Edit:
- continue using the existing edit logic
- but the action menu itself must be native iOS

When user selects Delete:
- use the native iOS confirmation system already used in other parts of the app
- destructive action must be native red
- do not use a custom web modal

====================================================
9. KEEP CURRENT PAGE STRUCTURE
====================================================

Do not move major sections.

Keep this structure:

Header
→ Reminder Notifications
→ Search
→ Reminder Cards

This task is styling + native iOS controls only.

====================================================
10. CONSISTENCY
====================================================

Reuse the SAME shared iOS glass/native components already used in:

- Settings
- Chat
- native close/back controls
- native confirmation menus

Do not create another separate fake-glass implementation just for Reminders.

====================================================
11. REAL IPHONE TEST
====================================================

Test on the connected real iPhone.

Verify:

1. Back button is glass.
2. + button is glass.
3. Reminder Notifications card is glass.
4. Its internal buttons are glass.
5. Search input is glass.
6. Every reminder card is glass.
7. Three-dot button is glass.
8. Three-dot menu is real native iOS.
9. Delete uses native destructive behavior.
10. Inputs do not jump or scale.
11. No Android/Web behavior is changed.

When finished, report:
- which shared components were reused
- which custom web elements were removed
- which actions now use native iOS UI
- real iPhone test result
