The authentication flow still has a fundamental UI consistency problem.

Do NOT patch this.

Investigate the entire authentication component architecture first.

========================================================
FIRST - INVESTIGATE
========================================================

Audit every authentication screen:

• Login
• Create Account
• Forgot Password
• Reset Password
• Any reusable authentication input component

Inspect:

- Shared Input component
- Focus state
- Active state
- Material
- Colors
- Border
- Background
- Loading animation
- Existing reusable components

If old implementations are conflicting:

Remove the obsolete implementation.

Replace it with one clean reusable authentication component.

Do NOT stack fixes.

========================================================
INPUT DESIGN
========================================================

The authentication inputs must use ONE visual style.

Exactly one.

The visual appearance must NOT change when focused.

Currently the input changes its material/background during focus.

This is NOT the desired behavior.

The input should always keep the same premium material.

Think of the chat composer.

The authentication inputs should have the same premium glass/material feeling.

Only the cursor, caret and focus state should change.

NOT the entire input background.

The user should feel continuity.

========================================================
INPUT COMPONENT
========================================================

Create ONE reusable authentication input.

Use it everywhere:

• Login
• Create Account
• Forgot Password
• Future authentication screens

Do NOT duplicate implementations.

========================================================
LOADING ANIMATION
========================================================

The current loading indicator is incorrect.

It currently uses a logo with the letter "B".

This is NOT the official BlueMind loading mark.

Use ONLY the original BlueMind symbol.

No "B".

No alternative logo.

The loading animation should use the authentic BlueMind mark.

========================================================
LOADING MOTION
========================================================

The loading animation currently feels static.

It must clearly communicate that work is happening.

Use a premium subtle animation.

Smooth.

Continuous.

Elegant.

The movement must be visible.

Do NOT use an aggressive spinner.

Do NOT make it look generic.

It should feel like a first-party BlueMind loading animation.

========================================================
CONSISTENCY
========================================================

Login and Create Account must behave identically.

Same:

• Input style
• Material
• Focus behavior
• Icons
• Animations
• Loading behavior

Everything should feel like one unified authentication system.

========================================================
VERIFY
========================================================

Test:

• Login
• Create Account
• AutoFill
• Password manager
• Keyboard
• Focus
• Blur
• Loading
• Success
• Failure

Confirm that all authentication screens now share the exact same reusable components.

========================================================
FINAL REPORT
========================================================

Explain:

• Which old authentication code was removed.
• Which components were rebuilt.
• How the reusable input system now works.
• Why the loading animation is now based on the real BlueMind logo.
• Confirm Android and Web were not modified.
