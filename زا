NEXT IOS-ONLY STUDIO FIX — make the top header/selector float over the gallery with real iOS glass behavior

We are working only on the iOS Studio presentation.

Do NOT modify Web.

Do NOT redesign the gallery, composer, or generation logic.

⸻

Current problem

The top of BlueMind Studio still feels like a solid black header sitting above the gallery.

Right now the structure visually feels like:

black header

→ BlueMind Studio title

→ Create Image / Create Video / History

→ gallery starts below

I do NOT want that.

⸻

Desired behavior

I want the gallery/content to extend underneath the top controls.

The top controls should feel like floating native iOS glass elements over the content, similar to how the bottom composer already floats over the gallery.

Think:

floating back button

floating BlueMind Studio title

floating segmented control

all above the scrolling gallery.

The gallery should continue behind them.

⸻

Back button

Keep the current back button position and size, but make sure it is a true floating iOS glass/material control.

The gallery should be able to scroll behind it.

Do not place it inside a solid black header container.

⸻

BlueMind Studio title

Keep:

BlueMind Studio

centered.

Do not give the title area a large opaque black background.

It should visually float above the content.

If needed, use a subtle native blur/material behind the title area only enough to preserve readability.

Do not create a full-width opaque navbar.

⸻

Create Image / Create Video / History control

Keep the current three-section selector.

But its container should use a native translucent iOS material/glass treatment.

Requirements:

* translucent
* subtle blur
* rounded
* content colors should softly show through from behind
* no heavy opaque dark slab
* selected BlueMind pill remains visible

Use the existing official BlueMind primary color:

BLUE_MIND_COLORS.primary

#193B68

only for the selected segment.

⸻

Scroll behavior

This is important.

The gallery should scroll under the floating header controls.

When the user scrolls:

* Anime/Nature/etc. images should move behind the back button/title/segment control
* the floating controls remain above the content
* the glass material should blur/tint the image content behind it

Do not push the gallery permanently below a large header block.

⸻

Layout structure

Prefer a structure conceptually like:

ZStack / overlay

background:

* scrollable gallery/content

foreground:

* top floating controls
* bottom floating composer

The top controls should not consume a large permanent opaque vertical area.

⸻

Safe area

Respect the iPhone top safe area and Dynamic Island/status bar.

Do not let the controls overlap the status bar incorrectly.

Use proper top insets.

⸻

Important visual reference

The bottom composer is already much closer to the desired feeling:

floating glass over the images

I want the top area to follow the same visual philosophy.

Top and bottom should feel like one coherent iOS design system:

floating glass controls

with the gallery/content flowing underneath.

⸻

Do NOT change

Do not modify:

* image assets
* image order
* gallery card sizes
* composer behavior
* Customize
* Create Video content
* History content
* backend
* Web

⸻

Test on real iPhone

1. Open BlueMind Studio → Create Image.
2. Confirm gallery visually extends behind the top controls.
3. Scroll upward/downward.
4. Confirm images move behind the back button/title/section selector.
5. Confirm the top controls remain readable.
6. Confirm the glass/translucency reacts naturally to content behind it.
7. Confirm no large opaque black header remains.
8. Confirm Dynamic Island/safe area remains correct.
9. Confirm Web is unchanged.

After fixing, report:

* which top container was causing the opaque header effect
* how the gallery was moved underneath it
* which native iOS material/glass component is used
* confirmation that only iOS was changed
