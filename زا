Implement the final voice-entry UI for ChatPop.

IMPORTANT:
Do not redesign the whole app.
Keep the existing dark ChatPop style.
Use an iOS-native feel and smooth transitions.

What I want:
When the user taps the voice/send orb in the input area while there is NO typed text, open a dedicated voice screen/modal.

Behavior:
1. If the input is empty and the user taps the voice orb/button:
   - open the voice interface
   - do NOT send anything
2. If there is typed text:
   - keep normal send behavior

Voice UI requirements:
- Open a full-screen or large modal voice interface
- Background should be dark / black
- The main focus should be a centered circular voice orb
- The orb should be:
  - white or very light in the center
  - with blue / light-blue waveform lines or animated audio lines
  - elegant, clean, premium-looking
- Make it feel modern and polished

Controls:
- Add a visible X button to close and return to chat
- Add a microphone button so the user can mute/stop speaking if they do not want to talk
- The close button and mic button should be clear and easy to tap
- Keep the interface minimal, not crowded

Design direction:
- Use iOS-style visuals
- Smooth animation when opening/closing
- Use the current ChatPop dark theme
- Blue accents only (not random colors)
- Make it feel premium and simple

Expected interaction:
- Tap voice button -> voice screen opens
- User sees dark background + centered white/blue voice orb
- User can close with X
- User can control speaking with the mic button

Implementation notes:
- Keep the current chat screen structure
- Only add the voice modal/screen and the correct trigger logic
- Do not break existing chat UI
- Do not change unrelated parts of the app

Please implement the UI cleanly and show me the final result.
