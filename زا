We are now implementing the final ChatPop app intro animation.

IMPORTANT:
I will attach a sequence of still images/keyframes that represent the exact visual stages of the ChatPop logo animation.

Your job is to use those images as KEYFRAMES and create ONE smooth, continuous, native iOS intro animation from them.

DO NOT treat the images as a slideshow.
DO NOT simply fade from one static image to another.
DO NOT create separate screens for each image.

The final result must feel like one real continuous logo animation.

==================================================
1. SOURCE MATERIAL
==================================================

I will provide multiple images in a specific order.

Treat them as the visual timeline of the animation.

For example:

Image 1 = very early appearance
Image 2 = logo more visible
Image 3 = logo fully visible
Image 4 = brand name beginning to appear
Image 5 = full CHATPOP brand visible
Image 6 = brand beginning to close
Image 7 = text mostly gone / logo remaining
Image 8 = final disappearing state

Use the exact order I provide.

Do NOT reorder the images.

Do NOT redesign the logo.

Do NOT reinterpret the P symbol.

Do NOT change the star shape, orbital ring, proportions, or white/blue visual identity.

==================================================
2. FINAL ANIMATION DURATION
==================================================

The entire animation must be SHORT.

Target total duration:

3.0 to 4.0 seconds maximum.

Do NOT make it longer than 4 seconds.

This is an app-opening intro, so it must feel fast and premium, not cinematic and slow.

Suggested duration target:
approximately 3.5 seconds.

==================================================
3. BACKGROUND
==================================================

The background must be:

PURE BLACK

Use:
#000000

Requirements:
- no stars
- no particles
- no dots
- no gradients
- no space texture
- no background glow
- no floor
- no surface
- no reflections
- no smoke
- no environment
- no decorative elements

The only visible elements should be:
- ChatPop logo
- CHATPOP text
- subtle logo-related glow if needed

The background must visually match the main ChatPop app black background.

==================================================
4. DO NOT SHOW APP UI DURING INTRO
==================================================

During the intro animation, do NOT show:

- Settings icon
- Menu button
- Model selector
- Chat input
- Search
- Creative Studio card
- Any other ChatPop app UI
- Status overlay created by our app

The intro should be visually isolated.

Only the native iOS system status bar may exist if unavoidable, but ideally present the intro in a way that feels clean.

==================================================
5. KEYFRAME INTERPOLATION
==================================================

This is extremely important.

Use the attached images as visual reference states, but interpolate smoothly between them.

Do NOT simply swap images.

Use native animation properties such as:

- translation / position
- scale
- opacity
- subtle blur
- masking
- timing curves
- spring/ease where appropriate

The viewer should NOT be able to tell that the source came from separate still images.

It should feel like a real generated animation.

==================================================
6. LOGO MOTION
==================================================

The P logo begins partially off-screen / appearing from the left side according to the first keyframes.

Motion direction:

LEFT -> CENTER

The logo should move smoothly into its final centered position.

Use:
- smooth acceleration/deceleration
- premium easing
- no sudden jump

Do NOT:
- rotate the logo
- flip it
- morph it
- distort it
- stretch it

The exact P shape must remain visually consistent.

==================================================
7. LOGO SETTLE / BOUNCE
==================================================

When the P reaches its main position:

Add a VERY SMALL premium settle motion.

Example:
scale 1.00 -> 1.03 -> 0.99 -> 1.00

This should last only a fraction of a second.

It must feel like:
"the logo landed cleanly"

Do NOT make it cartoonish.

Do NOT use a large bounce.

==================================================
8. STAR INSIDE THE P
==================================================

The star inside the P may briefly become brighter.

This should be subtle.

Use:
- white
- very slight blue edge light if needed

Do NOT add:
- particle burst
- lens flare explosion
- large glow sphere
- extra star elements

The star shape must remain exactly consistent with the provided logo.

==================================================
9. BLUE ORBIT / RING
==================================================

The blue orbital ring belongs to the logo identity.

Keep it.

But:
- do not create a blue background
- do not flood the screen with blue light
- do not create a large blue aura

The blue orbit itself may glow subtly.

The background must still remain pure black.

==================================================
10. CHATPOP TEXT REVEAL
==================================================

When the brand name appears:

Use:

CHATPOP

ALL CAPS.

Do NOT use:
ChatPop
chatpop
Chatpop

Use exactly:
CHATPOP

The text must be:
- large
- strong
- bold
- white
- clean
- modern
- high contrast

Use a clean sans-serif style compatible with the existing iOS visual language.

Avoid futuristic gimmick fonts.

==================================================
11. CHATPOP TEXT SIZE
==================================================

The current brand text needs to feel larger and stronger.

Make CHATPOP visually prominent.

It should take a meaningful amount of horizontal space.

The composition should feel like:

[P LOGO] CHATPOP

Do not make the word tiny beside a huge P.

Balance the logo and wordmark so both feel intentional.

==================================================
12. TEXT REVEAL DIRECTION
==================================================

The CHATPOP text should feel like it emerges FROM the P logo.

Preferred direction:

P -> C -> H -> A -> T -> P -> O -> P

The text may reveal from left to right.

Possible methods:
- mask reveal
- horizontal clip reveal
- slight translation
- opacity increase

Do NOT animate every character with a big separate bounce.

Keep the reveal cohesive and premium.

==================================================
13. TEXT MOTION BLUR
==================================================

If motion blur is used during the text reveal:

The blur must be NEUTRAL.

Do NOT tint the blur blue.

Do NOT tint it purple.

Do NOT tint it any color.

The blur should look like clean optical motion blur only.

==================================================
14. HOLD FRAME
==================================================

Once the full identity is visible:

[P LOGO] CHATPOP

Hold it only briefly.

Approximately:
0.4 to 0.7 seconds.

The user should have enough time to recognize the brand.

Do NOT hold it for several seconds.

==================================================
15. EXIT / CLOSING SEQUENCE
==================================================

After the short hold:

Begin the closing animation using the final keyframe images I provide.

The closing animation should feel like the natural reverse/continuation of the intro.

Possible sequence:

CHATPOP starts retracting/disappearing
↓
word becomes shorter / disappears toward the logo
↓
P remains briefly
↓
P moves/scales/fades according to the provided closing keyframes
↓
pure black

Do NOT abruptly remove the entire image.

==================================================
16. CLOSING DIRECTION
==================================================

Use the direction shown in the closing keyframes.

If the closing images show the logo moving back toward the left:

follow that exactly.

Do not invent another exit direction.

==================================================
17. END OF INTRO
==================================================

At the end:

The screen should be pure black for only a tiny transition moment.

Then immediately reveal the main ChatPop application.

No white flash.

No gray flash.

No visible loading screen between the intro and app.

No frame jump.

==================================================
18. TRANSITION INTO APP
==================================================

The transition from intro -> app should feel continuous.

Preferred:
- fade intro layer away
- reveal already-loaded app underneath

Avoid:
- destroy intro
- show black frame for a long time
- then load app

The app should already be prepared behind the intro if practical.

==================================================
19. NATIVE IOS IMPLEMENTATION
==================================================

Build the intro natively inside the EXISTING ChatPop iOS project.

Use the most suitable native iOS animation technique.

Possible acceptable approaches:
- UIViewPropertyAnimator
- Core Animation / CALayer
- CAKeyframeAnimation
- SwiftUI animation if compatible with existing architecture

Choose the cleanest solution for the current project.

Do NOT add a video player just to play the intro if native animation can reproduce it better.

==================================================
20. IMAGE ASSET HANDLING
==================================================

Import the attached keyframe images into the existing iOS asset structure cleanly.

Use production filenames.

Do NOT:
- reference temporary local absolute paths
- load from Downloads/Desktop at runtime
- rely on external network requests

The intro must work offline.

==================================================
21. IMAGE CONSISTENCY
==================================================

Because the frames come from generated images, inspect them for consistency.

If there are tiny differences between:
- logo position
- logo scale
- orbit size
- text alignment

Do NOT simply display the differences as flicker.

Normalize the animation so the final result appears stable.

The logo must not visibly "change shape" between frames.

Use the frames as guides, not literal frame-by-frame video playback.

==================================================
22. NO SLIDESHOW LOOK
==================================================

The result must NOT look like:

Image 1
fade
Image 2
fade
Image 3

That is NOT acceptable.

It must look like:

one object continuously moving and revealing.

==================================================
23. TIMING GUIDE
==================================================

Use approximately this timing:

0.00 – 0.20 sec
Pure black

0.20 – 0.95 sec
P logo enters from left toward center

0.95 – 1.20 sec
P settles with subtle bounce

1.20 – 2.10 sec
CHATPOP reveals from the P

2.10 – 2.65 sec
Full brand hold

2.65 – 3.40 sec
Closing/retraction animation

3.40 – 3.70 sec
Transition directly into app

This is a guide.
Adjust slightly if necessary, but keep total duration under 4 seconds.

==================================================
24. SPEED
==================================================

The previous motion felt too fast between visual states.

Make transitions between keyframes slightly slower and smoother.

However:
do NOT extend the total intro beyond 4 seconds.

The goal is:

slower-feeling interpolation
+
short overall sequence

Achieve this by removing unnecessary pauses.

==================================================
25. NO COLORED BLUR
==================================================

This is a strict requirement:

Any blur used around moving elements must NOT have blue/purple color tint.

Blur must remain neutral.

The logo's own blue orbit is allowed.

The background and blur layer are not blue.

==================================================
26. COLOR RULES
==================================================

Allowed:
- #000000 black background
- white logo
- white CHATPOP text
- original blue orbit/glow from logo

Not allowed:
- purple atmosphere
- pink
- green
- blue background gradient
- colored fog
- colored blur

==================================================
27. ACCESSIBILITY / REDUCE MOTION
==================================================

Respect iOS Reduce Motion if practical.

If Reduce Motion is enabled:
use a simpler short fade/scale reveal.

Do not disable the intro completely unless necessary.

==================================================
28. PERFORMANCE
==================================================

The intro must run smoothly on a real iPhone.

Target:
60fps-feeling motion.

Avoid:
- giant uncompressed assets
- excessive image decoding during animation
- heavy runtime effects
- unnecessary GPU-heavy blur

Preload assets before starting animation.

==================================================
29. APP STARTUP
==================================================

Do NOT allow the intro to delay app startup unnecessarily.

If app initialization finishes early:
complete the short intro normally.

If app initialization takes longer:
do not freeze the animation.

Handle startup cleanly.

==================================================
30. RUN ONLY ON APP LAUNCH
==================================================

The intro should appear at a sensible time:

- cold app launch

Do not replay the entire intro every time the user:
- opens Settings
- opens Menu
- starts a new chat
- returns from another screen

It is the app launch brand intro.

==================================================
31. DO NOT TOUCH UNRELATED FEATURES
==================================================

Do NOT modify:

- Chat UI
- Creative Studio
- Settings
- Menu
- Authentication
- Voice
- Backend
- subscriptions
- database
- model selector
- iOS signing
- Bundle Identifier
- Development Team

Only implement the startup intro animation.

==================================================
32. EXISTING IOS PROJECT
==================================================

Use the existing working ChatPop iOS project.

DO NOT:
- delete /ios
- recreate the iOS project
- run npx cap add ios again
- create another Xcode project

Preserve the current real-iPhone build configuration.

==================================================
33. REAL IPHONE VERIFICATION
==================================================

Test on the existing real iPhone.

Verify:

1. App launches.
2. Pure black screen appears.
3. P enters smoothly from left.
4. P shape does not flicker or morph.
5. Star remains stable.
6. Blue orbit remains stable.
7. CHATPOP appears in ALL CAPS.
8. CHATPOP is large and readable.
9. Motion is smooth.
10. No Settings icon appears.
11. No other app UI appears during intro.
12. No colored blur appears.
13. No particles appear.
14. Closing animation is smooth.
15. Main app appears immediately after intro.
16. No white flash.
17. No black-frame delay.
18. Total duration is between 3 and 4 seconds.

==================================================
34. IMPORTANT BEFORE YOU START
==================================================

First inspect all attached keyframe images.

Then briefly identify:

- their intended order
- which ones belong to opening
- which ones belong to full brand reveal
- which ones belong to closing

Then implement the animation.

Do not invent a different sequence if the supplied keyframes already define it.

==================================================
35. FINAL REPORT
==================================================

When finished, STOP.

Report:

1. Exact keyframe order used.
2. Total animation duration.
3. Native animation technique used.
4. Files added.
5. Files changed.
6. How the image assets were normalized.
7. How the CHATPOP reveal is implemented.
8. How the closing sequence is implemented.
9. Whether Reduce Motion is supported.
10. Whether it was successfully tested on the real iPhone.
11. Whether any animation warning/error remains.

Do not continue with any other feature after this task.
