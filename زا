Critical iOS bug investigation — black screen after successful Face ID

I recorded the exact issue on the real iPhone.

Current behavior:

1. Face ID authentication appears.
2. Authentication succeeds.
3. The security/settings view closes.
4. Instead of returning to the previous BlueMind screen, the app becomes completely black.

Face ID itself appears to succeed.
The bug happens after successful authentication / dismissal.

Do NOT guess the cause.

Trace the full runtime flow after Face ID success:

* authentication callback
* state updates
* overlay/sheet dismissal
* navigation stack
* root view visibility
* any loading/locked state
* any opacity/hidden flags
* any modal presentation state
* any WebView/native bridge interaction

Specifically inspect whether:

* the authenticated state is updated but the previous view is still hidden;
* a full-screen black overlay is not being dismissed;
* the app root/container is being replaced with an empty view;
* the security sheet dismissal removes the wrong parent/root view;
* an async state race occurs between unlock success and navigation;
* a native Face ID overlay is dismissed while the underlying React/iOS view remains in a locked/hidden state;
* the app is routing to a blank screen after success.

Add temporary debug logging around each step:

Face ID started

Face ID success

authenticated state updated

security modal dismissed

navigation restored

main/root UI visible

Test on the real iPhone and identify the exact step where the UI disappears.

Do not mark this fixed until:

* Face ID succeeds
* the security/settings screen closes
* the exact previous BlueMind screen becomes visible immediately
* no black screen appears

Do not modify unrelated authentication/backend logic.

After fixing, report the exact root cause and which state/view/navigation handler was corrected.
