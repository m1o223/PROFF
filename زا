FIX ONLY THE SEARCH CONVERSATIONS SCREEN.

The current bug is that the Chat composer UI is still visible while I am in the Search Conversations screen.

This screen is NOT an active chat.

Therefore, while Search Conversations is open, hide/remove from this screen:

* Ask anything... composer
* attachment/plus button
* microphone button
* send button
* secondary arrow/checkmark toolbar
* any other controls that belong to an active chat composer

Expected Search screen layout:

1. Header / Work AI controls
2. Search conversations... search field
3. Search results / recent chats
4. iOS keyboard when the search field is focused

Nothing else should float above the keyboard.

The keyboard must belong only to the Search conversations... field on this screen.

Do NOT change the actual Chat screen composer.
Do NOT remove the composer globally.
Only hide it when the current route/state is Search Conversations.

When the user leaves Search and opens an actual conversation, restore the normal chat composer.

Also keep the existing Apple/iOS glass styling for the Search page buttons.

Test on the real iPhone:

* Open Search Conversations.
* Tap the search field.
* Keyboard opens.
* Confirm only the search UI + results are visible.
* Confirm no Ask anything..., mic, send, arrows, or checkmark bar appears.
* Open a real chat afterward and confirm the normal composer returns there.

هاي هي المشكلة بالضبط
