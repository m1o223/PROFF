TASK: Fully fix the Projects feature end-to-end.

CURRENT PROBLEM:
The iOS UI for Projects is now working and visible on the real iPhone, but the backend routes are not available in production.

Observed errors:
- GET /api/projects -> Route not found
- POST /api/projects -> Route not found

This means the frontend is reaching the backend correctly, but the Projects API is either:
1. not implemented,
2. not mounted under /api/projects,
3. not deployed to the production backend,
4. or the mobile app is pointing to an old backend deployment.

GOAL:
Make Projects fully functional end-to-end:
- create
- list
- open
- rename/update
- delete
- persist in database
- enforce authentication and user ownership
- return clean errors
- work from the real iPhone against the production backend

IMPORTANT:
Do NOT redesign the Projects UI.
Do NOT change unrelated screens.
Do NOT touch Face ID, Schedule, AI Plans, Chat, Settings, or authentication unless required only to correctly secure Projects.
Keep the current Projects frontend design as-is.

1. VERIFY BACKEND ROUTE REGISTRATION

First inspect the production backend structure and verify that the Projects router is actually mounted.

Expected base route:
/api/projects

Verify the main API/router index includes the Projects routes.

Do not assume the files existing means the routes are active.

Confirm that production supports:

GET /api/projects
POST /api/projects
GET /api/projects/:projectId
PATCH or PUT /api/projects/:projectId
DELETE /api/projects/:projectId

Use the existing backend architecture and conventions.

2. AUTHENTICATION

Every Projects route must require authentication.

Do not accept a userId from the frontend as proof of ownership.

Use the authenticated server-side user/session identity.

Every read/update/delete operation must verify that:
project.userId === authenticatedUser.id

If the project does not belong to the logged-in user:
- do NOT expose whether another user's project exists
- return the existing safe not-found/authorization behavior used elsewhere in BlueMind

3. PROJECT MODEL / DATABASE

Ensure Projects use persistent MongoDB/database records.

Required core fields should include the existing model fields where applicable:
- _id / id
- userId
- name
- description
- createdAt
- updatedAt

If the current model already contains counts or metadata, preserve them:
- chatCount
- fileCount
- taskCount
- noteCount
or equivalent existing fields.

Do not create duplicate competing Project models.

The project name must be validated:
- trim whitespace
- reject empty names
- enforce a reasonable maximum length

Description:
- optional
- trim safely
- enforce a reasonable maximum length

4. CREATE PROJECT

POST /api/projects

Input:
- name
- optional description

Behavior:
- validate input
- use authenticated user identity
- create persistent project
- return created project
- do not create duplicates from accidental repeated submit if the frontend already has request protection
- return proper status codes

The iPhone New Project form shown in the current UI must work with this endpoint.

5. LIST PROJECTS

GET /api/projects

Return only projects owned by the logged-in user.

Sort in a sensible order:
prefer most recently updated/created first, consistent with current BlueMind behavior.

If user has no projects:
return an empty array / valid empty result.

Do NOT return:
"Route not found"
or raw backend text to the UI.

6. GET SINGLE PROJECT

GET /api/projects/:projectId

Verify:
- valid project ID
- authenticated ownership
- not soft-deleted if soft-delete is used

Return the exact requested project only.

7. RENAME / UPDATE

PATCH or PUT /api/projects/:projectId

Support current UI Rename behavior.

At minimum allow:
- name
- description

Validate updates.
Preserve ownership.
Update updatedAt.

Rename must update the exact selected project, not another project with the same display name.

Always use projectId + authenticated user ownership.

8. DELETE PROJECT

DELETE /api/projects/:projectId

Use the existing intended project deletion model.

If the current architecture uses soft delete, preserve that.
If current Projects design explicitly uses hard delete, follow the existing backend convention.

Important:
Deleting one project must never delete:
- another user's project
- another project with the same name
- unrelated chats/files/tasks

Use the unique project ID.

Return a clean success response.

9. FRONTEND / MOBILE SERVICE CHECK

Inspect the Projects frontend service and make sure it calls the correct production API routes.

Expected behavior:
- list -> GET /api/projects
- create -> POST /api/projects
- rename/update -> PATCH/PUT /api/projects/:id
- delete -> DELETE /api/projects/:id

Do not hardcode a different API host.

Verify the mobile app is using the current BlueMind production backend base URL.

10. PRODUCTION DEPLOYMENT

This is critical.

The current iPhone error strongly suggests the new Projects backend is not deployed.

Check:
- current production branch
- Render/backend deploy source
- auto-deploy branch
- current deployed commit
- route registration in production

If the Projects backend changes exist only locally:
commit them to the correct backend branch and deploy through the existing production process.

Do not create a second backend service.

After deployment, verify the real production endpoint.

11. CLEAN ERROR HANDLING

The current UI displays raw text such as:
"Route not found: POST /api/projects"

This must NOT appear to users.

For backend/network failures:
- keep technical details in logs
- show a clean user-facing error state/toast using the existing BlueMind UI

Example:
“Couldn’t create project. Please try again.”

Do not expose:
- route internals
- stack traces
- database errors
- server implementation details

12. TEST THE FULL CRUD FLOW

Test against the actual production backend using an authenticated user.

Test:

A. GET projects with no projects
Expected:
valid empty state, no route error.

B. Create Project
Example:
Name: Test Project
Description: Test description

Expected:
project is created and returned.

C. Reload/reopen app
Expected:
project still exists from MongoDB/database.

D. Rename
Test Project -> Updated Project

Expected:
same project ID, updated name.

E. Update description
Expected:
persisted correctly.

F. Delete
Expected:
project disappears and stays deleted after app reload.

G. Ownership
Verify one user cannot read/update/delete another user's project.

H. Invalid IDs
Return clean safe errors.

I. Duplicate button taps
Make sure repeated taps do not accidentally create multiple identical records due to UI/request race conditions.

13. TEST ON REAL IPHONE

After backend deployment:
- sync/rebuild iOS only if necessary
- open Projects on the connected real iPhone
- verify list loads
- press +
- enter project name and description
- press Create Project
- confirm project appears immediately
- close/reopen Projects
- confirm it persists
- rename it
- delete it
- confirm no raw API error appears anywhere

IMPORTANT:
Do not stop after local validation.
The final success condition is that Projects works from the actual installed iPhone app against the production backend.

14. REPORT BACK

When finished, give me a precise report containing:

1. Root cause of GET /api/projects failure.
2. Root cause of POST /api/projects failure.
3. Backend files changed.
4. Route registration changes.
5. Database/model changes, if any.
6. Authentication/ownership protections.
7. Production deployment status.
8. Production commit/deployment used.
9. CRUD tests performed.
10. Real-iPhone test result.
11. Any remaining placeholder functionality inside Projects.
12. Confirmation that no unrelated UI/features were modified.
