STOP working on Files, Tasks and Notes. We are now building ONE Project section at a time. Start with PROJECT CHATS ONLY.

The current Chats screen is only a placeholder saying:

Chats
This project section is ready for its dedicated content in the next stage.

Delete this placeholder presentation and build the real iOS Project Chats experience.

Do NOT work on Files, Tasks or Notes in this task.

Purpose

Project Chats is NOT another generic Chat page.

It is the conversation workspace belonging to the currently opened Project.

A Project can contain multiple chats.

The page should let the user:

* see all chats belonging to this Project;
* create a new Project chat;
* search chats;
* open an existing chat;
* rename/delete chats where supported;
* return to the Project.

Header

Use native BlueMind iOS design.

Show:

* native/glass back button;
* Project name as contextual information;
* Chats as the page title;
* compact native + / New Chat action.

Do not use a giant navbar or Web-style header.

Chat list

Each conversation row should show:

Conversation title

short preview of the latest message

last updated time/date

Keep rows compact and clean.

Use native iOS separators/material where appropriate rather than giant cards.

Sort newest/recently updated chats first.

Tapping a row opens that conversation.

Search

Add a compact native iOS search field.

Search the Project’s conversations by title and locally available preview text where supported.

Do not build a new backend search system for this task.

New Chat

New Chat must create/open a conversation associated with the CURRENT PROJECT.

Reuse the existing BlueMind Chat experience.

Do not build a second chat interface.

When the user opens a Project chat, use the existing BlueMind Chat UI/composer/messages and preserve the project association.

Inspect the existing data model/API first. If projectId or equivalent project association already exists, use it.

If backend project-chat association does NOT exist yet, do not fake persistence. Build the frontend correctly and report exactly what backend capability is missing.

Empty state

If this Project has no chats, do NOT show the current giant placeholder.

Use a compact native empty state:

No chats yet

Start a conversation for this project.

[New Chat]

The empty state should occupy only the necessary area.

iOS interactions

Where technically appropriate, support:

* swipe left → Delete
* context menu / long press → Rename, Delete

Add Pin only if the current data model already supports it. Do not invent backend fields.

Use confirmation before destructive deletion where appropriate.

Critical architecture rule

A Project Chat must remain distinguishable from a normal standalone chat.

Preserve a project identifier/context in navigation/state so later the backend can provide project-aware AI context from:

* Project Files
* Project Notes
* Project Tasks

Do NOT implement that AI context system now.

We are preparing the frontend/data flow for it.

Visual language

Match the existing BlueMind iOS application:

* black background
* native iOS controls
* Liquid Glass/material where appropriate
* SF Symbols
* BlueMind primary #193B68 only as the established accent
* compact spacing
* proper safe areas
* Arabic RTL support
* Swedish/English LTR support

Do not make this screen look like a Web dashboard.

Global toast

Use the existing global toast system for transient feedback:

Chat created

Chat renamed

Chat deleted

Could not create chat

Do not render large inline success boxes.

Do NOT touch

* Files
* Tasks
* Notes
* Project Overview
* Normal Chat design
* BlueMind Studio
* Web UI
* unrelated backend

Required real-iPhone flow

Test:

Project → Chats

→ empty state if appropriate

→ New Chat

→ existing BlueMind Chat UI opens

→ send a message using existing functionality

→ go back

→ conversation appears in Project Chats if persistence/project association is supported

→ reopen it

→ conversation history remains

Also test search, rename and delete wherever supported.

Do not mark this finished with placeholder UI or dead buttons.

After implementation, stop and report:

1. what Project Chats now supports;
2. what existing chat components/API were reused;
3. how chats are associated with the Project;
4. anything missing from the backend;
5. what was tested successfully on the real iPhone.
