
Update ONLY the ChatPop authentication UI and the Sign In entry inside Settings.

The attached screenshots are the visual/UX reference for the authentication flow.

Use the same general hierarchy, spacing logic, and simple iOS flow shown in the references, but DO NOT use ChatNow branding, logo, text, or purple/pink colors.

Everything must use ChatPop's identity:

- Black / near-black background
- Dark gray surfaces
- Medium ChatPop blue
- White text
- iOS gray secondary text
- Real native iOS material/glass where appropriate
- SF Symbols/native iOS controls where available

==================================================
1. SETTINGS — SIGN IN BUTTON
==================================================

The current "Account / Sign in" row is too small and looks like a normal Settings row.

Change it.

When the user is NOT signed in, show a larger, prominent Sign In button/card inside the Account & Subscription section.

It should say:

Sign in

Optional small secondary text:

Sync your account and subscription

Design:

- Larger than a normal settings row
- Medium ChatPop blue
- Slightly glossy / premium
- Native iOS feeling
- Rounded
- High contrast
- Not pale cyan
- Not purple
- Not pink

Use the existing REAL native iOS glass/material implementation where appropriate.

Do NOT fake the glossy/glass effect using arbitrary CSS gradients and shadows.

==================================================
2. AUTH ENTRY SCREEN
==================================================

When the user taps Sign In, open a dedicated authentication screen.

Follow the attached reference hierarchy closely.

TOP:
- Native Back button
- Native Close X button

CENTER:
- ChatPop logo/mark placeholder
- Title:

"Log in or sign up"

Then show these authentication options vertically:

[ Apple icon ] Continue with Apple

[ Google icon ] Continue with Google

[ Microsoft icon ] Continue with Microsoft

Then:

──────── OR ────────

[ Log in ]

[ Sign up ]

At the bottom:

Checkbox:
"I agree to the Terms of Service and Privacy Policy"

Terms of Service and Privacy Policy should appear as tappable links.

==================================================
3. AUTH SCREEN COLORS
==================================================

The reference screen uses a soft colored upper background.

For ChatPop, replace that with our BLUE identity.

Use a tasteful dark-to-medium-blue atmosphere near the upper part of the screen, transitioning into the dark/black ChatPop background.

IMPORTANT:

Do NOT use:
- Purple
- Pink
- Lavender

Use:
- Black
- Dark navy
- Medium ChatPop blue

Keep it subtle and premium.

The buttons themselves should remain dark iOS-style surfaces so the blue branding does not overwhelm the screen.

==================================================
4. SOCIAL SIGN-IN BUTTONS
==================================================

Keep the visual hierarchy similar to the reference:

Apple
Google
Microsoft

Each should be a large rounded button.

Use the official recognizable provider icons where legally/technically appropriate.

Do NOT recolor the Google or Microsoft brand marks incorrectly.

Apple can use the appropriate Apple sign-in presentation.

IMPORTANT:
These are UI interactions for now.

Do NOT connect OAuth/backend authentication yet unless authentication is already implemented elsewhere in the project.

==================================================
5. LOG IN
==================================================

When the user taps:

Log in

Open a dedicated Login screen.

Use the same ChatPop design language.

Include:

- Back button
- Close X
- ChatPop logo/mark
- Title: "Log in"
- Email input
- Password input
- Show/hide password button
- Forgot password
- Continue button

The Continue button should be disabled until the required fields contain valid-looking input.

Active Continue button:
- Medium ChatPop blue
- White text

Inactive:
- Dark gray
- Gray text

==================================================
6. SIGN UP / CREATE ACCOUNT
==================================================

When the user taps:

Sign up

Open the Create Account screen.

Follow the attached reference structure closely, but use ChatPop branding.

Include:

- Back button
- Close X
- ChatPop logo/mark

Title:

"Create an account"

Subtitle:

"Set your email and password to continue."

Inputs:

Email
Password

Password field:
- Native eye/show-hide control

Bottom:
Continue button

The Continue button should remain disabled until valid-looking email and password values are entered.

Active state:
- Medium ChatPop blue
- White text

==================================================
7. KEYBOARD / INPUT BEHAVIOR
==================================================

These screens must work correctly on real iPhone.

Make sure:

- Inputs use appropriate iOS keyboard types.
- Email uses email keyboard/autocapitalization behavior.
- Password uses secure entry.
- Keyboard never covers the active field or Continue button.
- Safe Areas are respected.
- Inputs move/scroll appropriately when the keyboard opens.
- Tapping outside can dismiss the keyboard where appropriate.

==================================================
8. NATIVE IOS DESIGN
==================================================

Use native iOS behavior/components where practical.

Especially:

- Back
- Close X
- Eye / password visibility
- Checkboxes/toggles where appropriate
- Navigation
- Keyboard handling
- Input behavior
- Glass/material surfaces

Prefer SF Symbols for system icons.

Do NOT manually draw approximations if an appropriate native iOS symbol/control already exists.

==================================================
9. IMPORTANT — UI ONLY
==================================================

For this task, focus on the authentication UI/flow.

Do NOT build a new authentication backend.

Do NOT invent API endpoints.

Do NOT store passwords insecurely.

Do NOT place authentication secrets in the frontend.

We will connect the real backend/authentication later.

==================================================
10. FLOW
==================================================

The flow should be:

Settings
↓
Sign In
↓
Log in or sign up
↓
┌ Continue with Apple
├ Continue with Google
├ Continue with Microsoft
│
├ Log in → Login screen
│
└ Sign up → Create Account screen

Back returns one level.

X closes the authentication flow and returns to Settings.

==================================================
11. EXISTING IOS PROJECT
==================================================

Preserve the existing working ChatPop iOS project.

DO NOT:

- Delete /ios
- Recreate iOS
- Run npx cap add ios
- Create another Xcode project
- Change Bundle Identifier
- Change Development Team
- Change Signing
- Reset Capacitor

Use the existing native integration.

You may add compatible packages/native components only if required for proper native iOS UI.

==================================================
12. FINISH
==================================================

After implementation:

1. Build the frontend.
2. Check for errors.
3. Sync the EXISTING iOS project.
4. Verify the current native iOS integration is preserved.
5. STOP.

Report:
- Components/screens added
- Files changed
- Native components/packages used
- Build result
- Capacitor sync result

Do not implement the real authentication backend yet.