Task: Redesign ONLY the main Schedule list page and add custom schedule naming.

Use the existing Reminders page as the visual reference.

IMPORTANT:
Do not redesign unrelated pages.
Do not break existing schedule data or schedule navigation.
Keep existing functionality unless explicitly changed below.

==================================================
1. MATCH THE REMINDERS PAGE VISUALLY
==================================================

The Schedule page should use the SAME visual system already used on the Reminders page.

Reuse the same components/styles where possible instead of creating another different design.

Match these elements:

- Back button
- Plus button
- Search field
- Main notification card
- Schedule cards
- Three-dot action button
- Card spacing
- Corner radius
- Dark material/glass appearance
- Typography hierarchy
- Borders
- Padding
- Shadows/material treatment

The Schedule page and Reminders page should clearly look like they belong to the same app and same design system.

Do NOT make an approximate copy if the existing Reminders components can be reused.

==================================================
2. TOP NAVIGATION
==================================================

Keep the title:

Schedule

The back button and + button must use the same native/iOS-inspired button style already used on Reminders.

Same size.
Same material.
Same icon weight.
Same spacing.

==================================================
3. SCHEDULE NOTIFICATIONS CARD
==================================================

The "Schedule Notifications" card should visually match the Reminder Notifications card.

Same layout.
Same glass/material styling.
Same button dimensions.
Same spacing.

Only the content stays schedule-specific.

==================================================
4. SEARCH BAR
==================================================

Make the Search schedules field visually identical to Search reminders.

The search must search schedules by their CUSTOM SCHEDULE NAME.

Example:

User creates a schedule named:

"محاضرات"

Then the card must display:

محاضرات

If the user searches:

محاضرات

that schedule must appear.

Do not search only by the generic label "Weekly Schedule".

Search should be case-insensitive where relevant and support Unicode text such as Arabic and Swedish.

==================================================
5. REMOVE THE GENERIC NAME PROBLEM
==================================================

Right now multiple cards display:

"Weekly Schedule"

This makes schedules impossible to distinguish.

This must change.

Every schedule must have its own saved custom name.

Add a field such as:

schedule.name
or
schedule.title

Do NOT overwrite all schedules with one shared label.

Each schedule record must preserve its own name.

==================================================
6. PLUS BUTTON — ASK FOR SCHEDULE NAME FIRST
==================================================

When the user presses the + button:

Do NOT immediately create another schedule named "Weekly Schedule".

First ask the user to name the schedule.

Use a real native iOS prompt/input if the native iOS bridge already supports this.

The prompt could say:

Title:
"New Schedule"

Message:
"Give this schedule a name."

Text field placeholder:
"Schedule name"

Buttons:
Cancel
Continue

Example:

User types:

محاضرات

Then Continue.

The newly created schedule must store:

name = "محاضرات"

and the Schedule list card must display:

محاضرات

If the user types:

University

the card displays:

University

If the user types:

جدول رمضان

the card displays exactly:

جدول رمضان

Support all normal languages and Unicode text.

==================================================
7. VALIDATION
==================================================

Do not allow a completely empty schedule name.

Trim leading/trailing whitespace.

If the name is empty after trimming:
do not create the schedule.

Do not silently fall back to "Weekly Schedule".

==================================================
8. EXISTING OLD SCHEDULES
==================================================

Do not delete existing schedules.

For existing records that currently do not have a custom name, preserve them safely.

They may temporarily display:

Weekly Schedule

as a legacy fallback.

But ALL newly created schedules must have an actual custom name.

Do not migrate or destroy existing schedule contents just to add the name field.

==================================================
9. SCHEDULE CARDS
==================================================

Make schedule cards visually match reminder cards.

Each card should show:

CUSTOM SCHEDULE NAME
Event count
Created date/time

Example:

محاضرات
4 Events

Created:
Friday, Aug 28
02:46 PM

The custom name should be the strongest text hierarchy.

The card must not move horizontally, overflow, or change layout depending on title length.

Long titles should truncate or wrap gracefully without breaking the card.

==================================================
10. THREE-DOT ACTION MENU
==================================================

The three-dot button should use the same button style used on reminder cards.

When tapped, use the same iOS/native action presentation already used for Reminders.

Do NOT make a new unrelated custom popup.

Schedule actions should contain the relevant content:

Open
Delete

Keep destructive Delete styled correctly as destructive/red.

The shell/appearance should match the Reminders action menu, but the options remain Schedule-specific.

==================================================
11. SEARCH LOGIC
==================================================

Update search filtering to use the schedule's saved custom name.

Example stored schedules:

"محاضرات"
"Work"
"Gym"
"جدول رمضان"

Search "work" -> Work
Search "محاضرات" -> محاضرات
Search "رمضان" -> جدول رمضان

Do not rely on card DOM text or generic labels.
Search the actual saved schedule data.

==================================================
12. DATA PERSISTENCE
==================================================

The custom schedule name must persist.

After:
- closing the Schedule page
- closing the app
- reopening the app
- reopening Schedule

the same schedule name must still be there.

Do not store the name only in temporary component state.

Persist it together with the schedule data using the existing schedule persistence architecture.

==================================================
13. DO NOT TOUCH THE NEXT SCHEDULE EDITOR YET
==================================================

For this task, focus on the Schedule LIST page only:

- matching Reminders design
- custom naming
- saving schedule names
- searching by name
- card display
- native/iOS action menu

Do NOT redesign the internal schedule editor/form yet.

We will handle that in a separate task.

==================================================
DELIVERABLE
==================================================

When finished, report:

1. What existing Reminders components/styles were reused.
2. Where the custom schedule name is stored.
3. How old schedules without names are handled.
4. How search now filters schedules.
5. Confirmation that newly created schedule names persist after app restart.
6. Confirmation that no unrelated screens were changed.
