Face ID authentication now works correctly, but the session flow is wrong.

Current bug:

1. I open the app.
2. The app sends me to the Sign In screen.
3. I enter my email.
4. Face ID appears and succeeds.
5. It says authentication succeeded.
6. But instead of restoring my existing session and entering BlueMind, it starts the login flow again from the beginning.

Fix ONLY this authentication/session bug. Do not redesign the login UI.

Required behavior:

* If the user already has a valid authenticated session/token stored, reopening the app should NOT show the Sign In screen.
* If Face ID protection is enabled, app launch should show Face ID first.
* After Face ID succeeds, immediately restore the existing authenticated session and navigate to the app/home/chat.
* Do NOT ask for the email again unless there is no saved account/session.
* Face ID must act as a local unlock for an existing signed-in account, not as a new login method that restarts authentication.
* If the access token expired but a valid refresh token exists, refresh the session in the background after Face ID succeeds.
* Only send the user to Sign In if there is genuinely no valid session/refresh token, the account was signed out, or session restoration fails.
* Do not clear auth storage when the app goes to background or is closed normally.

Trace these exact states:
Face ID success → read stored auth/session → validate/refresh token → restore user → navigate into app.

Check for any redirect loop between the Face ID gate, Sign In page, auth provider, and app initialization.

After fixing it, test this exact case:

1. Sign in normally once.
2. Enable Face ID.
3. Fully close the app.
4. Reopen it.
5. Face ID should appear immediately.
6. After successful Face ID, BlueMind should open directly without asking for email/password.

Then tell me exactly what was causing the loop and what you changed.