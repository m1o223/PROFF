TASK: Make “Ask BlueMind” appear immediately in the FIRST native iOS text-selection menu.

CURRENT PROBLEM:
The native integration is now working, but “Ask BlueMind” is hidden behind the iOS overflow / next-arrow menu.

The user has to tap the > arrow first before they can see Ask BlueMind.

This is not the desired UX.

DESIRED BEHAVIOR:
As soon as the user selects text, “Ask BlueMind” must already be visible in the FIRST native iOS edit menu.

The user should not need to:
- tap >
- open More
- open a secondary menu
- discover the action manually

Target behavior should conceptually be:

[ Ask BlueMind ] [ Copy ] [ Look Up ] [ Translate ] [ > ]

or another order allowed by iOS, but Ask BlueMind must be in the primary visible group.

IMPORTANT:
Keep using the REAL native iOS text-selection menu.

Do NOT bring back the custom HTML/CSS BlueMind bubble.

==================================================
PRIORITY
==================================================

Treat Ask BlueMind as a primary custom edit action.

When building/configuring the native edit menu:
- insert Ask BlueMind at the top-level menu
- prioritize it before secondary/system suggestions where the API allows
- avoid placing it inside a submenu
- avoid placing it after the overflow group if possible

Prefer:
Ask BlueMind
Copy
Look Up
Translate
...

rather than:
Copy
Writing Tools
Look Up
Translate
Search Web
Share
Ask BlueMind

==================================================
INTERACTION
==================================================

When text is selected:

1. native iOS menu appears
2. Ask BlueMind is immediately visible
3. user taps Ask BlueMind
4. capture selected text
5. send/pass it to the existing BlueMind action
6. dismiss native menu normally

No second menu.
No additional tap required.

==================================================
IF IOS CONTROLS FINAL ORDERING
==================================================

If iOS does not allow absolute ordering of all system actions:

- use the highest-priority supported top-level insertion
- ensure Ask BlueMind is not nested
- ensure it is eligible to appear in the first visible page
- keep the title concise: “Ask BlueMind”
- keep the action icon lightweight so it fits

Do not fake the menu just to force placement.

==================================================
TEST ON PHYSICAL IPHONE
==================================================

Test:

1. select a short English phrase
2. verify Ask BlueMind is visible immediately
3. do not tap >
4. select a long English paragraph
5. verify behavior
6. select Arabic/RTL text
7. verify Ask BlueMind is still immediately available
8. verify Copy / Look Up / Translate continue working
9. verify there is still only ONE native iOS menu

Report:
- native API used
- how Ask BlueMind was promoted to top-level
- whether iOS imposes any ordering limitation
- physical iPhone result
